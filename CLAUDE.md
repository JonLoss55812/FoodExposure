# CLAUDE.md

Guidance for Claude Code when working on TongueTutor (FoodExposure).

## Project Overview

**TongueTutor** is a mobile app (Expo + React Native) for tracking children's food exposure and literacy. Parents log foods their child has tried, mark allergies/aversions, and track nutrition/behavior over time. The app uses local SQLite storage with Convex backend support (future). Built for iOS and Android.

**Current state:** Core app stable (1.0.0) with 130+ tests passing, good coverage. Backend uses Convex + Drizzle ORM. Recently refactored with input validation, error handling, N+1 query fixes, and SafeAreaView. Ready for feature expansion or deployment.

## Tech Stack

| Layer | Tech | Details |
|-------|------|---------|
| **Mobile App** | Expo 55, React Native 0.83, TypeScript, Unistyles 3.1, React Query 5 | Typed routes, bottom tabs (settings/foods/progress/log) |
| **Local Storage** | SQLite (expo-sqlite), react-native-mmkv | Per-child food log, allergen tracking |
| **Backend** | Convex + Drizzle ORM | Real-time sync (future) |
| **Testing** | Jest 30, React Native Testing Library, Jest-Expo | 130+ tests, 6 suites |
| **Runtime** | Bun (package manager + scripts, not npm) | `bun.lock` included |
| **Sentry** | Error tracking + session tracking | Configured in app.json plugins |

## Key Features

- **Onboarding:** Add child (name, age, photo)
- **Food Log:** Log foods tried, date, reaction notes
- **Allergen Tracking:** Mark known allergies and aversions
- **Progress Charts:** Visualize exposure over time (with trends)
- **Settings:** App config, data export, privacy
- **Multi-child:** Support multiple children (future roadmap)

## Directory Structure

```
app/
├── (tabs)/              # Main tabbed navigation
│   ├── index.tsx       (Foods — main list)
│   ├── foods.tsx       (Foods tab)
│   ├── progress.tsx    (Charts + trends)
│   ├── log.tsx         (Activity log)
│   └── settings.tsx    (App settings)
├── food/               # Food detail pages
│   ├── add.tsx         (Add new food)
│   ├── [id].tsx        (View/edit food)
│   └── _layout.tsx
├── child/              # Child management
│   ├── add.tsx         (Add child)
│   ├── [id].tsx        (Edit child)
│   └── _layout.tsx
├── onboarding/         # First-time user flow
│   ├── index.tsx       (Start)
│   ├── add-child.tsx
│   └── join.tsx
└── _layout.tsx         # Root layout

src/
├── hooks/              # Custom hooks (useFood, useChild, etc.)
├── components/         # Reusable UI components
├── utils/              # Helpers (date, calculation)
├── db/                 # SQLite setup + queries
├── theme/              # Unistyles theme tokens
└── types/              # TypeScript types

convex/                # Backend functions (future)
├── schema.ts          (Drizzle ORM)
└── functions.ts       (API routes)

tests/                 # Jest test suites
├── components/
├── db/
├── hooks/
└── utils/
```

## How to Run

**Mobile (iOS Simulator):**
```bash
bun install
bun ios
# Opens Expo Go on iOS simulator
```

**Android:**
```bash
bun android
```

**Web:**
```bash
bun web
```

**Tests:**
```bash
bun run test            # Run all tests
bun run test:watch      # Watch mode
bun run test:coverage   # Coverage report
```

## Important Notes

- **Bun, not npm:** This project uses Bun as the package manager. All scripts use `bun run`.
- **Never `bun test`:** bare `bun test` invokes Bun's *built-in* test runner, which hangs on
  this jest-expo suite. Always `bun run test` (or `npx jest`) so the `package.json` script runs.
- **Fallback install:** if Bun is unavailable, plain `npm install` works (no `--legacy-peer-deps`
  needed as of v0.5.143). `node_modules/` and `package-lock.json` are gitignored.
- **SQLite on device:** Data is stored locally in SQLite. Sync to Convex is planned.
- **Expo Router:** Typed routes enabled (`experiments.typedRoutes: true`). Routes must match file structure.
- **Unistyles 3.1:** NitroModules required. Cannot use Expo Go — must use `bun ios` / `bun android`.
- **Multi-child support:** Schema designed for multiple children, but UI currently assumes single child (refactor in progress).

## Database Schema (SQLite)

```sql
children (id, name, birth_date, photo_url)
foods (id, name, category, allergen_flags)
logs (id, child_id, food_id, date, reaction, notes)
```

See `src/db/` for detailed setup.

## Testing

- **130+ tests** across 6 suites (components, db, hooks, utils)
- **43.8% line coverage** (good for MVP)
- Use `jest --testPathPattern=src/components` to run component tests only
- All tests use React Native Testing Library conventions

## Conventions

- **Allergen flags:** Store as comma-separated in schema, parse as array in app
- **Child reference:** All logs include child_id (future multi-child support)
- **Date format:** ISO 8601 in database, formatted on display
- **Navigation:** Use Expo Router's `Link` component, not React Navigation directly
- **Styles:** Unistyles tokens in `src/theme/themes.ts` (not inline or Tailwind)

## Key Hooks

- `useChild()` — Get current child from context
- `useFood(id)` — Fetch food by ID
- `useFoods()` — List all foods
- `useLog()` — Activity log
- `useProgress()` — Calculate exposure trends

## Build & Deployment

```bash
# EAS build (Expo's CI/CD)
bun run eas:build:ios
bun run eas:build:android

# Publish to App Store / Play Store
# (Set up with EAS Account)
```

See `eas.json` for build profiles.

## Known Limitations

- Single child assumption in UI (refactor planned)
- No cloud sync yet (Convex wiring in progress)
- Charts are basic (could add more detail)
- No multi-user / parental consent flows

## Next Priority

v1.0.0 is stable with 130+ tests passing. Next: wire Convex backend to replace local-only SQLite storage.

1. Verify tests pass: `bun run test` — expect 570+, 28 suites, no failures
2. Inspect `convex/schema.ts` and `convex/functions.ts` — see what's scaffolded
3. Pick first endpoint to wire: `fetchChildren` (simplest) → `addFood` → `logFood`
4. Update `src/hooks/useChild.ts` (or similar) to call Convex instead of SQLite
5. Keep SQLite as fallback during transition — do not remove local storage yet
6. Commit: `feat: wire $ENDPOINT endpoint to Convex backend`
7. Update this file with next endpoint

**Do not:** break existing tests, change `app.json`/`eas.json` without understanding EAS pipeline, deploy to App Store until all endpoints are wired.

## Research Background (Food Exposure Methods)

The app is grounded in evidence-based pediatric feeding therapy. Key findings:

**Why 8–15+ exposures matter:** Most parents give up after 3–5 attempts. Research shows typical children need 8–15 exposures for acceptance; picky eaters need 15–20+; ARFID/problem feeders need 20–30+.

**An "exposure" is not just tasting** — it includes seeing, smelling, touching, and being near the food.

**Core principles (common across SOS, Food Chaining, Three E's):**
- No pressure — never force, coerce, or bribe
- Repeated neutral exposure at a child-led pace
- Multisensory progression (sight → smell → touch → taste → eat)
- Preferred/safe foods always available alongside new foods
- Consistency over intensity

**Exposure hierarchy (SOS 6 categories):** Tolerate → Interact → Smell → Touch → Taste → Eat

**Key data to track per food:** name/category, preparation method, texture, temperature, current stage in hierarchy, exposure count (goal: 15+), acceptance rating (1–5), reaction notes.

**Key data to track per session:** date/time, meal type, setting, foods presented, child mood, pressure level, who was present.

**Progress metrics:** food repertoire size, exposures per food vs. threshold, stage movement, food chains (accepted → target), regression tracking, consistency streaks.

Sources: SOS Approach (Dr. Kay Toomey), Food Chaining (Fraker et al.), The Three E's (Melanie Potock), Kids Feeding Wellness 5 Levels.

## Structure
src/ — source code
app/ — Expo Router pages


## Versioning Rules
- Include a version number in the bottom-right corner of the UI (e.g., `v1.0.0`)
- Follow semantic versioning: `MAJOR.MINOR.PATCH`
- Increment the version after every feature:
  - PATCH for bug fixes and minor tweaks
  - MINOR for new features
  - MAJOR for breaking changes or major redesigns
- Update this CLAUDE.md file after every commit with:
  - Current version number
  - Brief note on what changed

## Current Version
v0.5.150

## Changelog
- v0.5.150 — Tests: screen coverage for `app/(tabs)/log.tsx`, the last of NEXT_STEPS gap #1's named targets and the app's primary write path. Every exposure the app ever records is inserted by this one handler, and that row is what the 15/20/30 acceptance threshold, the stage distribution, and the therapist-facing CSV export are all computed from — yet the handler shipped screen-level and unverified. New `app/(tabs)/__tests__/log.test.tsx` with 9 tests, built on the v0.5.148 tab-screen seams verbatim (`useFocusEffect` mocked to a plain `useEffect`, `SafeAreaProvider` with static `initialMetrics`, getter-based db mock, mock-prefixed router spy) plus one small helper, `queueLoad(children, foods)`, which names the fact that `loadData` issues its two reads in a fixed order. Coverage: the loaded children and foods render as selectable chips; an empty food list shows the v0.5.54 add-food CTA instead; Save with no food selected surfaces the inline `Select a food` error and issues zero writes, and likewise for no child selected — a blank id would insert an exposure row that every downstream join drops silently, so the screen must never reach the insert; the happy path persists childId/foodId/stage/loggedBy and lands the untouched optional dimensions as `null` rather than `''` (they are `.optional()` in the schema and nullable in SQLite, so "not recorded" has to round-trip as absent); the stage defaults to `tolerate`, the entry level of the hierarchy; after a successful save the v0.5.7 reset keeps the child but clears the food (a parent logs several short exposures per meal, so re-tapping the child every time is friction — but keeping the *food* would let an accidental second Save double-count toward the acceptance threshold); a failed load alerts and renders no chips; and a failed insert alerts while leaving the form intact, so the retry does not re-surface the validation error. Mutation-verified rather than assumed: clearing the child in the reset, keeping the food in the reset, changing the default stage, swallowing either Alert, nulling `loggedBy`, and mapping an absent rating to `0` instead of `null` each fail exactly one test. Two limits recorded rather than papered over: (a) react-native-web does not serialize `accessibilityState.selected`, so the chip highlight is not directly assertable — the retry-without-a-validation-error is the observable stand-in; (b) the v0.5.142 `resolveSelectedFoodId` repair is *not* covered through the screen, because the harness mounts once and a second focus load is therefore unreachable; its semantics stay owned by the 7 unit tests in `food-partition.test.ts`, and the gap is now written down in NEXT_STEPS rather than assumed covered. Bumped `APP_VERSION` to v0.5.150. 634 tests pass across 34 suites (was 625 across 33, +9). TypeScript clean.
- v0.5.149 — Tests: screen coverage for `app/food/add.tsx`, NEXT_STEPS gap #1's next named target. The load-bearing untested logic here is the v0.5.136 duplicate-name guard: a second `foods` row for the same food silently splits the per-food exposure count that the 15/20/30 acceptance threshold is read from, forks stage progression, and shows the therapist two part-histories in the CSV export — and the split is invisible until it can no longer be fixed cheaply (gap #2: no dedupe migration exists). The guard shipped screen-level and therefore unverified. New `app/food/__tests__/add.test.tsx` with 10 tests on the v0.5.146 harness (`createMockDb` + the four documented seams; no `SafeAreaProvider` needed — this screen is a plain `ScrollView`). Coverage: the name field, category grid, and submit action render; a whitespace-only name is rejected by `foodSchema` and issues zero queries (the schema owns that rejection, so the screen must not reach the DB at all); a case-insensitive duplicate (`apple` vs stored `Apple`) alerts Already Added, names the colliding row, and writes nothing; a whitespace-padded duplicate is likewise blocked; the happy path inserts the *trimmed* name scoped to `familyId` with the expected category/isSafeFood/defaultPreparation defaults; a selected category chip and the safe-food toggle both persist; the v0.5.16 Add Another button clears the name field without navigating; Done navigates back; a failed duplicate lookup alerts and — the important half — does **not** fall through to the insert, which would be a back door around the guard on exactly the flaky path; and the v0.5.144 latch is released after a failure so a retry re-queries. Mutation-verified rather than assumed: deleting the duplicate guard fails 2 tests, wrong `familyId` on the insert fails 1, swallowing the error Alert fails 1, dropping `submitLatch.release()` fails 1, and dropping the Add Another `reset` fails 1. One new helper over the v0.5.147/v0.5.148 files: `pressAlertButton`, which invokes a *named* button on the most recent Alert (the confirm helper in the two delete-flow files reaches for a fixed button); the success Alert here carries two actions, so the tests have to distinguish them. Bumped `APP_VERSION` to v0.5.149. 625 tests pass across 33 suites (was 615 across 32, +10). TypeScript clean.
- v0.5.148 — Tests: screen coverage for the Settings tab's Delete Child flow (v0.5.139), the app's other irreversible cascade and NEXT_STEPS gap #1's third named target. Deleting a child discards every exposure that child ever logged, and the flow then has to repair the store's selection — if the deleted child was the selected one, `ensureSelection` must fall through to a remaining child, or to null when none remain (where the dashboard's EmptyState takes over). That repair is exactly what a hand-rolled `if (selectedChildId === child.id) clear()` gets wrong, and nothing exercised it through the screen. New `app/(tabs)/__tests__/settings.test.tsx` with 8 tests. Two new seams beyond the v0.5.146 set, both of which every future tab-screen test will need: `useFocusEffect` is mocked to a plain `useEffect` (the screen mounts once under the harness, so the focus-driven load would otherwise never run), and the screen is wrapped in a `SafeAreaProvider` with static `initialMetrics` (`SafeAreaView`'s hook throws outside a provider — this is what made the first run fail all 8). Coverage: each child in the family renders its own delete action; the confirm Alert names the child and says "permanently deleted"; Cancel writes nothing and leaves both the row and the selection intact; confirming issues three deletes (the dependents-first ordering stays pinned by `cascade-delete.test.ts` — this pins that the screen runs the cascade rather than a bare child delete that would orphan rows), drops the row, and moves the selection to the surviving child; deleting the last child clears the selection to null; deleting a *different* child leaves the selection alone; a cascade failure alerts and leaves the row present and the selection unchanged, i.e. retryable; an empty family renders no delete rows, only Add Child. Mutation-verified: dropping `ensureSelection(remaining)` fails 2 tests, swapping the cascade for a bare `db.delete(children)` fails 1, dropping `setChildrenList(remaining)` fails 2, swallowing the failure Alert fails 1, and removing the child's name from the confirm copy fails 1. Bumped `APP_VERSION` to v0.5.148. 615 tests pass across 32 suites (was 607 across 31, +8). TypeScript clean.
- v0.5.147 — Tests: screen coverage for `app/food/[id].tsx`, the NEXT_STEPS gap #1 target named first. This screen holds the two most dangerous untested paths in the app — the v0.5.145 inline rename (which must not become a back door around the v0.5.136 duplicate-name guard) and the v0.5.138 delete cascade (which irreversibly discards every exposure logged against the food, for every child) — and both shipped screen-level and therefore unverified. New `app/food/__tests__/id.test.tsx` with 13 tests, built on the v0.5.146 harness (`createMockDb` + the four documented seams), with `useLocalSearchParams` added to the `expo-router` mock alongside `useRouter`. Coverage: the loaded screen renders its rename affordance and delete action; a missing row shows the Food Not Found empty state rather than a blank screen; a failing read alerts rather than rendering that same empty state (the two are distinguishable to the user, per v0.5.33). Rename: a whitespace-only draft is rejected by `foodSchema.shape.name` and issues no write, leaving the editor open to correct in place; a draft equal to the stored name after trimming closes the editor without a single query; a collision with a *different* food alerts Already Added, names the colliding row, and writes nothing; a case-only fix (`Apple` → `APPLE`) is allowed, which is the load-bearing `excludeId` case — the food must not collide with itself; the happy path persists the trimmed name; a failed sibling read releases the latch so a retry re-queries; Cancel abandons the draft. Delete: the confirm Alert names the food and says "every child" (exposures are family-wide per food, so the copy must not let a parent assume the delete is scoped to the selected child); tapping Cancel writes nothing and does not navigate; confirming issues three deletes and replaces to the Foods tab — the dependents-first ordering itself stays pinned by `cascade-delete.test.ts`, while this test pins that the screen runs the cascade rather than a bare food delete. Every test was mutation-verified rather than assumed: deleting the duplicate-collision guard, dropping the `excludeId` argument, deleting the no-op-rename early return, swapping the cascade for a bare `db.delete(foods)`, and dropping the schema validation message each fail exactly one test. Known cosmetic limitation recorded for the next session: this screen loads in a `useEffect`, so React emits "not wrapped in act" warnings on mount even with the render awaited inside `act`; the warnings are console noise only and do not affect pass/fail. Bumped `APP_VERSION` to v0.5.147. 607 tests pass across 31 suites (was 594 across 30, +13). TypeScript clean.
- v0.5.146 — Tests: first screen render harness in the codebase (NEXT_STEPS gap #1 — the single biggest remaining test gap). Every `app/` screen has shipped untested since v0.1.0 because no jest harness existed for expo-router + drizzle + zustand/MMKV screens; dozens of changelog entries cite the resulting "ship-without-test pattern" for handlers, Alert wiring, and conditional empty states. This commit stands one up and proves it on the screen with the most load-bearing untested logic, `app/onboarding/join.tsx`. Two pieces. (1) New `src/test-utils/mock-db.ts` exporting `createMockDb()` — a structural fake of the drizzle expo-sqlite builder covering the five chains the screens actually use (`select().from().where()[.orderBy][.limit]` as a thenable, `insert().values()`, `update().set().where()`, `delete().where()`). Reads resolve from a FIFO queue of canned result sets in the order the screen issues them and fall back to `[]` once drained, so a test describes only the reads it cares about; writes are recorded so a test can assert what was persisted; `failReads()` drives a screen's catch block. A hand-rolled structural fake rather than a `jest.mock` of drizzle itself, because the seam the screens actually import is the `db` singleton from `@/src/db/client`, and the recorded-writes shape keeps assertions readable. Excluded from `collectCoverageFrom` (test-only code) so the 100% figure stays honest. (2) New `app/onboarding/__tests__/join.test.tsx` with 9 tests, documenting the four seams — `expo-router` mocked to a shared `mockRouter` spy (the `mock` prefix is mandatory: jest's out-of-scope-variable guard rejects a plain `router`), `@/src/db/client` mocked with a getter so each test's fresh `createMockDb()` is picked up, `Alert.alert` spied so alert *copy* is assertable, and the real zustand stores reset via `logout()` between tests. Coverage: both fields and the action render; a whitespace-only name blocks submission and issues zero queries; `ABCO23` is rejected by the v0.5.88 charset guard before any lookup (the precise error beats the misleading "No family found" the lookup would produce); a code matching no family alerts Not Found and writes nothing; the v0.5.91 duplicate-name pre-check blocks the insert and leaves the auth store signed out; the happy path inserts with the trimmed name and derived `anne@tonguetutor.app` email, populates the auth store, and navigates; a read failure alerts and does not navigate; the latch is released after a failure so a retry re-queries; the Button reports `aria-busy` while in flight. Every test was mutation-verified rather than assumed: deleting the charset guard, the not-found guard, the duplicate pre-check, the `displayName.trim()`, and the Button's `loading` prop each fail exactly one test. That sweep also *removed* a test — a two-tap "only one insert" case that passed with the v0.5.144 latch deleted, and still passed with the latch AND the `loading` prop both deleted, because react-native-web's Pressable never dispatches a second press while the first is in flight. The same-tick double-tap the latch exists for is therefore not reachable from a DOM-level harness at all; that limitation is now recorded in a comment at the assertion site, and the latch's semantics stay covered by the `in-flight.test.ts` unit tests. Shipping a green-but-vacuous test would have been worse than shipping none. Bumped `APP_VERSION` to v0.5.146. 594 tests pass across 30 suites (was 585 across 29, +9). TypeScript clean.
- v0.5.145 — Feature: rename a food from the food detail page (NEXT_STEPS gap #3). Until now a mistyped food name was permanent-or-destroyed: the v0.5.136 duplicate guard blocks re-adding the corrected spelling only when it collides, and the only removal path was the v0.5.138 Delete Food cascade — which discards every logged exposure for that food, across every child. A parent who typed "Brocolli" had to choose between living with the typo on every stat surface (Foods tab, Progress per-food rows, CSV export a therapist reads) or throwing away the exposure history that the 15/20/30 acceptance threshold is counted from. Rename fixes the label and keeps the history. The food name on `app/food/[id].tsx` is now a Pressable (with a ✏️ affordance) that swaps in an inline TextInput plus Save/Cancel; `handleSaveName` validates through `foodSchema.shape.name` rather than a hand-rolled check, so rename and Add Food agree on trim/min(1)/max(80) and the user sees the schema's own message. A no-op rename (draft equal to the stored name after trim) closes the editor without touching the DB. The load-bearing correctness half is the uniqueness guard: rename must not be a back door around v0.5.136, or a parent could rename "Pear" to "Apple" and re-create exactly the split-stats defect that commit closed — but the guard also must not reject a food colliding with *itself*, which is what a case-only fix ("apple" → "Apple") or a whitespace trim looks like to a case-insensitive comparison. So `findDuplicateFood` gains a third parameter `excludeId?: string | null` that skips one row by id; blank/whitespace/non-string excludeId means no exclusion, so every existing Add Food callsite is unchanged. The sibling query is scoped by `food.familyId` (read off the already-loaded row, no store plumbing) matching the family-scoped semantics of the add-path check. Double-submit is guarded by the v0.5.144 synchronous `createInFlightLatch`, released in `finally` so the duplicate-collision early return can't strand it; the collision leaves the editor open so the parent can correct the name in place instead of retyping from scratch. A11y follows the established train: `accessibilityLabel={'Rename ' + food.name}` on the trigger, `"Food name"` on the input, and `accessibilityState={{ disabled, busy }}` on Save. +6 tests in a new `findDuplicateFood — excludeId` describe block: the excluded row does not match its own name (the load-bearing case); a case-only and a whitespace-padded rename of the excluded row are both allowed; a genuine collision with a *different* row is still reported; blank/`''`/`'   '`/null/undefined/non-string excludeId all behave as no-exclusion (regression lock — a contributor "simplifying" the guard to `food.id !== excludeId` would let `undefined !== undefined` quietly disable it for id-less rows); and with pre-existing duplicates, excluding either twin still surfaces the other, so the helper skips exactly one row rather than all matches. The screen wiring itself still needs the render harness of NEXT_STEPS gap #1. Bumped `APP_VERSION` to v0.5.145. 585 tests pass across 29 suites (was 579, +6). TypeScript clean, including `--noUnusedLocals`.
- v0.5.144 — Hardening: every async write handler in the app is now guarded against re-entry by a *synchronous* latch instead of a render-lagged `useState` boolean. The established pattern (v0.5.93, and copied since) was `const [saving, setSaving] = useState(false)` plus `if (saving) return;` at the top of the handler, backed by `disabled={saving}` on the Button. Both halves lag: the `if` reads the value captured in the *current render's* closure and `setSaving(true)` only schedules a re-render, and the Pressable only becomes disabled once that re-render commits. Two taps dispatched in the same event batch (fast double-tap on a slow device, RN's known double-fire on a dropped frame) therefore both observe `saving === false` and both proceed into the insert. New `src/lib/in-flight.ts` exports `createInFlightLatch()` — a closure-held boolean with `tryAcquire()` / `release()` / `busy`, mutated synchronously, so the second caller in the same tick bails. Held per-handler via `useRef(createInFlightLatch()).current`. Two handlers had *no* guard at all and are the load-bearing fixes: (1) `app/onboarding/join.tsx handleJoin` set `loading` but never checked it — two runs both pass the v0.5.91 duplicate-email pre-check (neither can see the other's uncommitted insert; classic check-then-act), and `users.email` carries no UNIQUE constraint, so both inserts land and the second `login()` overwrites the first, leaving an orphan `users` row that the future Convex sync layer would replicate as a ghost family member — precisely the failure v0.5.91 set out to prevent; (2) `app/(tabs)/log.tsx onSubmit` likewise, producing two `exposures` rows for one exposure, double-counting toward the 15/20/30 acceptance threshold (the app's core clinical number) and showing the event twice in the CSV a therapist reads — plus the missing `disabled={saving}` that every other submit button carries. The other six handlers had the lagged guard and were converted for uniformity: `food/add`, `child/add`, `onboarding/add-child`, `onboarding/index` (families + users), `food/[id]` bump-stage and delete-food cascade, `settings` delete-child cascade; `settings handleExport` had no guard at all and gained one. Placement detail for the two destructive confirm flows: the latch is acquired inside the Alert's Delete callback, not in the function that opens the Alert — acquiring at the top would strand the latch forever the moment a parent taps Cancel, permanently bricking delete. Those outer functions read `.busy` instead, which is a read, not a take. +7 tests in a new `in-flight.test.ts`: first acquire grants and marks busy; three `tryAcquire()` calls in one synchronous tick return `[true, false, false]` (the load-bearing case — exactly what a `useState` guard fails); re-grant after release; release-without-acquire is a no-op; independent latches don't share state; and an async handler fired twice before the first resolves runs its body exactly once and is reusable afterwards. The screen wiring itself still needs the render harness of NEXT_STEPS gap #1 — the latch semantics are what's unit-tested. Bumped `APP_VERSION` to v0.5.144. 579 tests pass across 29 suites (was 572 across 28, +7). TypeScript clean, including `--noUnusedLocals`.
- v0.5.143 — Chore: make a fresh checkout install and test without workarounds (NEXT_STEPS gap #6), and correct the documented test command. Three coordinated dependency/doc fixes, each verified against a wiped `node_modules` + `package-lock.json`. (1) Dropped `@testing-library/jest-native@^5.4.3`. The package is deprecated upstream (its matchers were folded into `@testing-library/react-native` v12.4+), it is imported nowhere in the repo, and `jest.config.js` declares no `setupFilesAfterEach` — so its matchers were never even registered. It was, however, the first thing to break `npm install`: it drags in `react-test-renderer@19.2.8`, whose peer range `react@^19.2.8` cannot be satisfied by the project's pinned `react@19.2.0`. (2) Pinned `react-test-renderer` to `19.2.0` as an explicit devDependency. Removing jest-native only moved the same conflict onto `@testing-library/react-native@13.3.3`, which peer-wants `react-test-renderer >=18.2.0` and lets npm float it to 19.2.8 — same unsatisfiable react peer. Pinning it to the exact React version resolves the graph while keeping RNTL installed, which matters: the screen-render harness of NEXT_STEPS gap #1 is the next big test investment and RNTL is the library it will be built on, so removing it (also currently unimported — the suite runs on the `jest-expo/web` preset via `@testing-library/react`) would have taken away the tool the next session needs. (3) Added `babel-preset-expo@~55.0.25` as an explicit devDependency. With the peer conflict resolved, npm's strict (non-`--legacy-peer-deps`) tree nests `babel-preset-expo` under `expo/node_modules` instead of hoisting it, and `babel.config.js` names the preset by bare specifier from the project root — so every one of the 28 suites failed to *run* with `Cannot find module 'babel-preset-expo'` while reporting zero tests, the most misleading possible green-looking red. A project whose own babel config names a preset should depend on it directly rather than relying on a transitive hoist; the `~55.0.25` range matches what `expo@55` resolves to. Net: `npm install` from a wiped tree now exits 0 and all 572 tests pass, where before it exited 1 on ERESOLVE and needed `--legacy-peer-deps`. Docs corrected alongside: the "How to Run" block said `bun test`, which invokes Bun's *built-in* test runner and hangs on this jest-expo suite — now `bun run test` / `bun run test:watch` / `bun run test:coverage`, with a matching "Never `bun test`" note added to Important Notes plus the npm fallback; the stale "expect 130+, 6 suites" line in Next Priority is now "570+, 28 suites". `bun.lock` is intentionally left untouched (Bun is not installed in this environment to regenerate it faithfully); plain `bun install` re-resolves the three-line `package.json` delta on the next Bun run. Bumped `APP_VERSION` to v0.5.143. 572 tests pass across 28 suites — unchanged, which is the point: this commit changes how you get to green, not what green is. TypeScript clean.
- v0.5.142 — Fix: the Log Exposure form now reloads its child/food lists on focus, and repairs a selected food that no longer exists (NEXT_STEPS gap #5). `app/(tabs)/log.tsx` was the one screen v0.5.140 skipped — it is a form, not a data list, so it kept its mount-only `useEffect`. The staleness is user-visible on the form's own happy path: tapping the "+ Add New" link in the Food section header pushes `/food/add`, and on return the chip row still does not contain the just-added food until the tab remounts, which reads as the add having failed (the same misread v0.5.140 fixed for a deleted food on the Foods tab). Same one-line swap as the other three tabs: `loadData` becomes `useCallback`-memoized on `[familyId, setValue, getValues]` and the mount `useEffect` becomes `useFocusEffect(useCallback(() => { loadData(); }, [loadData]))`. No `hasLoadedRef` spinner guard is needed here — unlike the tab screens, this form has no full-screen `ActivityIndicator` to flash; it renders the chip rows directly. The form's own state (`childId`/`stage`/optional details) is deliberately untouched by the reload, so a half-filled form survives a trip to Add Food and back. One new correctness guard rides along: the form outlives the food list, so a `foodId` selected before the reload can name a food deleted since (via the v0.5.138 Delete Food flow, reachable from the food detail page). Previously the id survived in form state with no chip highlighted, and Save would insert an `exposures` row pointing at a nonexistent food — an orphan that every downstream join then drops silently. After each reload the selection is run through a new pure helper `resolveSelectedFoodId(foods, selectedFoodId)` in `src/lib/food-partition.ts`, which returns the id unchanged when it still resolves and `''` (the form's "nothing selected" default, which the v0.5.1 inline `errors.foodId` message already covers) when it does not; blank/null/undefined/non-string ids normalize to `''` per the established v0.5.118/v0.5.135 typeof-guard idiom. Extracting the repair as a pure function is what makes this commit verifiable at all — the focus wiring itself still needs the screen harness of gap #1, but the load-bearing data-integrity half is unit-tested. +7 tests in a new `resolveSelectedFoodId` describe block: selection survives when present; clears when the food was deleted; clears against an empty list; `''`/null/undefined/non-string normalize to `''`; and exact-match locks (`'A'` does not match `'a'`, `'ab'` does not match `'abc'`) so a contributor "simplifying" the check to a prefix or case-insensitive comparison fails loudly. Bumped `APP_VERSION` to v0.5.142. 572 tests pass across 28 suites (was 565, +7). TypeScript clean, including `--noUnusedLocals`.
- v0.5.141 — Refactor + tests: extract the two destructive delete cascades into `src/lib/cascade-delete.ts` and cover them with unit tests. The v0.5.138 (Delete Food) and v0.5.139 (Delete Child) flows each issued three sequential `db.delete(...).where(...)` calls inline inside a screen-level Alert callback — the most dangerous code in the app (irreversible data loss) and, per NEXT_STEPS gap #1, entirely unverified: with no screen render harness there was no test anywhere asserting that the deletes run dependents-first, or that each one is scoped to the right column. A typo like `eq(exposures.childId, foodId)` in the food cascade would silently wipe an unrelated child's entire exposure history and no test would notice. Extracted `deleteFoodCascade(db, foodId)` and `deleteChildCascade(db, childId)`, both taking the database through a narrow structural `CascadeDeleteDb` interface (`delete: <TTable extends SQLiteTable>(table) => { where: (condition: SQL | undefined) => PromiseLike<unknown> }`) — narrow enough that a fake runner satisfies it in tests, wide enough that the real drizzle expo-sqlite instance assigns to it without a cast. Behavior is byte-for-byte the same as the inline code (same tables, same order, same predicates, including the `or(eq(sourceFoodId), eq(targetFoodId))` sweep that food_chains needs because it references foods twice), plus one addition: both helpers throw on an empty/whitespace/non-string id before issuing any delete, so a drifted id can't reach the DB layer as a silently-matches-nothing no-op. The screens keep their existing confirm Alert, in-flight guard, and log-then-toast catch — only the three delete lines moved. +8 tests in a new `cascade-delete.test.ts` driving a recording fake db: dependents-first table order for both cascades; exact per-step predicate equality (`toEqual` against a locally-constructed `eq`/`or` — verified non-vacuous, drizzle condition objects do compare unequal across different columns and different values, so a wrong-column typo fails loudly); mid-sequence failure stops the chain so the parent row survives in a retryable state (asserted for both a first-step and a second-step failure); and the id guard rejecting `''`/`'   '`/null/undefined/number/object without issuing a single delete. Bumped `APP_VERSION` to v0.5.141. 565 tests pass across 28 suites (was 557 across 27, +8). TypeScript clean, including `--noUnusedLocals` (the now-unused `or` import dropped from `app/food/[id].tsx`).
- v0.5.140 — Fix: the three data tabs (dashboard, Foods, Progress) now reload on focus instead of on mount only (NEXT_STEPS gap #4). Before this commit every tab loaded its data once per mount and otherwise only on pull-to-refresh, so a cross-screen write left the tab showing stale rows indefinitely: a food deleted via the v0.5.138 Delete Food flow still rendered on the Foods tab (and still counted toward the Progress tab's per-food rows and the dashboard's Foods Tracked card) until the parent happened to pull-to-refresh — reading as "the delete silently failed". Same for a child deleted via v0.5.139, and for the ordinary Add Food / Log Exposure round trip. Settings (v0.5.139) already demonstrated the `useFocusEffect` pattern; this applies it to the remaining three. Each tab swaps its `useEffect(() => { load(); }, [load])` for `useFocusEffect(useCallback(() => { load(); }, [load]))` — the load callbacks were already `useCallback`-memoized on their real dependencies (`familyId`/`selectedChildId`/`feedingProfile`), so focus reloads don't fire redundantly on re-render. The dashboard's auth-redirect stays in its own `useEffect` (it is auth-state-driven, not focus-driven) and the focus effect early-returns when unauthenticated, so an unauthenticated focus can't kick off a family query behind the redirect. The load-spinner semantics needed a guard: naively re-running `setLoading(true)` on every focus would flash the full-screen `ActivityIndicator` on every tab switch — a visible regression, since the whole point is a quiet background refresh. Added a `hasLoadedRef` (a `useRef(false)` set in each load's `finally`, and on the progress tab's `!familyId || !selectedChildId` early-return) that gates `setLoading(true)` to the first load only; subsequent focus reloads swap the data in silently, matching the existing pull-to-refresh precedent (which also never shows the full-screen spinner). No new tests — screen-level effect wiring needing a render harness (NEXT_STEPS gap #1). Bumped `APP_VERSION` to v0.5.140. 557 tests pass across 27 suites. TypeScript clean.
- v0.5.139 — Feature: Delete Child from Settings → Family (second half of the NEXT_STEPS "no food/child delete UI" gap; pairs with v0.5.138's Delete Food). The Family card previously contained only the Add Child row — a mistyped or test child persisted forever, and on a shared device the only cleanup path was reinstalling the app. Settings now loads the family's children (`{id, name, avatarEmoji}` projection scoped by `eq(children.familyId, familyId)`) and renders each as a row (`{avatarEmoji} {name}` + an error-colored "Delete" pressable) above the existing Add Child row. Loading runs through expo-router's `useFocusEffect` rather than a mount-only `useEffect` — the first focus-based reload in the codebase, deliberately: the list can change from the pushed `/child/add` screen, and a just-added child never appearing in the Family card until app restart would read as data loss (the mount-only staleness the tab screens tolerate via pull-to-refresh has no equivalent affordance inside a Settings card). Delete flow mirrors v0.5.138: destructive confirm Alert with blast-radius copy ("all of their logged exposures will be permanently deleted"), then dependents-first deletes — `food_chains` by childId, `exposures` by childId, then the `children` row — then the local list updates and `ensureSelection(remaining)` runs against the fresh list. Routing selection-repair through the store's existing `ensureSelection` (rather than a manual `if (selectedChildId === child.id) clear()`) is the load-bearing choice: it handles both the deleted-selected-child case (falls to the first remaining child, or null when none remain — the dashboard's no-children EmptyState takes over) and the deleted-other-child case (no-op) in one already-unit-tested code path. Double-tap/cross-row guard via `deletingChildId: string | null` — all Delete pressables disable while any delete is in flight (deleting two children concurrently could double-fire `ensureSelection` against stale lists); the in-flight row reads "Deleting…" with `accessibilityState={{ disabled, busy }}`. Catch follows the v0.5.34 log-then-toast idiom; the load path logs without an Alert (Settings renders fine with an empty list, and toasting on every tab focus would be noise — the card degrades to Add Child only, matching the pre-this-commit render). A11y per the established train: `accessibilityRole="button"` + `accessibilityLabel={'Delete ' + child.name}`. `handleLogout`'s `clearChildSelection` and `handleExport`'s `selectedChildId` usages are untouched. No new tests — screen-level Alert-callback handler needing a render harness (NEXT_STEPS gap #1); `ensureSelection`'s repair semantics are already pinned by the child-store unit tests. Bumped `APP_VERSION` to v0.5.139. 557 tests pass across 27 suites. TypeScript clean.
- v0.5.138 — Feature: Delete Food from the food detail page (first half of the NEXT_STEPS "no food/child delete UI" gap). `app/food/[id].tsx` gains a destructive "Delete Food" action at the bottom of the screen, styled after the Settings tab's Sign Out button (error-tinted card, error text). Previously a mistyped food ("Brocolli") lived forever: the v0.5.136 duplicate-name guard blocks re-adding the corrected spelling only if it collides, and every stat surface (Foods tab, Progress per-food rows, CSV export) kept rendering the typo row with no removal path. Flow: tap → `Alert.alert('Delete Food?', ...)` confirm with explicit blast-radius copy ("all of its logged exposures for every child will be permanently deleted") because exposures are family-wide per food, not scoped to the selected child — the on-screen history count only shows the selected child's rows, so the copy must not let a parent assume the deletion is similarly scoped. On confirm, dependents-first delete order: `food_chains` rows where the food is source OR target (drizzle `or(eq, eq)` — the table references foods twice), then `exposures` by foodId, then the `foods` row, then `router.replace('/(tabs)/foods')`. Dependents-first means a mid-sequence failure leaves a retryable state (food still present, some dependents gone) rather than orphaned child rows; no transaction wrapper since the codebase's expo-sqlite usage is uniformly sequential-await (matches every existing write path). Double-tap guarded by a `deleting` state per the v0.5.93 pattern: guard at handler top, `setDeleting(true)` before the writes, reset only in the catch (on success the screen unmounts via replace, and the button meanwhile reads "Deleting…" with `accessibilityState={{ disabled, busy }}`). Catch follows the v0.5.34 log-then-toast idiom (`console.error('Failed to delete food:', err)` + Alert). A11y: `accessibilityRole="button"` + `accessibilityLabel={'Delete ' + food.name}` per the v0.5.17-v0.5.47 polish train. No new tests — screen-level Alert-callback handler needing a render harness; matches the established v0.5.93 / v0.5.91 ship-without-test pattern for screen handlers (NEXT_STEPS gap #1 tracks the missing harness). Bumped `APP_VERSION` to v0.5.138. 557 tests pass across 27 suites. TypeScript clean.
- v0.5.137 — Fix: optional chip dimensions can now be deselected by re-tapping the selected chip. Every optional single-select surface in the app — the `RatingPicker` (`src/components/RatingPicker.tsx`) plus the Meal/Temperature/Texture/Setting chip rows on the Log Exposure form (`app/(tabs)/log.tsx`) and the Default Preparation chips on Add Food (`app/food/add.tsx`) — previously called `onChange(value)` unconditionally, so once a parent tapped any option there was no path back to "unset": tapping the highlighted chip re-set the same value. All six dimensions are `.optional()` in the zod schemas and nullable in SQLite (the DB writes are `data.X ?? null`), so "not recorded" is a first-class state the UI simply made unreachable after the first tap. The rating case is the clinically loaded one: a mis-tap on "Refused" permanently attached a 1-rating to the exposure — a therapist reading the CSV export or the avg-acceptance gauge can't distinguish that from a real refusal, whereas an absent rating correctly reads as "not recorded". The only workaround was abandoning the whole form (the post-submit `reset` from v0.5.7 was the sole path to a cleared optional field). Fix is the standard optional-chip toggle idiom, applied uniformly: `onPress={() => onChange(isSelected ? undefined : optionValue)}` on all six surfaces. `undefined` (not null or '') is the deliberate clear-value — it round-trips through react-hook-form's Controller as an unset field, passes the `.optional()` schemas verbatim, and lands as `null` in SQLite through the existing `?? null` mappers, identical to never having tapped. `RatingPicker`'s `onChange` prop type widens from `(rating: number) => void` to `(rating: number | undefined) => void` so the contract is visible at the type level; the log.tsx Controller callsite accepts it unchanged. Required dimensions (stage via StageIndicator, category on Add Food, food/child selection) are intentionally untouched — deselecting a required field would just manufacture a validation error; the toggle applies only where "unset" is a legal persisted state. +3 component tests in a new `deselect on re-tap (optional-field clearing)` describe block in `RatingPicker.test.tsx`: tapping the selected chip fires `onChange(undefined)` (the load-bearing case — previously fired the same number again), tapping an unselected chip while another is selected still fires the new value (regression lock that switching ratings stays a single tap — a contributor implementing toggle as "always clear first" would fail this loudly), and tapping with no selection set fires the value normally (pins the initial-tap happy path). The four log.tsx rows and the add.tsx prep row are screen-level Controller-rendered chips with no render harness — matches the v0.5.19/v0.5.39/v0.5.41 chip-row ship-without-test pattern; the RatingPicker component tests pin the shared idiom. Bumped `APP_VERSION` to v0.5.137. 557 tests pass across 27 suites (was 554, +3). TypeScript clean.
- v0.5.136 — Fix: block duplicate food names on the Add Food flow. `app/food/add.tsx`'s `onSubmit` previously inserted unconditionally — a parent adding "apple" when "Apple" already existed (or re-adding " Apple " with stray whitespace; `autoCapitalize="words"` makes casing inconsistent across entry points) created a second `foods` row with a distinct id. The damage is silent and compounding: every per-food stat keys on `foodId`, so the exposure-count-toward-acceptance metric (the app's core clinical number — 15/20/30 exposures per profile) splits across the duplicates and undercounts on both rows, the Foods tab lists the food twice, stage progression forks (bumping one row's stage leaves the twin behind), and the CSV export shows two part-histories a therapist can't distinguish from two different foods. Same intra-family-uniqueness defect class as v0.5.91 (duplicate user-email pre-check on Join Family) — the fix follows that precedent verbatim: pre-check + `Alert.alert` + short-circuit, scoped to the family (`eq(schema.foods.familyId, familyId)`), not global. Two coordinated changes: (1) new pure helper `findDuplicateFood(foods, name)` in `src/lib/food-partition.ts` — trims and lowercases both the candidate and each existing row's name before comparing, returns the first colliding row (so the caller can name it in the alert) or undefined; blank/whitespace-only candidates never match (the schema's `.min(1)` owns that rejection — the helper must not mask it by matching a hypothetical blank-named row), non-string candidate or row names are skipped per the established v0.5.118/v0.5.119/v0.5.135 typeof-guard idiom; (2) `add.tsx` `onSubmit` selects `{id, name}` for the family's foods before the insert and surfaces `Alert.alert('Already Added', '"<name>" is already in your food library. Open it from the Foods tab to log an exposure.')` — directing the parent at the actual next action (logging an exposure on the existing row) rather than a dead-end error. The check runs inside the existing try/finally so a select failure routes through the established catch → 'Failed to add food' path and `saving` always resets. A DB-level `CREATE UNIQUE INDEX ... ON foods(family_id, lower(name))` was considered and rejected: index creation fails if legacy duplicates already exist, and the resulting insert throw would surface as the generic 'Failed to add food' toast — the pre-check gives a precise, actionable message instead. +9 tests in a new `findDuplicateFood` describe block in `food-partition.test.ts`: exact match, case-insensitive match (the realistic double-add), whitespace-trim on both the candidate and existing rows, no-match undefined, empty/whitespace-only candidate undefined (the schema-owns-it lock), non-string candidate undefined (null/undefined/number drift defense), non-string row name skipped while the rest still match, first-match-wins when rows already collide, empty-list undefined. Bumped `APP_VERSION` to v0.5.136. 554 tests pass across 27 suites (was 545, +9). TypeScript clean.
- v0.5.135 — Fix: harden `filterFoods` (`src/lib/food-partition.ts:34`) against non-string `search` and `category`. The helper is typed `(foods, search: string, category: string)` so TypeScript blocks most callers, but the body's `search.trim().toLowerCase()` at the first line would throw `TypeError: Cannot read properties of null` / `is not a function` if a future surface delivered a non-string value through an `as any` cast or a relaxation of the upstream typing. Today the only callers are the form-state pair in `app/(tabs)/foods.tsx:68` (`searchQuery` from `useState('')` set by `onChangeText`, `selectedCategory` from `useState<'all' | FoodCategory>('all')` — both provably string) so production is safe. But the helper is the contract for any future surface that bypasses the screen: a deep-link query-param parser (`?search=null` would deliver the literal string "null" but a future contributor unpacking `useLocalSearchParams` might forward an undefined param verbatim), a future CSV-import wizard with a saved-filter feature, a future MMKV-persisted "last used filter" that drifts after a schema rename, a future JSON-RPC ingest of bookmarked filter URLs. The crash blast radius: the Foods tab is one of the four primary tabs and the only home for the food list — a single non-string filter input would unmount the entire Foods tab to a red box. Same defense-in-depth-at-the-boundary class as v0.5.118 (`buildExportFilename` non-string childName), v0.5.119 (`deriveLocalEmailPart` non-string displayName), v0.5.124 (`getNextStage`/`canBumpStage` typeof guard), v0.5.128 (`getHighestStage` row.stage typeof guard), v0.5.132 (`isValidInviteCode` non-string drift) — every public helper boundary is provably safe regardless of upstream gating. Three coordinated changes: (1) widened the parameter types from `string` to `string | null | undefined` on both `search` and `category` — the union admits the realistic drift surfaces (null from a corrupted rehydrated blob, undefined from a missing optional query param) without forcing `as any` casts at any future caller, matching the v0.5.124/v0.5.128/v0.5.132 widening shape verbatim; (2) added `const safeSearch = typeof search === 'string' ? search : ''` and `const safeCategory = typeof category === 'string' ? category : 'all'` at the top of the body that short-circuit before the `.trim()`/`.toLowerCase()`/`===` operations — so null/undefined/number/object/array all map to safe fallbacks (empty needle for search = no narrowing, 'all' category = no false-empty result), matching the v0.5.118 `safeName` and v0.5.119 `safeName` idiom verbatim; (3) the downstream `category === safeCategory` check switches to use `safeCategory` so a future contributor reordering the guards can't accidentally leak the original non-string `category` into the per-food comparison (would always be false → always-empty filter result, the worst-possible silent failure mode). The fallback shapes are deliberate: `''` for search collapses to "no narrowing" (the user sees the full list — fail-open, matches the existing whitespace-only contract from v0.5.9), and `'all'` for category matches the existing "all categories" sentinel (same fail-open semantic — no rows wrongly excluded). +7 tests in a new `non-string drift defense` describe block in `food-partition.test.ts`: null search returns full list (the load-bearing case — would previously throw); undefined search returns full list (same); numeric `42` search returns full list (regression lock against a future contributor "simplifying" the guard to `search ?? ''` which would let `(42).trim()` crash); null category returns full list (the false-empty defense); undefined category returns full list (same); numeric category returns full list; combined null search + 'fruit' category returns the fruit subset (pins that the two guards operate independently — a contributor folding them into one `if` block could break the cross-axis combinations). Bumped `APP_VERSION` to v0.5.135. 545 tests pass across 27 suites (was 538, +7). TypeScript clean.
- v0.5.134 — Refactor: route the dashboard's "today" start-of-day boundary through canonical `getStartOfDay` helper instead of an inline `new Date(); setHours(0, 0, 0, 0)` pair (`app/(tabs)/index.tsx:53`). The dashboard's "Today's Exposures" SQL filter at line 56-59 builds a `startOfDay` epoch for the `occurredAt >= startOfDay.getTime()` WHERE clause. Inline-vs-helper drift was a footgun: `getStartOfDay` got the v0.5.114 invalid-Date defense (`date instanceof Date && !Number.isNaN(date.getTime()) ? date : new Date()` fallback) plus a uniform "calendar-day" semantic that `formatRelativeDate` reads against for the "Today" label (per v0.5.49's calendar-day delta rewrite) — the inline version did neither, so the dashboard's "today" boundary could silently diverge from the rest of the app if a future contributor "improved" the inline version timezone-wise, or added DST handling to the helper, or hardened against Date drift on the helper side only. Same single-source-the-helper class as v0.5.43 (Progress tab profile-tag threshold routing through `getThresholdForProfile`), v0.5.50 (ExposureCard meta-row icon constants extracted), v0.5.104 (`getFeedingProfileConfig` helper consolidating bracket-derefs across `progress.tsx` and `settings.tsx`). Two coordinated lines: added `getStartOfDay` to the `@/src/lib/utils` import alongside the existing imports, and replaced the inline 2-line construction with a single `const startOfDay = getStartOfDay();` call. The fall-through behavior is semantically identical today — the helper's default `date = new Date()` parameter matches the inline `new Date()`, and the `.setHours(0, 0, 0, 0)` mutation matches the helper's internal `new Date(safeDate); d.setHours(0, 0, 0, 0)` pattern (which also clones first, so neither implementation mutates a caller-passed Date — they're semantically interchangeable). The downstream `.getTime()` call against the resulting Date is unchanged. No new tests — pure refactor against an already-100%-covered helper; the existing 24 `getStartOfDay` tests at `src/lib/__tests__/utils.test.ts` pin the contract that the dashboard now reads against. Bumped `APP_VERSION` to v0.5.134. 538 tests pass across 27 suites. TypeScript clean.
- v0.5.133 — Fix: harden `ProgressBar` (`src/components/ProgressBar.tsx:21`) against pathological numeric inputs. The component is mounted on the dashboard, food detail page, and every Progress-tab row (per-food, average-rating, stage-distribution) — every "X / Y" indicator in the app routes through it. The previous one-line `Math.min(current / target, 1)` was safe-by-accident: TypeScript types `current/target` as `number` so all current callers pass validated values (calcExposureProgress threshold from `getThresholdForProfile` returns 15/20/30, dashboard uses `Math.max(totalFoods, 1)`, rating gauge passes hardcoded `5`), but the bracket-divide admits four runtime drift surfaces: (a) target=0 → Infinity, then `Math.min(Infinity, 1) = 1` is fine but the accessibilityValue surfaces `max: 0` to a SR user, semantically broken; (b) NaN current/target → NaN, then `width: 'NaN%'` is invalid CSS and silently drops the bar's visual fill — the bar renders as an empty track with no error; (c) Infinity current → `Infinity / 15 = Infinity`, same NaN%/Infinity% drop; (d) negative current → `-5 / 15 = -0.33`, then `width: '-33%'` is invalid CSS and drops the fill. All four drift paths look identical to a user: the label renders (e.g. "0/15" or "5/0") but the visual bar is gone, indistinguishable from a 0% progress state. Same defense-in-depth-at-the-boundary class as v0.5.95 (`calcExposureProgress` Number.isFinite + Math.max(0) on `exposureCount`), v0.5.106 (rating accumulation Number.isFinite guard in `calcProgressStats`), and v0.5.122 (rating range gate to [1,5]) — every numeric input crossing a public surface boundary is coerced to a renderable shape. Two coordinated locals at the top of the function: `safeCurrent = typeof current === 'number' && Number.isFinite(current) ? Math.max(0, current) : 0` (drops NaN/Infinity/non-numeric to 0, clamps negatives to 0) and `safeTarget = typeof target === 'number' && Number.isFinite(target) && target > 0 ? target : 1` (drops 0/negative/NaN/Infinity/non-numeric to 1, the minimum-renderable denominator that produces a meaningful progress percentage). The visual `progress` computation and the `width` style now use the coerced values, so the bar's fill is always a finite renderable percentage. The accessibility `now`/`max` and the visible `{current}/{target}` label also switch to the coerced values — defensive consistency: a SR user announcing "5 of 0" is meaningless, and the visible label rendering "5/0" leaks the broken input through to a sighted user. The fallback shapes (safeTarget=1 for invalid target) match the established pattern: `safeCurrent=0` is the "no progress yet" semantic, `safeTarget=1` is the "minimum-renderable scale" semantic that produces a well-defined percentage. +6 component tests in a new `pathological numeric input defense` describe block: target=0 coerces to fallback (`5/1` rendered — pins the division-by-zero defense), NaN current renders `0/15` (pins the NaN coercion), Infinity current renders `0/15` (pins the Infinity coercion), negative current `-7` renders `0/15` (pins the negative clamp), negative target `-5` renders `3/1` (pins the negative-target fallback), NaN target renders `3/1` (pins the NaN-target fallback). The 6-case spread pins each drift surface on both the current and target axis so a future contributor "simplifying" one of the guards fails loudly on the matching test. Bumped `APP_VERSION` to v0.5.133. 538 tests pass across 27 suites (was 532, +6). TypeScript clean.
- v0.5.132 — Fix: harden `isValidInviteCode` (`src/lib/utils.ts:18`) against non-string runtime drift. The function previously declared the parameter as `code: string` so TypeScript blocked most direct callers, but the body's `code.length !== INVITE_CODE_LENGTH` check would throw `TypeError: Cannot read properties of null` / `Cannot read properties of undefined` if a future surface delivered a non-string value through an `as any` cast or a relaxation of the upstream typing. Two known paths today: (a) `joinFamilySchema.inviteCode.refine(isValidInviteCode, ...)` (`src/lib/validation.ts:105`) where the upstream `.string().trim().toUpperCase()` chain provably narrows to string before the refine runs — production-safe; (b) `app/onboarding/join.tsx:28` where the screen handler does `inviteCode.trim().toUpperCase()` on the form-typed `inviteCode: string` state — also safe. But the helper boundary should be provably safe regardless of upstream gating: future drift surfaces include a Convex sync layer ingesting cross-device invite codes, a CSV import path, a deep-link query param parser, or a JSON-RPC ingest where the value crosses the type boundary as `unknown`. Same defense-in-depth-at-the-boundary class as v0.5.124 (`getNextStage`/`canBumpStage` typeof guard widening the param to `ExposureStage | string | null | undefined`) and v0.5.128 (`getHighestStage` row.stage typeof guard) — every public helper boundary is provably safe regardless of upstream gating. Two coordinated changes: (1) widened the parameter type from `string` to `string | null | undefined` — the union admits the realistic drift surfaces (null from a corrupted SQLite row, undefined from a missing optional query param) without forcing an `as any` cast at any future caller; (2) added explicit `typeof code !== 'string'` guard at the top of the body that short-circuits before the `.length` access — so null/undefined/number/object/array all return false cleanly, matching the v0.5.124/v0.5.128 typeof-guard idiom verbatim. The existing length-and-charset checks downstream are unchanged. +4 tests in a new `non-string drift defense` describe block in `utils.test.ts`: null returns false (the load-bearing case — would previously throw), undefined returns false (same), number `123456` returns false (regression lock against a future contributor "simplifying" the guard to `code == null`), object `{}` returns false (regression lock against a contributor narrowing to `typeof code === 'object'` check). The 4-case spread pins the typeof-string contract against every realistic drift surface. Bumped `APP_VERSION` to v0.5.132. 532 tests pass across 27 suites (was 528, +4). TypeScript clean.
- v0.5.131 — Fix: add `CHECK(length(name) BETWEEN 1 AND 50)` to the `children` table CREATE statement (`src/providers/DatabaseProvider.tsx:35`). The column was declared `name TEXT NOT NULL` with no length constraint — `childSchema.name` validates `.trim().min(1).max(50)` at insert (`src/lib/validation.ts:15`) but SQLite has no DB-level length guard, so a corrupt insert path (future Convex sync delivering a 1MB child name from another device, hand-edited dev DB, CSV import bypassing the schema, future JSON-RPC ingest) would persist a row that balloons the SQLite database and surfaces as a render-blocking long string anywhere the column is read back (ChildSelector chip row at `src/components/ChildSelector.tsx:36`, the Settings tab's child management section, the dashboard's `<ExposureCard childName={...} />`, the CSV export's `buildExportFilename` slug computation — already capped at 50 via v0.5.113 but a 10MB pre-slug input still flows through the `.toLowerCase().replace().slice()` chain consuming time and memory). Same defense-in-depth class as v0.5.130 (foods.name length CHECK just shipped), v0.5.126 (families.invite_code length CHECK), and v0.5.127 (created_at > 0 CHECK across all 6 tables) — closing per-column drift surfaces uniformly across the data layer at the DB boundary. The `BETWEEN 1 AND 50` form matches the Zod schema's `.min(1).max(50)` contract exactly. Forward-only protection (CHECK only applies on CREATE TABLE IF NOT EXISTS — fresh installs and every v1.0 base of users get the protection; existing installs keep the old schema, matching the v0.5.123 / v0.5.125 / v0.5.126 / v0.5.127 / v0.5.130 precedent). No new tests — DatabaseProvider is React component code with no SQLite harness in the test suite; matches the established ship-without-test pattern. Bumped `APP_VERSION` to v0.5.131. 528 tests pass across 27 suites. TypeScript clean.
- v0.5.130 — Fix: add `CHECK(length(name) BETWEEN 1 AND 80)` to the `foods` table CREATE statement (`src/providers/DatabaseProvider.tsx:46`). The column was declared `name TEXT NOT NULL` with no length constraint — `foodSchema.name` validates `.trim().min(1).max(80)` at insert (`src/lib/validation.ts:63`) but SQLite has no DB-level length guard, so a corrupt insert path (future Convex sync delivering a 1MB food name from another tracking app, hand-edited dev DB, CSV import bypassing the schema, future JSON-RPC ingest) would persist a row that balloons the SQLite database and surfaces as a render-blocking long string anywhere the column is read back (Foods tab list, food detail page header, dashboard recent-exposures `<ExposureCard foodName={...} />`). Same defense-in-depth class as v0.5.126 (families.invite_code length CHECK) and v0.5.127 (created_at > 0 CHECK across all 6 tables) — closing per-column drift surfaces uniformly across the data layer at the DB boundary. The `BETWEEN 1 AND 80` form mirrors the v0.5.122 exposures.rating CHECK shape and matches the Zod schema's `.min(1).max(80)` contract exactly. Forward-only protection (CHECK only applies on CREATE TABLE IF NOT EXISTS — fresh installs and every v1.0 base of users get the protection; existing installs keep the old schema, matching the v0.5.123 / v0.5.125 / v0.5.126 / v0.5.127 precedent). No new tests — DatabaseProvider is React component code with no SQLite harness in the test suite; matches the v0.5.123 / v0.5.125 / v0.5.126 / v0.5.127 ship-without-test pattern. Bumped `APP_VERSION` to v0.5.130. 528 tests pass across 27 suites. TypeScript clean.
- v0.5.129 — Fix: cap `exposureSchema.childId` and `exposureSchema.foodId` at 64 characters via `.max(64, ...)` chained between `.min(1, ...)` and the existing string-typed contract (`src/lib/validation.ts:72-73`). Both fields previously declared bare `z.string().min(1)` with no upper bound — TypeScript's `z.infer` typed them as `string`, and the form path (`app/(tabs)/log.tsx`) always wired them from the in-app children/foods lists where IDs are 36-char UUIDv4 strings from `generateId()` (uuid v4 = 32 hex digits + 4 hyphens), so the production hot path was provably safe. But the schema is the contract for any future surface that bypasses the in-app selection — a future Convex sync layer ingesting cross-device rows, a CSV import path delivering legacy IDs from another tracking app, a JSON-RPC ingest, or a future "scan QR code to log exposure" path delivering an attacker-controlled string — and a missing upper bound on a free-form string field is a classic memory/storage drift surface (a 10MB foodId would persist verbatim into the exposures row, balloon the SQLite database, and surface as a render-blocking long string anywhere the column is read back). Same defense-in-depth-at-the-validation-boundary class as v0.5.111 (RFC-5321 64-char cap on derived email local-parts) and v0.5.113 (50-char cap on the export filename slug) — every free-form string field that lands in SQLite gets an upper bound at the schema, sized conservatively above the realistic emit-set (UUIDv4 = 36 chars, ULID = 26, prefixed-UUID = up to ~50; 64 leaves headroom for every reasonable ID format the codebase might one day generate or ingest, without being arbitrarily wide). The pattern matches the v0.5.111 RFC-5321 cap rationale verbatim — pick the conservative-but-future-proof upper bound that closes the unbounded-input surface without false-rejecting realistic values. +4 tests in the existing `exposureSchema` describe block in `validation.test.ts`: 64-char childId accepts (boundary lock — a contributor tightening the cap below 64 fails loudly), 65-char childId rejects (the cap-fires lock), 64-char foodId accepts, 65-char foodId rejects. The 4-case spread pins both boundaries on both fields so a future refactor splitting the schema into a separate exposure-form schema can't accidentally drop one of the caps. The other free-form string fields on `exposureSchema` (`preparation` 50, `notes` 500) already have caps from v0.5.83; the `id` fields were the last unbounded surfaces on the schema. Bumped `APP_VERSION` to v0.5.129. 528 tests pass across 27 suites (was 524, +4). TypeScript clean.
- v0.5.128 — Fix: harden `getHighestStage` (`src/lib/stage.ts`) against non-string stage values, runtime drift, and unknown casing — matches the v0.5.124 defensive-shield pattern applied to its siblings `getNextStage` / `canBumpStage`. The previous implementation was safe-by-accident: `STAGE_ORDER.indexOf(stage as ExposureStage) < 0` correctly returned -1 for an unknown stage like `'mystery'` and for non-string runtime values (null/undefined coerce to -1 through indexOf), so the `continue` branch fired correctly — but the parameter was typed `readonly { stage: string }[]` and the body carried an `as ExposureStage` cast, so any caller passing a runtime-drifted value (corrupted `exposures.stage` SQLite row with null, future Convex sync delivering a renamed stage, hand-edited dev DB, CSV import path) would need an `as any` cast to compile, masking the contract violation. Same defense-in-depth-at-the-boundary class as v0.5.124 (`getNextStage`/`canBumpStage`), v0.5.104 (`getFeedingProfileConfig`), v0.5.105 (`getStageConfig`), v0.5.118 (`buildExportFilename` non-string childName), v0.5.119 (`deriveLocalEmailPart` non-string displayName) — every public helper boundary is provably safe regardless of upstream gating. Four coordinated changes: (1) widened the row type from `{ stage: string }` to `{ stage: ExposureStage | string | null | undefined }` — the union admits runtime drift surfaces without requiring `as any` casts at any future caller, and matches the v0.5.124 widening verbatim; (2) added explicit `typeof stage !== 'string'` guard that short-circuits before the includes call so a non-string (number, object, null, undefined) doesn't pay the array scan; (3) added explicit `!(STAGE_ORDER as readonly string[]).includes(stage)` membership check that returns null for unknown stage strings — reads as a defensive "is this a valid stage?" gate matching the v0.5.124 / v0.5.104 / v0.5.105 pattern verbatim (easier for a future contributor to recognize as a defensive shield, not as accidental safety); (4) cached the highest index in a `highestIdx` local so the inner loop runs at most one `indexOf` per row instead of two — micro-optimization but the chief reason is reading clarity: the previous code did `STAGE_ORDER.indexOf(stage) > STAGE_ORDER.indexOf(highest)` which re-derived the highest's index on every row even though it never changed unless `highest` updated. The hot-path callers — `progress-stats.ts` (Progress tab summary computation, runs on every Progress-tab render), `app/food/[id].tsx` (food detail page's "Bump to next stage" gating, runs on every food-detail mount), `food-partition.ts` (Foods-tab per-food bucketing, runs on every Foods-tab render) — all carry strictly-typed `stage: string` projections today so production is safe regardless, but defense-in-depth at the helper boundary means every present and future caller is provably safe. +4 tests in the existing `getHighestStage` describe block in `stage.test.ts`: skips rows where stage is null/undefined and still resolves the rest (load-bearing — pins the v0.5.124-style typeof guard works on the row half, not just the scalar half v0.5.124 covered); returns null when every row has a non-string stage (no-survivors path); skips rows with non-string stage values (number/object/array) and still resolves the rest (regression lock against a future contributor "simplifying" the typeof guard to just truthy check, which would let `[]` flow through and produce undefined behavior); does not pick a casing-drifted `TOLERATE` (mixed-case lock — matches the v0.5.124 'TOLERATE' regression lock verbatim, defends the read path against a future CSV-import path delivering uppercase stage strings). Bumped `APP_VERSION` to v0.5.128. 524 tests pass across 27 suites (was 520, +4). TypeScript clean.
- v0.5.127 — Fix: add `CHECK(created_at > 0)` to the `created_at` column on every CREATE TABLE statement in `src/providers/DatabaseProvider.tsx` — families, users, children, foods, exposures, food_chains. Every table declared `created_at INTEGER NOT NULL` with no value-range constraint — drizzle's TypeScript `integer('created_at', { mode: 'timestamp' }).notNull()` declaration at `src/db/schema.ts` types the column as Date at the row level, but SQLite has no native Date type (the value is stored as an INTEGER millisecond epoch) and no DB-level positivity guard. Same drift class as v0.5.125 (which closed the equivalent gap on `exposures.occurred_at`) — but `created_at` is on every one of the 6 tables, so the protection has to land 6 times in one symmetric pass. Three drift paths the schema was admitting: (1) a corrupted insert path writing `created_at: 0` (the Unix epoch — same drift class v0.5.120 defended on the export side via `toIsoDate`'s null guard), (2) a future Convex sync layer delivering a negative timestamp (pre-1970 — implausible for a real row but the schema permits it), (3) a hand-edited dev DB or CSV import path writing zero through any future ingest surface. Same defense-in-depth class as v0.5.123 (foods.is_safe_food range), v0.5.125 (exposures.occurred_at positivity), v0.5.126 (families.invite_code length) — closing per-column drift surfaces uniformly across the data layer at the DB boundary. The downstream failure modes if a zero `created_at` row slipped in: every query that sorts by `created_at desc` (none today, but the column is the canonical "when was this row written" timestamp that any future audit/admin path would key off) would surface the epoch-zero row at the bottom; CSV exports of audit logs (future Convex sync rollback ledger) would emit `1970-01-01T00:00:00.000Z` as the create date — clinically nonsensical and indistinguishable from a real timestamp on a naive scan. Forward-only protection — CHECK only applies on CREATE TABLE IF NOT EXISTS, so existing installs keep the old schema and every fresh install gets the guard, matching the v0.5.123 / v0.5.125 / v0.5.126 precedent. No new tests — DatabaseProvider is React component code with no SQLite harness in the test suite; matches the v0.5.125 / v0.5.126 / v0.5.123 / v0.5.93 ship-without-test pattern. Bumped `APP_VERSION` to v0.5.127. 520 tests pass across 27 suites. TypeScript clean.
- v0.5.126 — Fix: add `CHECK(length(invite_code) = 6)` to the `families` table CREATE statement (`src/providers/DatabaseProvider.tsx:17`). The column was declared `invite_code TEXT NOT NULL` with no length constraint — the application-level contract pinned it at exactly 6 chars from the v0.5.88 `INVITE_CODE_LENGTH = 6` constant + the `generateInviteCode` helper that emits 6-char strings, but no DB-level guard backed the contract. Three drift paths the schema was admitting: (1) a future Convex sync layer delivering a renamed-format code (5-char codes from a different family-app product the user migrates from, 8-char codes from a "more secure" generator refresh), (2) a hand-edited dev DB or CSV import path writing a malformed code through any future ingest surface, (3) a partial-write race during a Convex schema migration that briefly persisted a truncated code. The downstream failure modes if a wrong-length code slipped in: the Join Family flow at `app/onboarding/join.tsx` already validates the user-typed code via `isValidInviteCode` (added v0.5.88, requires length 6 + the charset) so a 5-char persisted code could never be matched by a real join attempt — the code would be silently un-joinable, ghosting any secondary parent who tried to use it. The `joinFamilySchema.inviteCode` (v0.5.117) closes the user-typed half; this commit closes the persistence half. Same defense-in-depth class as v0.5.125 (exposures.occurred_at positivity) and v0.5.123 (foods.is_safe_food range) — closing per-column drift surfaces uniformly across the data layer at the DB boundary. Forward-only protection (CHECK only applies on CREATE TABLE IF NOT EXISTS — fresh installs and every v1.0 base of users get the protection; existing installs keep the old schema, matching the v0.5.123 / v0.5.125 precedent). The character-class half of the invite-code contract (uppercase A-Z minus I/O + 2-9, per the v0.5.88 INVITE_CODE_CHARSET) is intentionally NOT enforced at the SQLite layer — SQLite's CHECK supports `GLOB` and `LIKE` but the v0.5.88 charset is too narrow for a clean GLOB pattern, and forcing it would diverge from the established "schema declares shape, application code declares semantics" split (v0.5.88's `isValidInviteCode` at the application boundary already covers charset). Length is the load-bearing structural invariant — the part that matters for "is this row recoverable via the Join flow?" — and that's what this commit pins. No new tests — DatabaseProvider is React component code with no SQLite harness in the test suite; matches the v0.5.125 / v0.5.123 / v0.5.93 ship-without-test pattern. Bumped `APP_VERSION` to v0.5.126. 520 tests pass across 27 suites. TypeScript clean.
- v0.5.125 — Fix: add `CHECK(occurred_at > 0)` to the `exposures` table CREATE statement (`src/providers/DatabaseProvider.tsx:68`). The column was declared `occurred_at INTEGER NOT NULL` with no value-range constraint — drizzle's TypeScript `integer('occurred_at', { mode: 'timestamp' }).notNull()` declaration at `src/db/schema.ts` types the column as Date at the row level, but SQLite has no native Date type (the value is stored as an INTEGER millisecond epoch) and no DB-level positivity guard. Three drift paths the schema was admitting before this commit: (1) a corrupted insert path writing `occurred_at: 0` (the Unix epoch — same drift class v0.5.120 defended on the export side via `toIsoDate`'s null guard, which closed the visible-output half via `new Date(null).getTime() === 0`), (2) a future Convex sync layer delivering a negative timestamp (pre-1970 — implausible for a real exposure but the schema permits it), (3) a hand-edited dev DB or CSV import path writing zero through any future ingest surface. Same defense-in-depth class as v0.5.123 (foods.is_safe_food range check) — closing per-column drift surfaces uniformly across the data layer at the DB boundary. The downstream failure modes if a zero `occurred_at` row slipped in: the dashboard's recently-logged list at `app/(tabs)/index.tsx` (sorted by `occurred_at desc`) would surface the epoch-zero row at the bottom of the list with `formatRelativeDate` reading "Today" via the v0.5.49 calendar-day delta (epoch-zero is January 1, 1970, which the same-time-of-day round vs. now produces a very large negative number — but the v0.5.49 `days <= 0 → 'Today'` widening catches it and renders the row as logged today, masking the drift entirely until a therapist scans the CSV export and sees the 1970 date). The Progress tab's `weeklyExposures` count at `progress.tsx` filters on `occurredAt > weekAgo` so a zero timestamp would be excluded from the weekly count (silently undercounting). Defense-in-depth at the DB boundary: forward-only protection (CHECK CONSTRAINT only applies on CREATE TABLE IF NOT EXISTS, so existing installs keep the old schema — matches the v0.5.123 / v0.5.0 forward-only precedent — fresh installs and every v1.0 base of users get the protection). No new tests — DatabaseProvider is React component code running a `useEffect` against a real expo-sqlite instance; the test suite has no SQLite harness (every test uses in-memory fixtures or mocks drizzle directly), so the change is unverifiable in tests. Matches the established v0.5.123 / v0.5.93 / v0.5.81 / v0.5.76 ship-without-test pattern for screen/DB-level changes that need a render or SQLite harness. The drizzle schema at `src/db/schema.ts` is intentionally not edited — drizzle's `check()` helper exists but the codebase doesn't use it for any other column, and adding it here alone would diverge from the established "schema declares shape, DatabaseProvider declares constraints" split established v0.5.123. Bumped `APP_VERSION` to v0.5.125. 520 tests pass across 27 suites. TypeScript clean.
- v0.5.124 — Fix: harden `getNextStage` and `canBumpStage` (`src/lib/stage.ts`) against unknown stage strings, non-string drift, and undefined inputs. The previous implementation was safe-by-accident: the `STAGE_ORDER.indexOf(current) < 0` check correctly returned null for an unknown stage string like `'mystery'` because indexOf returns -1, but the parameter was typed `ExposureStage | null` so any caller passing a runtime-drifted value (corrupted `exposures.stage` SQLite row, future Convex sync delivering a renamed stage like `'observe'`, hand-edited dev DB, CSV import) would need an `as any` cast to compile — masking the contract violation. The two call sites at `app/food/[id].tsx:81,204,206,217` pass `highestStage: ExposureStage | null` from `getHighestStage` (which already gates unknown stages per the v0.5.4 contract) so the production hot path was safe, but defense-in-depth at the helper boundary means every present and future caller is provably safe regardless of upstream gating. Same defense-in-depth-at-the-boundary class as v0.5.104 (`getFeedingProfileConfig`), v0.5.105 (`getStageConfig`), v0.5.118 (`buildExportFilename` non-string childName), v0.5.119 (`deriveLocalEmailPart` non-string displayName) — every public helper boundary is provably safe regardless of upstream gating. Three coordinated changes: (1) widened the parameter type from `ExposureStage | null` to `ExposureStage | string | null | undefined` on both functions — the union admits runtime drift surfaces without requiring `as any` casts at any future caller; (2) added explicit `typeof current !== 'string'` guard that short-circuits before the includes call so a non-string (number, object) doesn't pay the array scan; (3) added explicit `!(STAGE_ORDER as readonly string[]).includes(current)` membership check that returns null for unknown stage strings (the previous `indexOf < 0` check was equivalent but reading as a defensive "is this a valid stage?" gate matches the v0.5.104/v0.5.105 pattern verbatim — easier for a future contributor to recognize as a defensive shield, not as accidental safety). The undefined branch maps to `STAGE_ORDER[0]` (tolerate — the canonical "start of the hierarchy" fallback that matches the null branch behavior, semantically: "no current stage means we're at the very beginning, the next step is the first stage"). +6 tests in the existing stage.test.ts describe blocks: getNextStage with undefined returns tolerate; getNextStage with unknown strings ('mystery', 'TOLERATE' mixed-case, '' empty) returns null; getNextStage with non-string inputs (42, {}, []) returns null; canBumpStage with undefined returns true; canBumpStage with unknown strings returns false; canBumpStage with non-string inputs returns false. The mixed-case 'TOLERATE' case is the load-bearing regression lock — a future contributor "simplifying" the includes check to a case-insensitive comparison would silently let in casing drift from a future CSV ingest path and fail this test loudly. Bumped `APP_VERSION` to v0.5.124. 520 tests pass across 27 suites (was 514, +6). TypeScript clean.
- v0.5.123 — Fix: add `CHECK(is_safe_food IN (0, 1))` to the `foods` table CREATE statement (`src/providers/DatabaseProvider.tsx:50`). The column was declared `is_safe_food INTEGER NOT NULL DEFAULT 0` with no range constraint — the only protection was the DEFAULT 0 on a missing field, but nothing blocked an explicit `INSERT INTO foods (..., is_safe_food, ...) VALUES (..., 7, ...)` or `UPDATE foods SET is_safe_food = -1 WHERE id = ?`. Every other enum-shaped column on the foods/exposures tables already carries a matching CHECK at the same boundary: foods.category (line 47 — `CHECK(category IN ('protein','vegetable','fruit','grain','dairy','other'))`), exposures.stage, exposures.temperature, exposures.texture, exposures.meal_type, exposures.setting — `is_safe_food` was the lone enum-shaped column missing the protection pattern. The failure mode: drizzle declares the column as `integer('is_safe_food', { mode: 'boolean' })` (`src/db/schema.ts:39`) so the typescript-side reads/writes are gated to `boolean`, but drizzle's boolean mode coerces non-0 integers to truthy on read (any non-zero value reads as `true`) while filter predicates like `eq(foods.isSafeFood, true)` compile to `is_safe_food = 1` — so a corrupt row with `is_safe_food = 2` (future Convex sync drift writing a 3-state value, hand-edited dev DB, CSV import via a future ingest path) would render as a safe food in the v0.3.0 pinned "Safe Foods" row at `app/(tabs)/foods.tsx:89-97` but vanish from any UI that filters with `.where(eq(foods.isSafeFood, true))` — read-vs-filter drift, the worst kind of silent data inconsistency. Same defense-in-depth class as v0.5.108 (child-store rehydration validation), v0.5.109 (auth-store cross-field consistency), v0.5.117 (joinFamilySchema inviteCode normalization), v0.5.122 (rating range gate just shipped) — closing per-column drift surfaces uniformly across the data layer. Limitation noted: `CREATE TABLE IF NOT EXISTS` means the CHECK only applies on fresh installs (the table is created once on first launch; subsequent launches skip the CREATE entirely). Existing installs keep the old schema. This is forward-only protection — matches the precedent set by the v0.5.0 category CHECK and every other CHECK on the exposures table, which all share the same forward-only contract. The v1.0 base of users / every future fresh install gets the protection. No new tests — the DatabaseProvider is React component code that runs a `useEffect` against a real expo-sqlite instance; the test suite has no SQLite harness (every test in the suite uses in-memory fixtures or mocks drizzle directly), so the change is unverifiable in tests. Matches the established v0.5.93 / v0.5.81 / v0.5.76 ship-without-test pattern for screen/DB-level changes that need a render or SQLite harness. The drizzle schema at `src/db/schema.ts:39` is intentionally not edited — drizzle's `check()` helper exists in the API but the codebase doesn't use it for any other column, and adding it here alone would diverge from the established "schema declares shape, DatabaseProvider declares constraints" split. Bumped `APP_VERSION` to v0.5.123. 514 tests pass across 27 suites. TypeScript clean.
- v0.5.122 — Fix: range-gate rating accumulation in `calcProgressStats` (`src/lib/progress-stats.ts:67`) to the v0.5.82 schema contract ([1, 5]). v0.5.106 closed the NaN/Infinity half via `Number.isFinite(exp.rating)` — but that guard still admitted finite out-of-range values: rating=0 dragged avgRating toward zero ((3+0)/2=1.5 instead of dropping the row), and rating=7 / rating=100 pushed avgRating above the 5-cap, making the Progress-tab "Avg Acceptance" gauge at `progress.tsx:111` render past full (the gauge maxes visually at 5/5 but the underlying value flows verbatim into any future "5-star rating display" surface that doesn't clamp). The failure mode: zod's `.int().min(1).max(5)` (v0.5.82) gates form input correctly, but the exposures.rating SQLite column has no range CHECK on legacy rows (drizzle's `integer('rating')` declaration in `src/db/schema.ts` doesn't carry a check), so a corrupt drift (future Convex sync delivering an out-of-range value, hand-edited dev DB, CSV import, a future fixture-data path) would persist and skew the gauge. Same defense-in-depth class as v0.5.106 (NaN/Infinity), v0.5.95 (`calcExposureProgress` NaN coercion), and v0.5.101 (profile fallback) — defending the in-app analytics path against ways rating can drift from the schema contract at runtime. Surgical fix: replaced `Number.isFinite(exp.rating)` with `typeof rating === 'number' && rating >= 1 && rating <= 5`. The range check naturally subsumes the NaN/Infinity guard (`NaN >= 1` is false, `Infinity <= 5` is false, `-Infinity >= 1` is false), so the contract is now uniform with the schema and the `as number` cast disappears (typeof narrows). Decimal ratings like 3.7 pass through verbatim — the schema requires `.int()` but the avgRating gauge already shows fractional values (3.5, 4.2) from integer inputs, so decimal-vs-integer is a secondary concern; the load-bearing failure mode is range, not granularity. +2 tests in the existing `calcProgressStats` describe block: out-of-range mix (4, 0, -1, 6, 100, 2) returns avgRating=3 (only the 4+2 contribute — pins that 0/-1 don't drag toward zero and 6/100 don't inflate past 5); inclusive boundary (1, 5) returns avgRating=3 (regression lock — a future contributor narrowing the gate to `> 1 && < 5` or `>= 2 && <= 4` would silently drop the most clinically-meaningful endpoints, Refused and Enjoyed, from every average — and fail this test loudly). The existing v0.5.106 NaN/Infinity tests pass unchanged since the range check naturally rejects them. Bumped `APP_VERSION` to v0.5.122. 514 tests pass across 27 suites (was 512, +2). TypeScript clean.
- v0.5.121 — Fix: defensive non-finite number guard on `csvEscape` (`src/lib/export.ts:43`). The function previously stringified any number-typed value via `String(value)` after passing the null/undefined early-return — but `String(NaN)` emits the literal text `"NaN"` and `String(Infinity)` / `String(-Infinity)` emit `"Infinity"` / `"-Infinity"` directly into the therapist-facing CSV cell. The failure mode: a corrupt SQLite row carrying `exposures.rating: NaN` (or any future numeric column the export grows — preparation/texture/temperature today are strings, but the projection is one new column away from a numeric drift surface; v0.5.106 closed the parallel rating-accumulation gap on `calcProgressStats` where the same NaN/Infinity values poisoned the avg-acceptance gauge) would emit `NaN` as the rating cell value. Excel/Sheets/Numbers render the literal `NaN` as an opaque string that looks identical to a legitimate sample on a glance scan (it's a 3-character ALL-CAPS token next to a column of integers, easy to skim past), but breaks every downstream formula: `AVERAGE(F2:F100)` returns `#VALUE!` when any cell is non-numeric, `SORT(by F)` puts the bad rows in an unpredictable position relative to the numeric neighbors (Excel sorts text after numbers by default; Sheets is reversed; Numbers is locale-dependent), and `=COUNTIF(F:F, ">=4")` silently drops the bad row from the willing/enjoyed cohort. A feeding therapist scanning the rating column for "rows where the child was Willing or Enjoyed (4-5)" would see the corrupt row contribute nothing to their count — a silent loss of clinical signal, indistinguishable on the surface from a "rating not recorded" gap. Same drift class as v0.5.106 (`Number.isFinite` rating accumulation guard on the in-app progress side) and v0.5.120 (`toIsoDate` null guard on the same export pipeline) — defense-in-depth against ways a number-typed value can drift from a real measurement at runtime. One-line fix: `if (typeof value === 'number' && !Number.isFinite(value)) return '';` between the null/undefined guard and the `String(value)` coercion. Falls back to empty string (the same shape as null/undefined), so the therapist sees a blank rating cell rather than the misleading `NaN` literal — and `AVERAGE`/`COUNTIF` correctly treat the gap as "no rating recorded" rather than "a strange string". The typeof gate short-circuits before `Number.isFinite` runs against non-numeric values, so a string like `'NaN'` (a legitimate parent-typed note value) still flows through verbatim and gets quote-escaped by the existing `[",\n\r]` branch — only number-typed NaN/±Infinity hit the new branch. +4 tests in a new `non-finite number guard` describe block in `export.test.ts`: NaN returns '' (the load-bearing case — pins the bug); +Infinity returns ''; -Infinity returns ''; finite happy-path loop (0, -1, 3.14, 42) stringifies normally (regression lock against a future contributor "simplifying" the guard to `typeof value === 'number'` would drop the `Number.isFinite` and break legitimate numeric coercion). Bumped `APP_VERSION` to v0.5.121. 512 tests pass across 27 suites (was 508, +4). TypeScript clean.
- v0.5.120 — Fix: defensive null/undefined early-return on `toIsoDate` (`src/lib/export.ts:55`) to prevent epoch coercion. The existing guard `if (Number.isNaN(d.getTime())) return ''` (v0.5.96 — covers `new Date('not-a-date')`, `new Date(undefined)`, `new Date(NaN)`) had a silent gap: `new Date(null)` does NOT produce an invalid Date — JS coerces `null` to `0` via `Number(null) === 0` and returns `new Date(0)`, which is the Unix epoch `1970-01-01T00:00:00.000Z` with a valid `.getTime()` of `0` (NOT `NaN`). The existing NaN guard lets it through. The failure mode: a corrupted `exposures.occurredAt` SQLite row (no DB-level NOT NULL guarantee on legacy rows; future Convex sync delivering null; hand-edited dev DB; a partial-write that briefly reads null between the row write and a re-read) flows through `formatExposuresCsv` and emits `1970-01-01T00:00:00.000Z` as the date column. A feeding therapist opening the exported CSV in Excel/Sheets/Numbers would see exposures dated "before computers existed" — clinically nonsensical, indistinguishable from a real exposure to a naive scan, and worst-case: a therapist sorting the CSV by date column would silently bury the bad row at the top, hiding it. Same drift class as v0.5.96 (the original CSV `occurredAt` guard that covered the NaN side), v0.5.98 (`formatRelativeDate` invalid-Date), v0.5.102 (`formatDate` invalid-Date), v0.5.107 (`buildExportFilename` `at` invalid-Date), v0.5.114 (`getStartOfDay` invalid-Date) — defense-in-depth against ways a Date-typed value can drift from a real timestamp at runtime. The other null-shape cases (`undefined`) already pass through the NaN guard correctly (`new Date(undefined).getTime() === NaN`) but caught here too for symmetry and to lock the early-return contract. One-line fix: `if (value === null || value === undefined) return '';` at the top of the function, before the Date constructor runs. The early-return short-circuits the entire pipeline so a contributor "simplifying" the NaN guard to a different check (e.g. `d.getTime() > 0`) couldn't accidentally let null slip through again. +2 tests in the existing `formatExposuresCsv occurredAt corruption defense` describe block: null returns '' AND asserts `cols[0] !== '1970-01-01T00:00:00.000Z'` (the regression lock that pins the bug — a contributor dropping the null check would emit the epoch and fail the negative assertion loudly); undefined returns '' (symmetry lock). Bumped `APP_VERSION` to v0.5.120. 508 tests pass across 27 suites (was 506, +2). TypeScript clean.
- v0.5.119 — Fix: guard `deriveLocalEmailPart` (`src/lib/utils.ts:57`) against non-string `displayName`. Same shape as v0.5.118 (`buildExportFilename`) — the function is typed `(displayName: string) => string` so TypeScript blocks the common callers, but the helper is exported as a public utility and the very first line `displayName.toLowerCase()` would throw `TypeError: Cannot read properties of null` / `is not a function` on any runtime drift (a corrupted users.displayName row, a future Convex sync delivering null/number/object, a future loose `as any` cast on a hot-path call, a future CSV/JSON-RPC ingest path). The crash blast radius: every Join-Family flow calls `deriveLocalEmailPart(trimmedName)` at `app/onboarding/join.tsx:40` to synthesize the offline-only email, so a single corrupted typed value would brick the secondary parent's onboarding entry point. The screen-level handler at `app/onboarding/join.tsx:20` calls `.trim()` first which already throws on non-string before reaching the helper, so the production hot path is safe today — but defense-in-depth at the helper boundary means every present and future caller (a CSV import, a Convex sync layer, a relaxation of the upstream string-typed contract) is provably safe regardless of upstream gating. Same defense-in-depth-at-the-boundary class as v0.5.78 (sanitize the character class), v0.5.111 (RFC-5321 64-char cap), v0.5.114 (`getStartOfDay` invalid-Date), v0.5.118 (`buildExportFilename` non-string childName) — every public helper boundary is provably safe regardless of upstream gating. One-line fix at the top of the function: `const safeName = typeof displayName === 'string' ? displayName : '';` then use `safeName` for the sanitize pipeline. The empty-string flows through the existing `sanitized.length > 0 ? sanitized : 'user'` fallback at line 65 (v0.5.78 contract), so a non-string input produces `user@tonguetutor.app` — the same email as a displayName with all-stripped characters (e.g. `'👶'` or `'!!!'`). +4 tests in the existing `deriveLocalEmailPart` describe block: null/undefined cast `as unknown as string` fall back, numeric `42` falls back, empty-object `{}` falls back — each asserts the exact pinned fallback `'user'`. The 4-case spread pins the typeof-string contract against a contributor "simplifying" the guard (e.g. dropping it to `displayName ?? ''` would let `(42).toLowerCase()` crash and fail the number case loudly). Bumped `APP_VERSION` to v0.5.119. 506 tests pass across 27 suites (was 502, +4). TypeScript clean.
- v0.5.118 — Fix: guard `buildExportFilename` against non-string `childName`. The function is typed `(childName: string, ...) => string` so the type system blocks most callers — but a runtime drift (corrupted `children.name` row, future Convex sync delivering null/number/object, hand-edited dev DB, a future loose `as any` cast on the call path) would slip an unexpected value through, and the very first line `childName.toLowerCase()` would throw `TypeError: Cannot read properties of null` / `is not a function`. The crash blast radius: the Settings → Export Data button at `app/(tabs)/settings.tsx` calls `exportChildData(selectedChild.id, selectedChild.name)`, so a single corrupted child row would brick the entire offline CSV-share flow — the only documented path for therapists to receive a parent's logged exposures (per NEXT_STEPS.md P5). `childSchema.name` validates at the insert boundary but SQLite has no DB-level NOT NULL string-type guarantee, so a row that drifts after insert (or arrives via future ingest path) admits the crash. Same defense-in-depth-at-the-boundary class as v0.5.113 (50-char slug cap on the same function), v0.5.114 (`getStartOfDay` invalid-Date guard), v0.5.115 (avatarEmoji bounding) — every public helper boundary is provably safe regardless of upstream gating. One-line fix at the top of the function: `const safeName = typeof childName === 'string' ? childName : '';` then use `safeName` instead of `childName` for the slug pipeline. The empty-string fallback flows through the existing `slug.length > 0 ? slug : 'child'` fallback at line 101 (v0.5.0 contract), so a non-string input produces `tonguetutor-child-YYYYMMDD.csv` — the same filename as a child row with all-stripped characters (e.g. `'   '` or `'!!!'`). +4 tests in the existing `buildExportFilename` describe block: null/undefined cast `as unknown as string` fall back, numeric `42` falls back, empty-object `{}` falls back — each asserts the exact pinned filename `'tonguetutor-child-20260510.csv'`. The 4-case spread pins the typeof-string contract against a contributor "simplifying" the guard (e.g. dropping it to `childName ?? ''` would let `42.toLowerCase()` crash and fail the number case loudly). Bumped `APP_VERSION` to v0.5.118. 502 tests pass across 27 suites (was 498, +4). TypeScript clean.
- v0.5.117 — Fix: uppercase `joinFamilySchema.inviteCode` at the schema boundary. The field previously chained `.string().trim().refine(isValidInviteCode, ...)` — case was preserved verbatim. `isValidInviteCode` (`src/lib/utils.ts:18`) only accepts characters from `INVITE_CODE_CHARSET = 'ABCDEFGHJKLMNPQRSTUVWXYZ23456789'` (uppercase A-Z minus I/O plus 2-9), so a lowercase-or-mixed-case input like `abc234` would be rejected by the refine even though it maps unambiguously to a valid code `ABC234`. The screen handler at `app/onboarding/join.tsx:20` already calls `.trim().toUpperCase()` before `isValidInviteCode` so the user-typed flow worked correctly, but the schema is the contract for any future ingest path that bypasses the screen (Convex sync, JSON-RPC, CSV import, deep-link param). Same defense-in-depth-at-the-boundary class as v0.5.112 (`.toLowerCase()` on email fields) — RFC 5321 permits case-sensitive local-parts but no real-world consumer treats them as distinct; same principle applied to invite codes, where the charset is provably uppercase-only and the only sane interpretation of `abc234` is "the user mis-shifted, normalize". Closes the contract uniformly: every present and future surface that parses through `joinFamilySchema` will see uppercase invite codes regardless of input case. One-line fix on the field: insert `.toUpperCase()` between `.trim()` and `.refine(isValidInviteCode, ...)`. Ordering matters: trim runs first to strip surrounding whitespace (preserves v0.5.76's contract); toUpperCase runs next so the refine sees the normalized charset; the refine then validates length and per-char membership. Replaced the existing `'rejects invite codes with lowercase letters or punctuation'` test (which locked the now-incorrect "lowercase rejected" contract) with two split tests: `'normalizes lowercase inviteCode to uppercase before validating'` (pins the transform — `'abc234'` parses successfully to `'ABC234'`) and `'normalizes mixed-case inviteCode after trimming surrounding whitespace'` (pins the order trim → uppercase → validate by feeding `'  AbC234  '` and asserting the parsed output is `'ABC234'`); plus a new `'rejects invite codes with punctuation even after case normalization'` test that preserves the punctuation-rejection half (both `'ABC-23'` and the lowercase equivalent `'abc-23'` are rejected — regression lock that the uppercase transform doesn't accidentally let through punctuation by normalizing it away). The screen handler's redundant `.toUpperCase()` is preserved untouched — the screen does its own ad-hoc validation via `isValidInviteCode` (not through the schema) and the redundant uppercase is harmless. Bumped `APP_VERSION` to v0.5.117. 498 tests pass across 27 suites (was 496, +2 net — 3 new minus 1 replaced). TypeScript clean.
- v0.5.116 — Fix: guard `captureEvent` and `identifyUser` in `src/lib/posthog.ts` against empty and whitespace-only string inputs. Both functions previously trusted the TypeScript `string` signature and called through to `posthog?.capture(event, properties)` / `posthog?.identify(userId, properties)` directly — so a caller passing `''` (an upstream bug that lets an empty event name slip through, e.g. `captureEvent(food.name)` where the food name briefly read as `''` during a render race) or `'   '` (a poorly-normalized input from a future hook that builds event names from user-typed strings) would send a literal empty-string event into PostHog's ingestion pipeline. Same risk on the identify side: a corrupted auth-store rehydration (the failure class v0.5.109 closed for the in-app auth tuple consistency check — but the rehydrated value still flows into `identifyUser` if the `App.tsx` mount calls it before the consistency check defangs it) carrying `userId: ''` would create a ghost analytics identity that no real user maps to. PostHog's backend will ingest these silently — the events show up in the dashboard as untyped/uncategorized entries, polluting the analytics table forever (or until manually purged). Same defense-in-depth-at-the-boundary class as v0.5.92 (loginSchema email trim), v0.5.103 (joinFamilySchema inviteCode charset refinement), and v0.5.78 (`deriveLocalEmailPart` sanitize) — the wrapper module is the contract for every call site, present and future, and the contract should normalize-then-call, not call-and-hope. Fix mirrors the v0.5.92 pattern verbatim: `if (typeof event !== 'string' || event.trim().length === 0) return;` at the top of `captureEvent`, same on `identifyUser` against `userId`. The `typeof` guard short-circuits before `.trim()` runs against a non-string (defensive belt — TypeScript blocks most callers, but a future loose `as any` cast in a hot path could let a number/null/undefined through), and the `.trim().length === 0` catches both `''` and `'   '` in one check. The `properties` parameter is left untouched — PostHog's SDK already accepts `undefined` properties cleanly, and a properties object with a single junk key isn't the same risk class as a missing event name (no analytics ghost row gets created from properties alone). +5 tests in a new `empty/whitespace input guards` describe block at the end of `posthog.test.ts` using the established `jest.isolateModulesAsync` pattern: captureEvent with `''` is a no-op (mockCapture not called), captureEvent with `'   '` is a no-op (regression lock for the trim half — a contributor "simplifying" the guard to bare `event.length === 0` would fail this test loudly), identifyUser with `''` is a no-op, identifyUser with `'   '` is a no-op, captureEvent with single-char `'x'` fires through to mockCapture exactly once (regression lock for the happy path — a contributor tightening the guard to require `length >= 2` would fail this test). Bumped `APP_VERSION` to v0.5.116. 496 tests pass across 27 suites (was 491, +5). TypeScript clean.
- v0.5.115 — Fix: bound `childSchema.avatarEmoji` with `.trim().min(1).max(8)` before `.default('👶')`. The field previously declared `z.string().default('👶')` with no length/trim/min — TypeScript's `z.infer` typed it as `string`, the form path at `app/child/add.tsx:33` and `app/onboarding/add-child.tsx:34` constrained input to the 12-emoji `EMOJI_OPTIONS` chip row (all 2-5 UTF-16 code units), and the DB column at `src/db/schema.ts:26` declared `text('avatar_emoji').notNull().default('👶')` with no length constraint either. Three failure paths the schema was admitting: (1) a future CSV import or Convex sync layer (the v1.0 NEXT_STEPS item) writing a 200-char displayName-like blob into the avatar slot, (2) a hand-edited dev DB row carrying surrounding whitespace `'  👧  '` that ChildSelector at `src/components/ChildSelector.tsx:36` would render verbatim alongside the child name with visible leading/trailing space, (3) an empty-string row that would render as a sized-but-empty `<Text style={styles.emoji}>` Pressable in the ChildSelector chip row — visually a "gap" the parent can't tell is tappable. Same drift class as v0.5.78 (sanitize derived email), v0.5.111 (RFC-5321 64-char cap on `deriveLocalEmailPart`), and v0.5.113 (50-char cap on `buildExportFilename` slug) — defense-in-depth at the schema boundary so every present and future ingest path is provably safe regardless of upstream UI gating. `.trim()` strips surrounding whitespace per the v0.5.75 normalize-then-validate pattern; `.min(1, 'Avatar is required')` rejects whitespace-only after trim; `.max(8, 'Avatar emoji must be 8 characters or less')` accepts the longest current `EMOJI_OPTIONS` entry `'🧑‍🍳'` (5 units: 2-unit chef + ZWJ + 2-unit cookware) with headroom for future skin-tone-modifier variants like `'👨🏽‍🍳'` (7 units) — 8 is the conservative-but-future-proof upper bound. The `.default('👶')` is preserved at the end of the chain so a missing/undefined field still fills with the canonical baby emoji (existing test `'defaults avatarEmoji to baby emoji'` at line 57 still passes). +6 tests in a new `avatarEmoji length and trim handling` describe block: single 2-unit emoji `'👧'` accepts (regression lock for the happy path so a contributor tightening max below 2 fails loudly), 5-unit ZWJ sequence `'🧑‍🍳'` accepts (pins the at-least-5-units contract — a future contributor narrowing to .max(4) would silently break the chef-emoji avatar option), 8-character boundary accepts, 9-character boundary +1 rejects (the cap regression lock), whitespace-only rejects (the trim+min(1) interaction lock — both must apply for this to hit), and surrounding whitespace strips to the bare emoji (pins the trim ordering — a contributor moving the trim after default or removing it would produce `'  👧  '` verbatim). Bumped `APP_VERSION` to v0.5.115. 491 tests pass across 27 suites (was 485, +6). TypeScript clean.
- v0.5.114 — Fix: defensive invalid-Date guard on `getStartOfDay` (`src/lib/utils.ts:43`). The function is typed `(date: Date = new Date()) => Date` so the type system blocks most callers from passing an invalid Date — but a Date can drift to invalid at runtime: `new Date('not a date')`, `new Date(undefined)`, `new Date(NaN)`, or a Date constructed from a SQLite `INTEGER` column whose value briefly read as `null` between read and `new Date(...)` wrap. The unguarded path `new Date(invalidDate)` returns another invalid Date, then `setHours(0,0,0,0)` mutates it to NaN, and downstream `.getTime()` returns NaN — which poisons `formatRelativeDate`'s day-delta computation (`Math.round((NaN - validTime) / 86400000)` returns NaN, fails every branch including the `days <= 0` "Today" check, falls through to `formatDate(date)` which has its own v0.5.102 guard returning `''`). The cascade is visible only by silent empty-string rendering on the dashboard's recently-logged list. Same drift class as v0.5.98 (`formatRelativeDate` guard), v0.5.102 (`formatDate` guard), v0.5.107 (`buildExportFilename` `at` guard) — defense-in-depth against ways a Date can drift from a real timestamp at runtime, applied uniformly at every public Date helper boundary. Fix mirrors v0.5.102's shape exactly: `const safeDate = date instanceof Date && !Number.isNaN(date.getTime()) ? date : new Date();` then the existing `new Date(safeDate)` clone and `setHours` chain runs against the validated input. The `instanceof Date` check is belt-and-suspenders since the parameter is typed `Date`, but matches the established idiom verbatim. Fallback is the current time — sensible because the only sane interpretation of "you asked me for the start-of-day of an invalid date" is "the start of today". +2 tests in the existing `getStartOfDay` describe block: `new Date('not a date')` falls back and the produced time lies between the test's before/after `Date.now()` bounds (pins the fallback IS `new Date()`, not some other constant — regression lock against a contributor "simplifying" the guard by returning a fixed epoch), plus `new Date(NaN)` falls back and the produced calendar date matches today (pins the fallback uses today's calendar, not some midnight-of-arbitrary-day shortcut). The two existing direct callers (`formatRelativeDate` at line 33-34) are already guarded upstream (v0.5.98 ensures `date` is validated before reaching line 34), but the helper is exported as a public utility — defense-in-depth at the helper boundary means every present and future caller is provably safe regardless of whether they pre-validate. Bumped `APP_VERSION` to v0.5.114. 485 tests pass across 27 suites (was 483, +2). TypeScript clean.
- v0.5.113 — Fix: cap `buildExportFilename` slug length at 50 chars so a corrupt childName row can't produce a filename that overflows the 255-byte POSIX/HFS+ limit or trips share-sheet preview truncation. `childSchema.name` validates at 50 chars on insert (`src/lib/validation.ts:11`), but SQLite has no DB-level length constraint — a row drift (future Convex sync, hand-edited dev DB, CSV import that bypasses the schema) carrying a 200-char name flowed through `buildExportFilename` unchanged, producing `tonguetutor-aaaa…[200 a's]…-20260510.csv`. Most filesystems cap at 255 bytes (POSIX); iOS Files app and macOS Finder truncate share-sheet previews around 30-40 chars; Excel on Windows truncates the title bar. Same defense-in-depth class as v0.5.111 (`deriveLocalEmailPart` RFC-5321 cap) and v0.5.107 (invalid-Date guard on the same function). Fix mirrors v0.5.111's pattern: lifted `EXPORT_FILENAME_SLUG_MAX = 50` constant with a docstring citing the rationale, then chained `.slice(0, EXPORT_FILENAME_SLUG_MAX).replace(/-+$/, '')` onto the existing sanitize pipeline AFTER the char-class strip and existing trailing-hyphen sweep. Ordering matters: applying the cap BEFORE sanitization could leave the slug ending mid-stripped-char; applying AFTER means the 50 chars are all valid slug chars by construction. The follow-up `.replace(/-+$/, '')` sweeps any dangling hyphen that the slice may have left at position 49 (when the sliced char happened to be a hyphen boundary) — preserves the v0.5.0 "no trailing hyphen on filenames" contract. The empty-string fallback `'child'` survives unchanged. +4 tests in the existing `buildExportFilename` describe block: 200-char input clamps to exactly 50 chars (regression lock — a future contributor "simplifying" the chain by dropping the slice fails loudly), mid-word truncation case pins the `'aaaa…aaaa-s'` shape (48 a's + hyphen + 's') so a contributor moving the slice before sanitization would produce a different slug shape and fail, exactly-at-hyphen-boundary case pins that the trailing-hyphen sweep fires (`'a'.repeat(49) + ' b'` → slice ends in hyphen → swept to `'a'.repeat(49)` with no dangling `-`), and a boundary case pinning 50-char input passes through unchanged so a contributor tightening the cap to 49 would fail. Bumped `APP_VERSION` to v0.5.113. 483 tests pass across 27 suites (was 479, +4). TypeScript clean.
- v0.5.112 — Fix: lowercase email at the auth-schema boundary. `loginSchema.email`, `registerSchema.email` (inherited via `.extend`), and `joinFamilySchema.email` previously chained `.trim().email()` — case was preserved verbatim. RFC 5321 permits case-sensitive local-parts, but no real-world provider treats `John@Example.com` and `john@example.com` as different inboxes; preserving case at the schema boundary is a duplicate-account-by-case trap waiting at the future Convex sync layer (the v1.0 NEXT_STEPS item). Same drift class as v0.5.75 (whitespace-only required names) and v0.5.92 (email field trim) — the schema is the contract for any future ingest path that bypasses the offline-only screens, and the contract should normalize, not preserve. One-line fix on the two declared `email` fields: insert `.toLowerCase()` between `.trim()` and `.email()`. The ordering (trim → lowercase → email format check) matters: lowercasing happens after trim so `'  John@X.COM  '` → `'John@X.COM'` → `'john@x.com'` → email check, and `registerSchema` picks up the change automatically through its `loginSchema.extend(...)` inheritance (same shape v0.5.92's surrounding-whitespace trim relied on). +5 tests in a new `email lowercasing on auth schemas` describe block: loginSchema lowercases mixed-case (`'John@Example.COM'` → `'john@example.com'`), loginSchema combines trim + lowercase, registerSchema inherits the lowercase via extend (regression lock — a future contributor switching registerSchema to a hand-built `z.object` without the inheritance would fail loudly), joinFamilySchema lowercases mixed-case, and an already-lowercase pass-through to pin the no-op happy path. Bumped `APP_VERSION` to v0.5.112. 479 tests pass across 27 suites (was 474, +5). TypeScript clean.
- v0.5.111 — Fix: cap `deriveLocalEmailPart` output at 64 characters per RFC 5321 § 4.5.3.1.1. The helper at `src/lib/utils.ts:49-56` (added v0.5.78) sanitized the input character class but had no length cap — a caller passing a 100-char displayName would produce a 100-char local-part that no RFC-compliant email validator would accept. Today the upstream `childSchema.name` and `registerSchema.displayName` both cap at 50 chars (well under 64) so the production hot path is safe — but the helper is exported from `utils.ts` and could be called by any future surface (a CSV import, a Convex sync layer, a relaxation of the upstream schema cap) where that 50-char gate doesn't apply. Defense-in-depth at the helper boundary means the contract is uniform: every caller of `deriveLocalEmailPart`, present and future, gets an RFC-valid local-part regardless of input length. Same defensive-shield class as v0.5.78 (sanitizing the character class) and v0.5.81/v0.5.88 (validating invite-code format) — close gaps before they ship in code that depends on them. Lifted the 64 magic number into a named constant `RFC_5321_LOCAL_PART_MAX = 64` with a docstring citing the RFC, then added `.slice(0, RFC_5321_LOCAL_PART_MAX)` as the last step of the sanitize chain, AFTER the existing char-class strip + period-collapse + period-trim. The ordering matters: applying the cap before sanitization could leave a 64-char output that ends mid-stripped-char, producing an RFC-invalid result; applying after means the 64 chars are all valid local-part chars by construction. The empty-string fallback `'user'` survives unchanged — if 64 chars of stripped output is still empty (e.g. all 100 chars were emoji), the fallback fires. +2 tests in the existing `deriveLocalEmailPart` describe block: a 100-char `'a'.repeat(100)` clamps to exactly 64 chars (regression lock — a future contributor "simplifying" the chain by dropping the slice fails loudly with `result.length === 100`), and a 65-char input where char 65 is a stripped `'!'` produces a 64-char output (regression lock for the apply-cap-after-sanitize ordering — a future contributor moving the slice before the sanitize chain would produce a 63-char output for this case and fail). Bumped `APP_VERSION` to v0.5.111. 474 tests pass across 27 suites (was 472, +2). TypeScript clean.
- v0.5.110 — Fix: prepend UTF-8 BOM (`\uFEFF`) to every CSV export so Excel on Windows opens files with the correct codepage. The app is designed for offline therapist sharing per NEXT_STEPS.md, and a parent who logged "Crème brûlée" or "café" saw mangled chars (`CrÃ¨me brÃ»lÃ©e`) when their feeding therapist opened the share-sheet export in Excel — Excel defaults to ANSI/Windows-1252 unless a BOM hints otherwise. Sheets and Numbers sniff content for UTF-8 detection, but Excel is the dominant therapist tool and the documented broken pipeline (canonical writeup at https://stackoverflow.com/q/6002256). One-byte fix in `formatExposuresCsv`: return `'\uFEFF' + [HEADER, ...body].join('\n') + '\n'`. The BOM is invisible to every consumer that reads UTF-8 directly (text editors, command-line tools, `csv-parse` and equivalents in any reasonable language), and it's stripped by `String.prototype.trim()` per ECMAScript 2020+ — so the existing test assertions that already use `csv.trim().split('\n')` (most of them) are unaffected and only the one strict-equality empty-export test needed updating to the BOM-prefixed expectation `'\uFEFF' + header + '\n'`. The integration tests at `export.integration.test.ts` use `.toContain()` and `.trim()` which both survive the BOM, so no integration-side updates needed. +2 new tests pin the contract: `csv.charCodeAt(0) === 0xFEFF` (regression lock — a future contributor "simplifying" the return statement to drop the BOM fails loudly), and a non-ASCII round-trip test with `foodName: 'Crème brûlée'` asserting `csv.startsWith('\uFEFF' + header)` then `csv.contains('Crème brûlée')` so the encoding pipeline is intact end-to-end (the test verifies both the BOM is at position 0 AND the multi-byte UTF-8 chars survive serialization through `csvEscape`). The CSV byte-level shape change is technically observable (the file is one byte longer) but every reasonable parser the export targets either ignores the BOM (Excel/Sheets/Numbers/`csv-parse`) or strips it transparently (`fs.readFile` + `String.prototype.trim()`), so no downstream consumer breaks. Bumped `APP_VERSION` to v0.5.110. 472 tests pass across 26 suites (was 470, +2). TypeScript clean.
- v0.5.109 — Fix: validate persisted auth-store fields on rehydration with cross-field consistency. v0.5.108 closed the equivalent gap on `child-store` and v0.5.90 on `settings-store` — but `auth-store` was explicitly deferred ("auth-store persists multiple fields whose mutual consistency is more complex"). The store persists 6 fields (userId, familyId, email, displayName, isAuthenticated, isOnboarded) and trusted zustand-persist's default shallow-merge — a corrupted MMKV blob (future schema rename, hand-edited dev DB, jailbroken device, partial write across an upgrade) carrying `userId: 42` (number), `isAuthenticated: 'true'` (string-not-bool), or worst: `isAuthenticated: true` with `userId: null` would slip through. The cross-field case is the load-bearing one: every screen-level gate (`app/(tabs)/_layout.tsx:38`, `app/(tabs)/index.tsx:100,113`) checks `(!isAuthenticated || !isOnboarded)` to decide redirects, and `app/(tabs)/settings.tsx`'s "Sign Out" button assumes the auth tuple is non-null when isAuthenticated is true — a corrupt blob breaking that contract would crash on `displayName.split(' ')[0]` or surface a "logged in as null" header. Added two predicates (`isValidStringOrNull` for the 4 string-or-null fields, `isValidBoolean` for the 2 flags) and a `merge` callback that validates each field independently against its predicate, falling back to `current.X` (the in-memory default — null/false at store-creation time) on type mismatch. The cross-field consistency check then forces `isAuthenticated = persistedAuth && tupleComplete` where `tupleComplete = userId !== null && familyId !== null && email !== null && displayName !== null` — so a corrupt blob with `isAuthenticated: true` but any field of the auth tuple null gets defanged to false (the safe direction: false → user re-onboards, vs. true → screens crash). The other fields are preserved verbatim so we don't poison good data trying to rescue bad data. `isOnboarded` is independent of the auth tuple and validated as a standalone boolean (matches its independent setter `setOnboarded` which fires from onboarding/add-child even before login completes). +9 tests in a new `src/stores/__tests__/auth-store-rehydrate.test.ts` mirroring v0.5.90 / v0.5.108 verbatim with `jest.doMock` + `jest.resetModules`: full valid state preserves, signed-out state preserves, isAuthenticated=true with userId=null defangs to false (the load-bearing cross-field test), per-field tuple-incompleteness loop covers all 4 fields (regression lock — a future contributor narrowing tupleComplete to fewer fields fails loudly), non-string-or-null userId falls back, non-boolean isOnboarded falls back, non-boolean isAuthenticated falls back ('true' string fails strict-bool check), per-field × per-bad-type loop (4 fields × 4 bad types = 16 cases pinning the predicate's narrow accept set), missing keys fall back to defaults. Bumped `APP_VERSION` to v0.5.109. 470 tests pass across 27 suites (was 461, +9). TypeScript clean.
- v0.5.108 — Fix: validate persisted `selectedChildId` on `child-store` rehydration. `src/stores/child-store.ts` previously trusted the default zustand-persist `merge` (shallow object spread) — whatever JSON came back from disk landed in state verbatim. A corrupted MMKV blob (future schema rename, hand-edited dev DB, jailbroken device, partial write across an upgrade) carrying `selectedChildId: 42` (number) or `selectedChildId: { evil: 'object' }` would slip through; the downstream `ensureSelection` does `c.id === current` (string vs non-string strict-equality — always false) so a real child id never matches a corrupt persisted value, but worse: a non-null non-string value evaluates as truthy in `current !== null`, so the early return on the children-empty path would skip clearing the bogus selection, and on the children-populated path the auto-fallback to `children[0].id` would still fire correctly but the bogus value would briefly leak into the dashboard's first render before `ensureSelection` self-healed. Same defensive-shield class as v0.5.90 (settings-store rehydrate validation against the parallel theme/feedingProfile enums) — both stores persist load-bearing keys via zustand-persist and were the only two stores in the codebase that needed the merge-callback validation; auth-store persists multiple fields whose mutual consistency is more complex (deferred). Added a single `isValidSelectedChildId` predicate gating on `value === null || typeof value === 'string'` (the canonical shape from the `selectedChildId: string | null` interface line), then a `merge` callback that destructures the persisted blob and falls back to `current.selectedChildId` (which is `null` at store-creation time, the initial value declared on line 41 of the persist init) when the persisted value fails the predicate. The `current` parameter holds the freshly-created store with default values, so the fallback is automatic — no separate "default config" object to keep in sync. Mirrors v0.5.90's settings-store-rehydrate.test.ts pattern verbatim with `jest.doMock` + `jest.resetModules` to inject corrupt MMKV blobs. +6 tests in a new `src/stores/__tests__/child-store-rehydrate.test.ts`: valid persisted string preserves; valid persisted null preserves; number falls back to null; object falls back; array falls back; missing key (undefined) falls back. The number/object/array trio is the load-bearing regression lock — a future contributor narrowing the predicate to "string only" (forgetting the legitimate null case) would fail the persisted-null test and ship a regression; widening it to accept anything truthy would fail the number/object/array tests. Bumped `APP_VERSION` to v0.5.108. 461 tests pass across 26 suites (was 455, +6 from the new test file). TypeScript clean.
- v0.5.107 — Fix: defensive invalid-Date guard on `buildExportFilename`'s optional `at` parameter (`src/lib/export.ts:81`). The function defaulted to `new Date()` so the standard call from `exportChildData` was always safe — but the parameter is exported so any caller (a future test fixture, a future scheduled-export path, a future "regenerate previous export" surface) could pass a corrupt Date instance (`new Date('invalid')`, `new Date(undefined)`, a Date constructed from a SQLite `INTEGER` column whose value briefly read as `null`) and the unguarded `at.getUTCFullYear()` / `getUTCMonth()` / `getUTCDate()` chain would emit `NaN` for each — producing the literal filename `tonguetutor-emma-NaNNaNNaN.csv`. Semantically broken: unsearchable in iOS Files app's date-keyword filter, won't sort chronologically alongside other valid exports, and confuses any therapist receiving multiple shared exports from the same parent ("which one is the latest?"). Same drift class as v0.5.96 (CSV `occurredAt` defensive Date guard), v0.5.98 (formatRelativeDate guard), v0.5.102 (formatDate guard), v0.5.106 (Number.isFinite rating guard) — defense-in-depth against ways a Date instance can drift from a real timestamp at runtime. Fix mirrors v0.5.102's shape exactly: `const safeAt = at instanceof Date && !Number.isNaN(at.getTime()) ? at : new Date();` — the `instanceof Date` check is belt-and-suspenders since the parameter is typed `Date`, but matches the v0.5.102 / v0.5.98 pattern verbatim so a future contributor scanning the codebase for the canonical "is this Date safe" idiom finds one consistent shape. The downstream three lines read from `safeAt` instead of `at`. Fallback is the current time — sensible because the only sane interpretation of "you asked me to filename this with an invalid date" is "the export was generated now". +2 tests in the existing `buildExportFilename` describe block: invalid Date falls back and the produced filename matches `/^tonguetutor-emma-\d{8}\.csv$/` with no literal "NaN" substring (the regex is the regression lock — a future contributor "simplifying" the guard by dropping the Number.isNaN check would re-emit "NaN" literally and fail loudly), plus a window-check that the fallback YMD lies between the test's before/after Date.now() bounds (pins that the fallback is `new Date()`, not some other constant); valid Date passes through unchanged producing the exact pinned filename `tonguetutor-emma-20260510.csv` (regression lock for the happy path so a contributor tightening the guard too far doesn't accidentally reject all dates). Bumped `APP_VERSION` to v0.5.107. 455 tests pass across 25 suites (was 453, +2). TypeScript clean.
- v0.5.106 — Fix: defensive `Number.isFinite` guard on rating accumulation in `calcProgressStats` (`src/lib/progress-stats.ts:67`). Previously `if (exp.rating != null)` accepted both `NaN` and `±Infinity` — neither is a meaningful rating but both are valid `number` values that pass the `!= null` gate. The schema validates rating as `.int().min(1).max(5)` (v0.5.82) but the SQLite `integer('rating')` column has no DB-level range constraint, so a corrupted row (Convex sync drift, hand-edited dev DB, future CSV import) carrying `rating: NaN` would land `totalRating += NaN` on line 68, poisoning the whole sum to `NaN`. The Progress tab's "Avg Acceptance" gauge at `progress.tsx:111` then renders `NaN` as the value. Same defensive-shield class as v0.5.95 (calcExposureProgress NaN coercion on the `exposureCount` side) and v0.5.101 (profile fallback) — the rating-accumulation half was the symmetric gap. Switched the gate to `Number.isFinite(exp.rating)` which excludes `NaN`/`±Infinity`/`null`/`undefined` in one check; cast `as number` on the accumulator since `Number.isFinite` is not a type predicate. +2 tests in the existing `calcProgressStats` describe block: a 5-row mix (4, NaN, Infinity, -Infinity, 2) returns avgRating=3 (not NaN — the regression lock pins the survival of the 4+2 average over the 3 corrupt drops); an all-non-finite case returns avgRating=0 (the no-surviving-samples path collapses cleanly through the existing `ratingCount > 0` ternary). Bumped `APP_VERSION` to v0.5.106. 453 tests pass across 25 suites (was 451, +2). TypeScript clean.
- v0.5.105 — Fix: defensive `getStageConfig` helper for unknown exposure-stage strings on `ExposureCard`/`FoodCard`. v0.5.84 added `getCategoryConfig` for the same drift class on the food-category enum, and v0.5.104 just shipped the parallel `getFeedingProfileConfig`. The `STAGE_CONFIG[stage]` bracket-deref pattern at `src/components/ExposureCard.tsx:40` and `src/components/FoodCard.tsx:24` was the last unguarded lookup of this shape — and the highest-frequency one, since every exposure row in the dashboard's recently-logged list and every food card on the Foods tab flows stage strings from drizzle queries against the `exposures.stage`/`foods` rows. SQLite has no native enum constraint (drizzle's `text('stage', { enum: [...] })` declaration only types at the row level — runtime values are unconstrained), so a corrupted DB row carrying `stage: 'mystery'` (Convex sync drift, CSV import from another app, hand-edited dev DB) would return `undefined` from the bracket lookup. ExposureCard would then crash on `stageConfig.color + '20'` at line 52 and `stageConfig.icon` at line 53 — taking down the entire dashboard recently-logged list AND the food-detail per-food exposure history. FoodCard would crash on `stageConfig.color + '20'` at line 54 (taking down the Foods tab list). Same drift class as v0.5.74 fixed for the food detail page (the `as ExposureStage` cast at line 252 was a workaround for the same gap). Three coordinated changes. (1) Added `getStageConfig(value: string | null | undefined)` to `src/lib/constants.ts` mirroring v0.5.84's `getCategoryConfig` shape exactly — gates on `value && STAGE_ORDER.includes(value)`, falls back to `STAGE_CONFIG.tolerate` (the entry-level stage — sensible visual fallback that surfaces the row at the lowest position in the hierarchy rather than disappearing it). (2) `ExposureCard.tsx` swaps `STAGE_CONFIG[stage]` for `getStageConfig(stage)` and widens the `stage` prop type from `ExposureStage` to `string` (matches v0.5.84's FoodCard widening from `FoodCategory` to `string` — the helper validates internally so the prop doesn't need the literal-union contract anymore). The `STAGE_CONFIG` and `ExposureStage` imports drop. (3) `FoodCard.tsx` swaps `STAGE_CONFIG[currentStage]` for `getStageConfig(currentStage)` inside the existing `currentStage ? ... : null` ternary so the "no stage badge if no current stage" semantics are preserved exactly. The `STAGE_CONFIG` import drops; `ExposureStage` import stays for the `currentStage?: ExposureStage` prop type (callers still benefit from the literal-union contract at type-check time, but a runtime drift now resolves rather than crashes). The four other `STAGE_CONFIG[...]` lookups in the codebase (`StageIndicator.tsx:53`, `app/(tabs)/log.tsx:184`, `app/(tabs)/progress.tsx:195`, `app/(tabs)/index.tsx:194`, `app/food/[id].tsx:196,206`) iterate over `STAGE_ORDER` directly or run the value through `getHighestStage`/`getNextStage` validators (which already filter unknown stages per v0.5.4 / v0.5.11), so the index is provably safe — left untouched. The helper is wired only at the DB-sourced boundaries where the runtime value is uncertain. +4 tests in a new `getStageConfig` describe block in `constants.test.ts`: every known stage resolves to its exact STAGE_CONFIG entry (regression lock — a future contributor "simplifying" the helper to always return `.tolerate` would fail the loop), unknown strings (`'mystery'`, mixed-case `'TOLERATE'`, empty string) fall back to `.tolerate`, `null`/`undefined` fall back to `.tolerate`, and a fallback-shape assertion pins that the returned config has non-empty label + icon + color (matching the existing `^#[0-9A-Fa-f]{6}$` regex used elsewhere in the file) + description strings — so any caller doing the deref is provably safe. Bumped `APP_VERSION` to v0.5.105. 451 tests pass across 25 suites (was 447, +4). TypeScript clean.
- v0.5.104 — Fix: defensive `getFeedingProfileConfig` helper for unknown profile values. v0.5.101 hardened `getThresholdForProfile` against runtime drift (corrupted MMKV blob, future Convex sync mismatch, hand-edited dev DB) — but the parallel `FEEDING_PROFILE_CONFIG[feedingProfile]` bracket-deref pattern at `app/(tabs)/progress.tsx:160` (`.label`) and `app/(tabs)/settings.tsx:155` (`.description`) was still unguarded. A drifted profile value would return `undefined` from the lookup, then crash on the `.label`/`.description` deref — taking down the Progress tab's "Exposures Toward Acceptance" header and the Settings tab's profile-description block. Settings's other lookup at line 134 iterates over `FEEDING_PROFILES` directly so is provably safe and stays untouched. Added `getFeedingProfileConfig(profile: unknown)` to `src/lib/thresholds.ts` mirroring v0.5.84's `getCategoryConfig` and v0.5.101's `getThresholdForProfile` fallback shape — gates on the existing `isValidProfile` type guard, falls back to `FEEDING_PROFILE_CONFIG.typical` (the canonical default the v0.5.101 work established). The `unknown` parameter type (rather than `FeedingProfile`) acknowledges the helper is the boundary where runtime values cross the literal-union contract — no caller has to cast to call it. Two callsites switched: `progress.tsx` swaps the import + lookup; `settings.tsx` keeps the `FEEDING_PROFILE_CONFIG` import for the safe `FEEDING_PROFILES.map` chip iteration but routes the bare `feedingProfile` lookup through the new helper. +6 tests in a new `getFeedingProfileConfig` describe block: every valid profile resolves to its exact CONFIG entry (regression lock — a future contributor "simplifying" the helper to always return `.typical` would fail the loop), unknown string falls back, undefined/null fall back, non-string (number/object/array) falls back, and a fallback-shape assertion pins that the returned config has non-empty label + description strings (so any caller doing the `.label`/`.description` deref is provably safe). Bumped `APP_VERSION` to v0.5.104. 447 tests pass across 25 suites (was 441, +6). TypeScript clean.
- v0.5.103 — Fix: tighten `joinFamilySchema.inviteCode` to validate charset, not just length. Schema previously declared `z.string().trim().length(6, '...')` (`src/lib/validation.ts:95`) — only the length was enforced. The screen handler at `app/onboarding/join.tsx` already calls `isValidInviteCode` directly (added v0.5.88) so the user-typed flow rejects ambiguous chars correctly, but the schema is the contract for any future ingest path that bypasses the screen (Convex sync, JSON-RPC, CSV import). A length-6 garbage string like `'ABC123'` (contains '1' which is excluded from the v0.5.88 charset) parsed cleanly. Replaced `.length(6, ...)` with `.refine(isValidInviteCode, { message: 'Invite code must be 6 characters and use only A–Z (excluding I, O) and 2–9.' })` — single-sources the contract with the screen handler. Test fixtures using `'ABC123'` updated to `'ABC234'` (5 places) since '1' is now correctly rejected by the schema. +3 tests pin charset rejection: ambiguous-letter (O/I), ambiguous-digit (0/1), lowercase + punctuation. Bumped `APP_VERSION` to v0.5.103. 441 tests pass across 25 suites (was 438, +3). TypeScript clean.
- v0.5.102 — Fix: defensive invalid-Date guard on `formatDate` in `src/lib/utils.ts`. `formatRelativeDate` got the same guard in v0.5.98 (early-returns empty string on invalid Date or non-Date input before delegating to `formatDate`), but `formatDate` itself — the underlying public helper — had no guard. A direct caller passing a corrupt Date (e.g. `new Date(undefined)`, `new Date('not a date')`, or a SQLite `INTEGER` column whose value briefly appears as `null` between read and `new Date(...)` wrap) would surface the literal string `"Invalid Date"` verbatim through the rendered UI. Today the only direct callers are the formatRelativeDate fallback (already guarded upstream) and the test file, but the helper is exported as a public utility — defense-in-depth for future callers (a CSV export column, a meta row on the food detail page, an offline notification body) means the guard belongs on the helper itself, not just on the caller. Same shape as v0.5.98 (and v0.5.96's CSV-export `occurredAt` guard): `if (!(date instanceof Date) || Number.isNaN(date.getTime())) return '';` early-returns. +2 tests in the existing `formatDate` describe block: invalid Date inputs (`new Date('not a date')`, `new Date(NaN)`) return `''`; non-Date values (`undefined`, `null`) return `''`. Bumped `APP_VERSION` to v0.5.102. 438 tests pass across 25 suites. TypeScript clean.
- v0.5.101 — Fix: defensive fallback in `getThresholdForProfile` for invalid profile values. The function previously did a bare `EXPOSURE_THRESHOLDS[profile]` lookup — TypeScript's `FeedingProfile` literal-union ensured callers passed valid values at compile time, but a runtime drift (corrupted MMKV blob the v0.5.90 rehydration guard didn't catch, future Convex sync schema mismatch, hand-edited dev DB, future contributor using `as FeedingProfile` cast) would return `undefined`, cascading through `calcExposureProgress` as `current/undefined = NaN` for `pct` and `current >= undefined = false` for `reached`. The v0.5.95 NaN/Infinity coercion only hardened the `exposureCount` side; the `threshold` side was still vulnerable. Added `isValidProfile` type guard against `FEEDING_PROFILES` plus `DEFAULT_PROFILE = 'typical'` constant; the lookup now resolves to `EXPOSURE_THRESHOLDS[isValid ? profile : DEFAULT_PROFILE]`. Mirrors v0.5.84's `getCategoryConfig` fallback pattern. +6 tests: invalid string, undefined, null, non-string (number/object), and an integration test asserting `calcExposureProgress(7, 'restrictive')` returns sane `{threshold:15, pct:7/15, reached:false}` rather than NaN. Bumped `APP_VERSION` to v0.5.101. 437 tests pass across 25 suites (was 431, +6). TypeScript clean.
- v0.5.100 — Fix: reject implausibly old DOB on `childSchema.dateOfBirth`. v0.5.87 added a future-date guard but left the symmetric past-date side open — a parent fat-fingering the year as `1925-05-07` instead of `2025-05-07` (off-by-100 typo, the most common DOB fat-finger class) passed validation, persisted into `children.dateOfBirth`, and the downstream age computation (Settings tab today, future Progress-tab "child is N months old") would silently render a "100-year-old child". Added a third `.refine()` chained after the existing two: re-runs the format check (early-return `true` on undefined/empty/malformed so the format and future-date refines own those error messages — preserves the v0.5.87 error-message-precedence contract) then computes a UTC-anchored "earliest" date 130 years before today (no living human is older — conservative bound that catches the off-by-100 / off-by-1000 / year-typo classes without false-rejecting any plausible adult-as-child input the app might one day support). The `.setUTCFullYear(... - 130)` rolls forward correctly across leap-year boundaries via JS Date semantics. +6 tests in a new `childSchema.dateOfBirth past-date guard` describe block: 1-year-ago and 100-year-ago accept (regression lock for the conservative bound — a contributor narrowing to 25 years would fail the 100-year case loudly); 150-year-ago and `1825-05-07` reject with the new "too far in the past" message; `0001-01-01` rejects (extreme case); malformed `99-99-99` still surfaces the format error not the past-date message (pins the error-precedence contract). Bumped `APP_VERSION` to v0.5.100. 431 tests pass across 25 suites (was 425, +6). TypeScript clean.
- v0.5.93 — Fix: prevent double-tap duplicate family/user creation on the first-launch onboarding screen. `app/onboarding/index.tsx`'s `handleGetStarted` did two `db.insert` calls (families, then users) followed by `login()` and `router.push('/onboarding/add-child')` — but the "Get Started" Button had no `loading` or `disabled` prop, so a parent who tapped twice in quick succession (common on slower devices where the SQLite write + router transition take ~150-300ms) triggered two parallel handler runs, each generating its own `familyId`/`userId`/`inviteCode` via `generateId()` (random per-call) and inserting two distinct family rows + two distinct user rows. The second `login()` call would overwrite the first's auth-store state, leaving the user authenticated as the second family while the first family's rows persisted as orphans in `families` and `users` — invisible at the moment they're created (the parent successfully reaches the add-child screen and proceeds normally), and visible only at sync time when the future Convex layer (the v1.0 NEXT_STEPS item) replicates ghost rows that no client knows about. Same drift class as v0.5.91 (duplicate user-email pre-check on Join Family) and the standard double-tap class for any async screen-level handler doing irreversible writes — every other write surface in the codebase already follows the established pattern (`app/onboarding/add-child.tsx:24,41,43,63` carries `saving` state + `if (!familyId || saving) return;` guard + try/finally + `loading={saving} disabled={saving}` Button props; `app/onboarding/join.tsx` uses `setLoading(true)` similarly). The first-launch onboarding screen was the only async-write Button in the app missing this pattern. Four coordinated changes: (1) imported `useState` from react and added `const [saving, setSaving] = useState(false);`, (2) early-return guard `if (saving) return;` at the top of `handleGetStarted`, then `setSaving(true);` immediately after — the guard short-circuits parallel taps before any work begins, the set fires synchronously so a second tap one render later sees the updated state, (3) `setSaving(false)` only in the catch block — on success the screen unmounts via `router.push('/onboarding/add-child')` so leaving `saving=true` until unmount is correct: it keeps the Button visually disabled during the in-flight transition window, preventing a stale third tap from re-firing the handler after the inserts succeeded but before the screen actually navigated away, (4) `loading={saving} disabled={saving}` on the Button element — `loading` swaps the label text for an ActivityIndicator (visual cue), `disabled` ensures Pressable's onPress short-circuits below the React layer (defensive belt — even if a child component swallowed the saving check, the Pressable wouldn't fire). The Button component (`src/components/Button.tsx`) already carries `accessibilityState={{ busy: loading, disabled: disabled || loading }}` (v0.5.46) and `aria-busy={loading}` (also v0.5.46) so VoiceOver/TalkBack announces "dimmed, busy" during the in-flight window. No new automated tests — this is a screen-level useState + handler-side-effect that needs a render harness, and the change matches the v0.5.91 / v0.5.81 / v0.5.76 ship-without-test pattern for screen-level handlers (the underlying Button's loading/disabled prop forwarding is already locked by the Button component tests). Bumped `APP_VERSION` to v0.5.93. 408 tests pass across 25 suites. TypeScript clean.
- v0.5.92 — Fix: trim surrounding whitespace on email fields in `loginSchema` and `joinFamilySchema` (and `registerSchema` via inheritance) before format validation. The five user-facing `z.string().min(1)` text fields across the schemas all got `.trim()` in v0.5.75, but the three email fields were left as bare `z.string().email().max(254)` — so a paste of `"  test@example.com  "` from a chat message or a clipboard manager that adds trailing whitespace failed the `.email()` regex and surfaced the generic "Invalid email" message, leaving the user to figure out the surrounding-space slip on their own. Same drift class as v0.5.75 (whitespace-only required names) — schemas that imply normalization but only enforce literal-byte equality. Three of the six places `.trim()` belongs got it in v0.5.75, the email half was missed because email strings are also gated by `.email()` which a contributor might assume already covered "all the obvious normalization" — it doesn't, RFC 5321 doesn't permit leading/trailing whitespace in the local-part or domain. Today these schemas aren't yet wired into screen code paths (the offline-only auth model uses `deriveLocalEmailPart` to synthesize emails, not user-typed ones — see v0.5.78), but the schemas are the contract for the future Convex sync layer (the v1.0 NEXT_STEPS item) where typed emails will reach the user-input boundary. Closing this gap before that path goes live means the contract is uniform across all user-typed strings: `.trim()` then format-validate. Two coordinated edits to `src/lib/validation.ts`: (1) loginSchema.email gets `.trim()` between `.string()` and `.email()` (registerSchema inherits via `loginSchema.extend(...)` so it picks up the trim automatically — same inheritance shape v0.5.57's max-length boundary tests confirmed), (2) joinFamilySchema.email gets the same. The `.trim()` runs before `.email()` so a leading/trailing whitespace gets stripped first, then the format check passes; previously the whitespace failed the regex and the user saw a misleading format error. Password fields are intentionally not trimmed (preserves the v0.5.75 decision: some users include leading/trailing space in passwords — unusual but legal). +5 tests in a new `whitespace handling on email fields` describe block at the end of `validation.test.ts`: loginSchema strips surrounding whitespace and rejects whitespace-only (which fails the format check after trim, regression lock for the strip-then-validate ordering); registerSchema inherits the trim (pins the inheritance contract — a future contributor switching registerSchema to `z.object({ ... }).merge(loginSchema)` or rewriting it from scratch without the trim fails loudly); joinFamilySchema strips and rejects whitespace-only with the same shape. The whitespace-only rejection cases pin the failure mode that the trim is meant to surface — the user can no longer accidentally submit `"   "` as an email; they get a format error pointing at the actual problem. Bumped `APP_VERSION` to v0.5.92. 408 tests pass across 25 suites (was 403, +5). TypeScript clean.
- v0.5.91 — Fix: pre-check duplicate user-email before insert in the Join Family flow. `app/onboarding/join.tsx`'s `handleJoin` derives an offline-only email from the typed displayName (`${deriveLocalEmailPart(trimmedName)}@tonguetutor.app`, hardened in v0.5.78) and unconditionally inserts a fresh row into `users` after the family lookup succeeds. The `users` schema at `src/db/schema.ts:11-19` declares `email: text('email').notNull()` with NO `.unique()` constraint — SQLite accepts the duplicate insert, and the v0.5.80 sign-out flow (which clears auth-store but not the underlying `users` rows) sets up the exact failure mode: a parent who joined "Anne", signed out, and re-joined the same family with the same name on the same device would create two distinct `users` rows with the same email. A second user on the same device (mom and grandma both typing "Annie") hits the same gap. Today the duplication is invisible because the auth-store keys on the freshly-generated `userId` and the secondary parent never sees a "Family Members" UI — but every duplicate row will replicate up to the future Convex cloud-sync layer (the v1.0 NEXT_STEPS item), creating ghost accounts that downstream "Family members" UIs (the kind a child pediatrician would see when reviewing exposure history) would render as `Anne, Anne` twice. The bug is silent at the moment it occurs and visible at the moment it can no longer be fixed cheaply (post-sync the rows have stable IDs that other tables reference). Same drift class as v0.5.78 (sanitize derived email) / v0.5.81 (length check) / v0.5.88 (charset check) — the same handler accumulating defensive guards for ways a manually-typed name can drift from what the system can safely accept. Two coordinated changes: (1) imported `and` from drizzle-orm alongside the existing `eq` so multiple WHERE clauses can be composed (drizzle's `.where()` is single-shot — repeated calls override each other), (2) lifted the `email` derivation up before the user-existence check (was at line 48 right before the insert), then added a select against `users` filtered by `and(eq(familyId, family.id), eq(email, derivedEmail))` — surfaces an `Alert.alert('Name Already Taken', 'A family member is already using this name. Please choose a different display name.')` if a row matches and short-circuits the handler. The check is scoped to `(familyId, email)` rather than just `email` because two different families can legitimately have a member with the same name — the constraint is *intra-family uniqueness*, not global. Auto-login-as-existing-user was considered and rejected for privacy: the offline auth model has no password, so silently logging grandma in as mom (the same name on the same device) would be a privacy leak, not a UX win. Blocking with a clear error preserves explicit identity selection. The user can pick a different name (Anne 2, Anne S, etc.) and retry. The `Alert.alert` matches the established UX pattern for this handler — same modal-style as v0.5.81's "Invalid Code" and v0.5.88's "Invite code must be 6 characters and use only A–Z (excluding I, O) and 2–9." No new automated tests — this is a screen-level Alert.alert handler that needs a render harness, and the change matches the v0.5.81 / v0.5.76 ship-without-test pattern for the same handler (the prior `isValidInviteCode` helper extracted in v0.5.88 was tested because it was a pure helper; this commit's logic is DB-bound query orchestration, not a pure function). Bumped `APP_VERSION` to v0.5.91. 403 tests pass across 25 suites. TypeScript clean.
- v0.5.90 — Fix: validate persisted enum values on settings-store rehydration. The MMKV-backed `settings-store` persists `theme: 'light' | 'dark' | 'system'` and `feedingProfile: 'typical' | 'picky' | 'arfid'` via zustand's `persist` middleware, but the default `merge` callback is a shallow object spread — it trusts whatever JSON came back from disk. Three real failure paths exist for this trust: (1) a future schema rename (e.g. `feedingProfile` enum dropping `'arfid'` for `'restrictive'`) on a previously-installed app would rehydrate the dead `'arfid'` value into state, (2) a parent jailbreaking the device or a developer hand-editing the persisted blob during testing could leave `theme: 'evil'` in MMKV, (3) a partial write or a future MMKV upgrade-bug could surface a number/null where a string is expected. Each invalid value flows downstream into surfaces that crash or produce NaN: `FEEDING_PROFILE_CONFIG[profile].label` at `progress.tsx:156` throws on an unknown profile key, `EXPOSURE_THRESHOLDS[profile]` returns undefined → `current/undefined` produces NaN in `calcExposureProgress`, and the Theme chip row at `settings.tsx:124-132` would show no chip highlighted (`theme === t` is false for every chip). Same defensive-shield class as v0.5.84 (getCategoryConfig fallback against unknown enum drift), v0.5.83 (whitespace-only optional text → undefined), and v0.5.86 (CSV formula injection guard) — schemas/storage that look correct on the visible-output test but admit a class of input the format permits. Three coordinated changes in `src/stores/settings-store.ts`: (1) extracted `THEME_MODES = ['light','dark','system'] as const` so the validator and the type both read from a single source of truth (matches the v0.5.88 INVITE_CODE_CHARSET pattern), (2) added two `is*` predicates that gate on `typeof === 'string' && (CONST as readonly string[]).includes(value)` — the typeof guard short-circuits before the includes call so a non-string (number, null, object) doesn't pay the array scan, (3) added a `merge` callback to the zustand `persist` config that destructures the persisted blob, runs each field through its predicate, and falls back to `current.theme`/`current.feedingProfile` (the in-memory defaults at store-creation time) when invalid. The merge callback is the canonical zustand-persist hook for this exact use case — it runs once on rehydration, before any consumer reads the state, so a corrupted theme never reaches `useUnistyles()` or the Theme chip render path. The `current` parameter holds the freshly-created store with default values (`theme: 'system'`, `feedingProfile: 'typical'`), so the fallback is automatic — no separate "default config" object to keep in sync. Imported `FEEDING_PROFILES` from `@/src/lib/thresholds` (already exported as `as const` since v0.4.0) so the validator's accept-set is provably the same as the canonical enum the rest of the app reads against. +7 tests in a new `src/stores/__tests__/settings-store-rehydrate.test.ts` file using the established `jest.doMock` + `jest.resetModules` pattern from `mmkv-resilience.test.ts`: valid persisted state preserves both fields; invalid theme falls back to default while preserving valid feedingProfile; invalid feedingProfile falls back to default while preserving valid theme; both invalid (number + null) → both defaults; missing keys → both defaults; per-mode loop pinning all 3 valid theme modes individually (regression lock — a future contributor narrowing THEME_MODES and forgetting the predicate fails loudly); per-profile loop pinning all 3 valid feeding profiles (same lock for the FEEDING_PROFILES side). The mock returns `{ state: ..., version: 0 }` JSON because that's the wrapper shape zustand-persist's `createJSONStorage` reads. Bumped `APP_VERSION` to v0.5.90. 403 tests pass across 25 suites (was 396, +7). TypeScript clean.
- v0.5.89 — Feature: surface the food's `isSafeFood` flag as a `safe_food` column in the therapist-facing CSV export. The schema captured the flag (drizzle `integer('is_safe_food', { mode: 'boolean' }).notNull().default(false)` at `src/db/schema.ts`), the v0.3.0 safe-food UI surfaces it on both the Foods tab pinned row and the food detail page's "Mark as Safe Food" toggle, and `fetchExportRows`'s drizzle projection already pulls every other food/exposure dimension a feeding therapist would want to see — but the CSV export silently dropped this one column. A therapist reading the export couldn't distinguish a parent's *baseline* food (the SOS-style preferred food the child reliably accepts, kept available alongside new targets) from an *exposure target* (the new food the parent is working through the 6-stage hierarchy on). Both render as identical rows: same food name, same stage progression, same rating curve. The distinction matters clinically — a "Stage 5 (Taste), Rating 2" row reads very differently for a safe food (concerning regression) versus a new target (normal early-exposure resistance). Same drift class as v0.5.77 (temperature column) and v0.5.50 (food detail page meta row): features land on the capture/persist path before the read path catches up. Followed v0.5.77's shape exactly — four coordinated edits to `src/lib/export.ts`: (1) `isSafeFood: boolean` added to the `ExposureRow` interface between `category` and `stage` (semantic adjacency: both are food-property columns, with stage being the only exposure-property column adjacent to them on the food side), (2) `'safe_food'` inserted at index 3 of the `HEADER` constant (between `'category'` and `'stage'`) — snake_case header name matches the existing `meal`/`setting` naming, (3) `csvEscape(r.isSafeFood)` added to the `formatExposuresCsv` row mapper at the matching position — `String(true)` → `'true'` and `String(false)` → `'false'` via the existing `csvEscape` natural coercion, no extra mapping needed since Excel/Sheets parse these literally as boolean values without quote-wrapping, (4) `isSafeFood: schema.foods.isSafeFood` added to the drizzle `select` projection in `fetchExportRows`. Test fixtures in `export.test.ts` widened to include `isSafeFood: false` on the default row + the optional-fields-empty case still pins the now-12-column count + the temperature/texture parity test cols shifted from [6]/[7] to [7]/[8]; integration test fixtures in `export.integration.test.ts` widened on all three rows (Apple `false`, Pear `true`, Banana `true`) and the substring assertion `'date,food,category,stage'` updated to `'date,food,category,safe_food,stage'`, header-only empty-export assertion updated to the new 12-column header. Two new pinning tests added: "serializes isSafeFood as a boolean string adjacent to category" pins col[2]=='fruit' && col[3]=='true' so a future contributor reordering the HEADER without updating the mapper gets a failing test (regression lock against silent reorder drift, same shape as v0.5.77's "temperature alongside texture" lock), and "serializes isSafeFood=false as the literal string 'false'" pins the false-branch coercion so a future "simplification" that maps boolean-false to empty string fails loudly. The CSV column count change from 11 → 12 is technically a breaking change for any therapist with a parser pinned to 11 cols, but the offline single-user app has no shipped therapist tooling depending on a frozen header (and the columns are name-keyed, not positional, in any reasonable Excel/Sheets parser). Bumped `APP_VERSION` to v0.5.89. 396 tests pass across 24 suites (was 394, +2). TypeScript clean.
- v0.5.88 — Fix: validate invite-code charset on the Join Family flow. v0.5.81 added a length check (`trimmedCode.length !== 6`) but left the *charset* unchecked — `app/onboarding/join.tsx:28` accepted any 6-character string, so a parent reading their partner's code aloud and mishearing `B` as `8` (or `S` as `5`, `2` as `Z`, etc.) typed a length-6 garbage string that passed the guard, hit the `eq(schema.families.inviteCode, trimmedCode)` lookup at line 36, missed silently, and surfaced the misleading "No family found" alert at line 40 — directing the user to debug a sync issue or re-ask their partner for the code, when the actual problem is a localized typo the system could detect and report precisely. The generator at `src/lib/utils.ts:8` produces codes from `'ABCDEFGHJKLMNPQRSTUVWXYZ23456789'` (24 letters skipping I and O, 8 digits skipping 0 and 1) — the OWASP-flavored "no visually-ambiguous characters" subset — so a real generated code can never contain `O`/`I`/`0`/`1`. A typed code that does is provably not a code anyone's app generated. Same drift class as v0.5.81 (length check) and v0.5.76 (whitespace trim) — the same handler accumulating defensive guards for ways a manually-typed code can drift from what the generator emits. Three coordinated changes. (1) Extracted the charset-and-length pair into `INVITE_CODE_CHARSET` and `INVITE_CODE_LENGTH` constants in `utils.ts` so the generator and validator share a single source of truth — a future contributor relaxing the charset (e.g. allowing lowercase) only has to touch one line, and the validator's accept-set is provably the same as the generator's emit-set. (2) Added `isValidInviteCode(code)` predicate that gates on length === INVITE_CODE_LENGTH and every char ∈ INVITE_CODE_CHARSET. The two checks are both fast — the length guard short-circuits before the for-loop runs on bad-length input, and the per-char `.includes` over a 32-char string is O(32) per char, well below any perceptible threshold for an 8-char-max input. (3) `join.tsx` now calls `isValidInviteCode(trimmedCode)` instead of the bare length check; the alert message updated to "Invite code must be 6 characters and use only A–Z (excluding I, O) and 2–9." — directs the user at the actual constraint instead of just length, and the precise enumeration of excluded letters/digits matches what they see when their partner shares a code. Tested empirically: typing `ABCO23` → tap Join → "Invalid Code: Invite code must be 6 characters and use only A–Z (excluding I, O) and 2–9." (was: "Not Found, No family found with that invite code" via the silent eq-mismatch path). +8 tests in a new `isValidInviteCode` describe block in `utils.test.ts`: round-trip 50 generated codes (regression lock — if a future contributor narrows the charset and forgets to update the validator, the round-trip fails loudly), three hand-crafted accept cases (`ABC234`, `ZYXWVU`, `234567` — pinning the digit-only and consonant-cluster boundaries), shorter/longer-length rejection (3 + 2 cases), the visually-ambiguous-letters case (`O`, `I` mid-code), the visually-ambiguous-digits case (`0`, `1`), lowercase rejection (caller is responsible for uppercasing — matches the existing `inviteCode.trim().toUpperCase()` flow at `join.tsx:20` and the input-field's `text.toUpperCase()` callback at line 96), and punctuation/whitespace rejection. The original `'only contains allowed characters (no ambiguous chars)'` test on `generateInviteCode` is unchanged — still pins the regex `/^[ABCDEFGHJKLMNPQRSTUVWXYZ23456789]{6}$/` against the generator output, providing a third regression lock independent of the new validator. Bumped `APP_VERSION` to v0.5.88. 394 tests pass across 24 suites (was 386, +8). TypeScript clean.
- v0.5.87 — Fix: reject future dates in `childSchema.dateOfBirth`. v0.5.79 closed the syntactic gap (rejecting `12/05/2020`, `2020-13-01`, `2020-02-30`, free-form text like `yesterday`) but left the semantic bound open — a parent typing `2099-05-07` or any other future-dated YYYY-MM-DD passed validation and persisted into `children.dateOfBirth`. The downstream age-display copy (Settings tab, future Progress-tab "child is N months old") would then compute a negative age, and any future cohort-bucketing logic (the kind of "show me my child's exposure trends compared to similar-age peers" feature in the project's Convex roadmap) would silently include misclassified rows. Same drift class as v0.5.79 — the format check answers "is this a parseable calendar date?" but not "is this a *plausible* DOB?" Today's parents typing into the field don't enter future dates intentionally, but fat-finger typos (`2026-05-07` vs `2025-05-07`, or a mistyped year like `9024` becoming `9024-05-07` which round-trips as a valid YYYY-MM-DD) slip through with no recovery hint — the persisted row is then a permanent off-by-thousands error in a field most parents won't revisit after onboarding. Added a second `.refine()` chained after the existing format-validation refine: re-runs the regex check (early-return true on undefined/empty/malformed so the format refine owns those error messages — keeps the user-facing error precise: a malformed string says "must be a valid YYYY-MM-DD date", a future date says "cannot be in the future"), parses to UTC midnight via the same `new Date(\`${val}T00:00:00Z\`)` construction the format check uses, and compares `d.getTime() <= Date.now()`. The UTC anchor matters for timezone fairness: a parent in UTC+13 logging on local-day X+1 while UTC says X would otherwise hit a spurious "future" rejection on the same calendar day; pinning both sides of the comparison to UTC means today === today everywhere on Earth. The `<= Date.now()` (not `<`) is deliberate so a same-day birth (an unusual but real case — newborn registered before midnight UTC) isn't rejected. The format refine's malformed-string handling is preserved untouched (the future-refine returns `true` early on malformed input so format errors aren't double-reported with a contradictory future-date message). +4 tests in a new `childSchema.dateOfBirth future-date guard` describe block: today (deterministic via `new Date()` + UTC YYYY-MM-DD helper) accepts; tomorrow (Date.now() + 86_400_000) rejects with the exact "cannot be in the future" message; far-future `2099-05-07` rejects with the same message; and a malformed string `tomorrow` still surfaces the v0.5.79 format-error message, not the new future-date message — pins the error-message-precedence contract so a future contributor reordering refines doesn't accidentally swap which message a malformed-vs-future input gets. Bumped `APP_VERSION` to v0.5.87. 386 tests pass across 24 suites (was 382, +4). TypeScript clean.
- v0.5.86 — Fix: defend the Settings → Export Data CSV against formula injection (OWASP CWE-1236). `csvEscape` in `src/lib/export.ts` previously quote-escaped commas, embedded quotes, and newlines per RFC 4180 — but had no guard against the cell-starts-with-trigger-character class of bug that lets a parent-typed note like `=cmd|'/c calc'!A0`, `+1234567890`, `-cmd|/c notepad`, or `@SUM(1,2)` execute as a formula when a feeding therapist opens the export in Excel/Sheets/Numbers. The note column is the realistic vector — a parent typing `=she said no, then ate it` (or pasting from a chat where the leading char happens to be a trigger) writes that string verbatim into `exposures.notes`, and the v0.5.0 CSV export emits it un-escaped to a downstream spreadsheet that interprets it as a live formula. Other columns are bounded by enum/integer schemas (rating is 1-5 int per v0.5.82, stage/category/temperature/texture/setting/mealType are zod enums per v0.5.67's lock) so they can't carry trigger chars under the current data model — but `notes` and `preparation` are free-form strings, and `foodName` is also user-typed (a parent could legitimately name a food `+Pluses Cereal` or `@Tea`). Same defensive-shield class as v0.5.84 (getCategoryConfig fallback against unknown enum drift) and v0.5.83 (whitespace-only optional text → undefined) — schemas/escapers that look correct on the visible-output test but admit a class of malicious input the format permits. Per OWASP guidance: detect leading `=`, `+`, `-`, `@`, tab, or CR and prefix the cell value with a single quote (`'`) inside proper RFC 4180 quoting — the receiving spreadsheet renders the literal text without evaluating, and most modern viewers strip the apostrophe from display so the parent reading their own export sees `=SUM(A1)` not `'=SUM(A1)`. Added `FORMULA_TRIGGERS = /^[=+\-@\t\r]/` regex at module scope (single source of truth, matches the OWASP-published trigger set), then split the existing escape branch into `needsQuotes` (the existing `[",\n\r]` test) and `needsFormulaGuard` (new — gated to `typeof value === 'string'` so numeric inputs like `-5` stringify cleanly without quotes; the schema rules out negative ratings but the type guard is a future-proofing belt). When either fires, the value is prefixed with `'` if needed, then the existing double-quote-escape pass runs on top — so `=said "no"` becomes `"'=said ""no"""` (formula-defanged AND double-quote-escaped, RFC 4180 valid). +9 unit tests in a new `formula injection guard` describe block: every trigger char (=, +, -, @, tab, CR) gets its own one-liner pinning the exact escaped output (regression locks against a future contributor "simplifying" the trigger regex), one negative case asserts a trigger char appearing later in the value (`total = 5`) is unaffected (the guard is leading-only — no false positives on legitimate prose), one combined case pins the embedded-quote-after-formula-prefix order (`=said "no"` → `"'=said ""no"""`) so a future refactor of the branching can't silently desync the prefix and quote-escape passes, and one numeric case (`-5`) pins that the typeof-string gate keeps numeric coercion clean. Bumped `APP_VERSION` to v0.5.86. 382 tests pass across 24 suites (was 373, +9). TypeScript clean.
- v0.5.85 — Fix: dashboard "Foods Tracked" summary card now counts every distinct food with at least one logged exposure, not just foods bucketed into a known stage. `app/(tabs)/index.tsx:123` previously derived the displayed value as `Object.values(stageCounts).reduce((a, b) => a + b, 0)` — sum of the per-stage food counts produced by `computeStageCounts`. That helper (introduced v0.5.10) intentionally skips exposures whose `stage` string isn't in `STAGE_ORDER` so a legacy/unknown stage row doesn't corrupt the Stage Distribution grid (`counts['mystery']` would never render under the STAGE_ORDER-driven JSX). Correct semantics for the distribution widget — wrong semantics for the summary card. A child with two distinct foods, one of which carried only unknown-stage exposures (future Convex sync drift, CSV import from another app, hand-edited dev DB) would show "Foods Tracked: 1" on the dashboard while the parent had clearly tracked two foods. Today the form path can't produce unknown stages (the StageIndicator chip row at `app/(tabs)/log.tsx:157-162` is gated to STAGE_ORDER), but the in-codebase drift class is documented — v0.5.11 closed the parallel gap on `progress-stats.ts` by switching `totalFoods` from `Object.keys(highestStagePerFood).length` to `Object.keys(exposureCountPerFood).length` for exactly this reason ("a food with only unknown-stage rows is still a tried food, even if its highest stage can't be classified"). Same fix shape on the dashboard. Adds a `foodsTrackedCount` state derived from `new Set(allExposures.map((e) => e.foodId)).size` — counts distinct foodIds across the entire exposure pull regardless of stage validity. The summary card's value and `accessibilityLabel` now read from this state. Renamed the surviving sum-of-stage-buckets local from `totalFoodsTracked` to `stageDistributionTotal` and used it as the gate for the Stage Distribution section (`stageDistributionTotal > 0` — same gate semantics as before, but the name now matches what it actually represents: "do we have any foods bucketed into known stages to show distribution for"). Two locals, one role each — no more name overload where the same number gated unrelated UI surfaces. The Set construction is one expression; not extracted to a helper since it doesn't recur in the codebase and the inline form reads cleanly. No tests touched — the change is screen-level state derivation, not a logic helper; the existing tests for `computeStageCounts` (which still does the right thing for distribution) continue to pin the Stage Distribution semantics. Bumped `APP_VERSION` to v0.5.85. 373 tests pass across 24 suites. TypeScript clean.
- v0.5.84 — Fix: defensive `getCategoryConfig` fallback for unknown food category strings. The `foods.category` SQLite column is declared via drizzle's `text('category', { enum: [...] })` (`src/db/schema.ts`) which gives a TS literal-union hint at the row level but does NOT enforce the enum at the database layer — SQLite has no native enum constraint, so a row inserted with `category: 'legume'` (e.g. via a future Convex sync layer that drifted from the constants list, a CSV import, or a hand-edited dev DB) would persist fine but crash three call sites at render time: (1) `app/food/[id].tsx:153` did `CATEGORY_CONFIG[food.category as FoodCategory]` then dereferenced `.color`/`.icon`/`.label` on lines 169/170/174/175 — `undefined.color` throws and the entire food detail screen unmounts to a red box, (2) `app/(tabs)/foods.tsx:104` did `CATEGORY_CONFIG[item.category as FoodCategory].icon` in the safe-foods chip row — same crash, takes down the whole Foods tab, (3) `src/components/FoodCard.tsx:23` did `CATEGORY_CONFIG[category]` — the prop was typed `FoodCategory` so the cast at the foods.tsx callsite "made it safe" at compile time but the runtime values flowed straight through. Same drift class as v0.5.74 (the `as ExposureStage` cast on the food detail page that hid a real schema-vs-union mismatch) and v0.5.61 (the `useState<any[]>` on the dashboard that lost the literal-union typing for `stage`). Adds a single `getCategoryConfig(value: string | null | undefined)` helper at the bottom of `src/lib/constants.ts` that returns `CATEGORY_CONFIG[value]` if `FOOD_CATEGORIES` includes the value, falling back to `CATEGORY_CONFIG.other` otherwise — `'other'` is the canonical "I don't know" bucket already in the enum (it's the default in `foodSchema` and the displayed label/icon "Other 🍽️" is a sensible visual fallback). Three coordinated callsites updated: `app/food/[id].tsx` (replaced the cast lookup + dropped the now-unused `FoodCategory` and `CATEGORY_CONFIG` imports), `app/(tabs)/foods.tsx` (replaced the safe-chip-row cast lookup), `src/components/FoodCard.tsx` (widened the `category` prop type from `FoodCategory` to `string` since the helper validates internally — this also lets the foods.tsx render-callback drop its `as FoodCategory` cast on the prop forwarding at line 76, removing the second type-hole in the same flow). The four other `CATEGORY_CONFIG[...]` lookups in the codebase (`app/food/add.tsx:115`, `app/(tabs)/progress.tsx:224`, `app/(tabs)/foods.tsx:163,166,168` filter chip row) iterate over `FOOD_CATEGORIES` directly so the index is provably safe and they're left untouched — the helper is used only at the DB-sourced boundaries where the runtime value is uncertain. +3 tests in a new `getCategoryConfig` describe block: every known category resolves to its exact CATEGORY_CONFIG entry (regression lock — a future contributor "simplifying" the helper to always return `CATEGORY_CONFIG.other` would fail the loop), unknown strings (`'legume'`, mixed-case `'PROTEIN'`, empty string) fall back to `.other`, and `null`/`undefined` fall back to `.other` (the function signature accepts both for caller convenience). Bumped `APP_VERSION` to v0.5.84. 373 tests pass across 24 suites (was 370, +3). TypeScript clean.
- v0.5.83 — Fix: trim optional text fields and treat whitespace-only as undefined. v0.5.75 added `.trim().min(1)` on every required string field so a parent submitting `"   "` got an inline "Name is required" error rather than a silent persisted whitespace row — but the four optional string fields (`childSchema.notes`, `foodSchema.defaultPreparation`, `exposureSchema.preparation`, `exposureSchema.notes` in `src/lib/validation.ts`) were left as bare `z.string().max(N).optional()` and silently accepted whitespace-only or surrounding-whitespace input. Concrete failure mode: a parent typing `"   "` into the Notes field on the Log Exposure form persisted `"   "` into the `exposures.notes` SQLite column, the v0.5.50 ExposureCard meta-row rendered a notes line of pure whitespace below the food name (with the `notes` style's `marginTop` still allocating layout space — so it looked like a phantom blank line below every "whitespace-noted" exposure), the v0.5.0 CSV export emitted `"   "` as the notes column for therapist review, and any future "has notes" boolean filter (the kind a therapist asking "show me only the exposures the parent commented on" would need) returned false positives. Same drift class as v0.5.75 closed for required fields and v0.5.78 closed for the email derivation path — schemas that "look optional" but admit semantically-empty strings instead of normalizing them to undefined. Adds a new `optionalTrimmedText(max, label)` helper at the top of `validation.ts` using `.transform().pipe()`: the input type stays `string | undefined` (so the form's `Controller` `field.value` type-checks against React Native's `TextInput.value: string | undefined` prop without casting — important because the `z.preprocess` form of the same idea would have produced `unknown` and broken three TextInput call sites with TS2769), the transform converts whitespace-only or empty strings to undefined and trims surrounding whitespace, and the piped inner schema runs the `.max(N)` validator on the post-trim output (so a parent typing `"   500-char-note   "` is judged on the 500 trimmed characters, not the 506 raw — matching what a sighted user would expect from the visible character count). The `.trim()` is applied inside the transform rather than via the built-in zod `.trim()` modifier so the empty-after-trim case can be coerced to undefined in the same pass — the built-in `.trim()` only mutates the string, doesn't conditionally drop it. Applied to four fields (no other optional string fields exist in any schema): `childSchema.notes` (500), `foodSchema.defaultPreparation` (50), `exposureSchema.preparation` (50), `exposureSchema.notes` (500). +10 tests in a new `whitespace handling on optional string fields` describe block at the end of `validation.test.ts` — three describe sub-blocks per field covering whitespace-only → undefined, empty string → undefined (where applicable), and surrounding whitespace stripped to trimmed value. The "empty string → undefined" cases pin the contract that the Controller's initial `defaultValues: { notes: '' }` flow doesn't accidentally persist the empty default as a literal empty string. Bumped `APP_VERSION` to v0.5.83. 370 tests pass across 24 suites (was 360, +10). TypeScript clean.
- v0.5.82 — Fix: tighten `exposureSchema.rating` to integer-only via `.int()`. The previous declaration `z.number().min(1).max(5).optional()` (`src/lib/validation.ts:37`) silently accepted decimals — `safeParse({ childId, foodId, stage: 'eat', rating: 3.7 }).success === true` before this commit, even though the rating field is an ordinal "Refused/Reluctant/Neutral/Willing/Enjoyed" 1–5 scale where 3.7 is meaningless. The capture surface (`src/components/RatingPicker.tsx`) only emits integers via the 5-segment chip row, so the form path was safe in practice — but the zod schema is the contract that any future surface (a CSV import for the v0.5.0 export round-trip, the eventual Convex sync layer, a JSON-RPC ingest) reads against, and "rating ≥ 1 ∧ rating ≤ 5 ∧ ¬integer" was an admit-decimals bug waiting to corrupt downstream rendering — `RATING_CONFIG[3.7]` is undefined, so an exposure with a decimal rating would crash any code that does `RATING_CONFIG[exp.rating]` at render time. Same drift class as v0.5.79 (validating that `dateOfBirth` is a real calendar date, not just a non-empty string) and v0.5.75 (treating `.min(1)` as a meaningful guard rather than just a length check) — schemas that pass the visible-label test but admit semantically-wrong values. One-line fix: `z.number().int('Rating must be a whole number').min(1).max(5).optional()`. The `.int()` is upstream of `.min/.max` so a decimal fails fast with the named message; the message is wired so an inline validation surface (none today, but the contract is in place) renders sensibly. +1 test in the existing `exposureSchema` describe block (`'rejects non-integer rating values'`) covers two non-integers (3.7, 1.5) plus a 5-value loop pinning 1/2/3/4/5 as still-accepted boundary integers — the loop is the regression lock that fails loudly if a future contributor "simplifying" the chain accidentally drops one of the boundaries. The empty-omit happy path (`rating` undefined) is already covered by the surrounding `.optional()` test. Bumped `APP_VERSION` to v0.5.82. 360 tests pass across 24 suites (was 359, +1). TypeScript clean.
- v0.5.81 — Fix: surface "Invite code must be 6 characters" inline error on the Join Family flow. `app/onboarding/join.tsx:23` previously checked `if (!trimmedCode || !trimmedName)` truthy-only — a parent who pasted a partial 4- or 5-character code (common on mobile when the partner's chat message wraps mid-word, or when the code is read aloud and miscounted) fell through to the `db.select().from(schema.families).where(eq(schema.families.inviteCode, trimmedCode))` lookup, the eq match silently failed, and the alert that surfaced was the misleading "No family found with that invite code" branch — directing the user to debug a sync issue or re-ask their partner for the code, when the actual problem is the local input length. The TextInput at line 95 caps at `maxLength={6}` so over-length is blocked client-side; only under-length slips through. The `joinFamilySchema.inviteCode` zod field at `src/lib/validation.ts:63` already enforces `.length(6, 'Invite code must be 6 characters')` but this screen bypasses the schema — same drift class as v0.5.76 (which closed the trim gap on the same handler because the schema-level fix didn't reach the form-bypassing flow). One coordinated guard added between the existing truthy check and the DB query: `if (trimmedCode.length !== 6) { Alert.alert('Invalid Code', 'Invite code must be 6 characters.'); return; }` — distinct alert title from "Missing Info" so a returning user who already cleared the truthy guard once doesn't see a stale identical-looking error. Tested empirically: typing "ABC1" → tapping Join → "Invalid Code, Invite code must be 6 characters" (was: "Not Found, No family found with that invite code"). No new automated tests — this is a screen-level Alert.alert handler that needs a render harness; matches the v0.5.76 ship-without-test pattern for the same screen. Bumped `APP_VERSION` to v0.5.81. 359 tests pass across 24 suites. TypeScript clean.
- v0.5.80 — Fix: Sign Out now fully resets app state. `auth-store.logout()` previously cleared `userId/familyId/email/displayName/isAuthenticated` but left `isOnboarded` set to `true` (`src/stores/auth-store.ts:55`), and the separate `child-store` kept `selectedChildId` pointing at the previous family's child. On a shared device, after a parent signed out and a different person signed in via the Join Family flow, the lingering `selectedChildId` in MMKV briefly flowed into the dashboard's first render until `ensureSelection` self-healed against the new family's children list — a small privacy bleed where exposure rows for the previous family's child could appear during the load window. The stale `isOnboarded=true` was also a soft state-machine smell: every callsite (`app/(tabs)/_layout.tsx:38`, `app/(tabs)/index.tsx:100,113`) gates on `(!isAuthenticated || !isOnboarded)` so the redirect still fired correctly when only `isAuthenticated` was cleared, but a future surface that gates on just `isOnboarded` (e.g. a "skip add-child if already onboarded" optimization) would silently misroute. Three coordinated changes: (1) added `clear: () => set({ selectedChildId: null })` to `child-store`'s state interface and persist body, (2) flipped `auth-store.logout()` to also reset `isOnboarded: false`, (3) wired the Settings tab's "Sign Out" alert action to call `clearChildSelection()` immediately before `logout()` — destructured the new action from `useChildStore` alongside the existing `selectedChildId`. Avoided the cleaner-looking cross-store import (auth-store calling `useChildStore.getState().clear()`) because keeping the two stores decoupled means one can be tested in isolation without the other being initialized. +3 tests: a new "resets isOnboarded so the onboarding flow re-runs after sign-out" case in the auth-store logout describe block, plus a new `clear` describe block on child-store covering both the populated-then-cleared and the no-op-when-empty paths. 359 tests pass across 24 suites (was 356, +3). TypeScript clean. Bumped `APP_VERSION` to v0.5.80.
- v0.5.79 — Fix: validate `childSchema.dateOfBirth` as a real YYYY-MM-DD calendar date and surface the inline error on both Add Child screens. The schema previously declared the field as `z.string().optional()` (`src/lib/validation.ts:5`) — no format check, no calendar-validity check — so a parent typing `12/05/2020` (US-style), `yesterday`, `2020-13-01` (invalid month), or `2020-02-30` (no such day) into the Date of Birth input on `app/child/add.tsx:121-134` or `app/onboarding/add-child.tsx:122-135` saw their submission succeed and persist a garbage string into `children.dateOfBirth`. The downstream age-display copy on the Settings tab and the Progress tab's "child is N months old" derivation (future feature, not yet wired but referenced in NEXT_STEPS.md) would have to swallow `new Date('yesterday')` → `Invalid Date` later — crash drift kicked into the future. The only signal the user got was the placeholder text `"YYYY-MM-DD"` — implication, not enforcement, the same gap class v0.5.75 closed for whitespace-only required names. Replaced the bare `z.string().optional()` with a `.trim().optional().refine(...)` chain that: (1) treats undefined and empty-after-trim as valid (the field is genuinely optional — most parents skip it on first add), (2) regex-checks `/^\d{4}-\d{2}-\d{2}$/` to reject US-style or free-form text, (3) constructs `new Date('${val}T00:00:00Z')` and round-trips back through `.toISOString().startsWith(val)` — the round-trip catches calendar rollovers that JS Date silently absorbs (`new Date('2020-02-30T00:00:00Z')` returns Mar 1 — the round-tripped ISO is `2020-03-01T00:00:00.000Z` which doesn't start with `2020-02-30`, so the refine fails). Same trick handles non-leap-year Feb 29 (`2021-02-29` rolls to Mar 1) and impossible day numbers like `2020-04-31` (rolls to May 1). Both Add Child screens now surface the inline error message via `{errors.dateOfBirth && <Text style={styles.error}>{errors.dateOfBirth.message}</Text>}` directly below the TextInput, mirroring the existing `errors.name` pattern that's already wired on those screens — same visual treatment, same error style, same `theme.colors.error` color, so a parent who fat-fingers the date sees a red message instead of a silent persisted garbage string. +12 tests in a new `childSchema.dateOfBirth format validation` describe block at the end of `validation.test.ts` covering: undefined/empty/whitespace-only as valid (the optional-field happy path); valid `2020-01-15` and surrounding-whitespace strip (`"  2020-01-15  "` → `"2020-01-15"`); rejection of `12/05/2020` (US format), `yesterday` (free text), `2020-13-01` (invalid month), `2020-02-30` (rollover), `2020-04-31` (rollover); leap-year edge cases (`2020-02-29` accepts, `2021-02-29` rejects). The leap-year tests are the load-bearing regression locks — a future contributor "simplifying" the round-trip back to a plain `!Number.isNaN(d.getTime())` would silently re-enable rollover, and the leap-year tests fail loudly. Bumped `APP_VERSION` to v0.5.79. 356 tests pass across 24 suites (was 344, +12 — the new `childSchema.dateOfBirth format validation` describe block). TypeScript clean.
- v0.5.78 — Fix: sanitize derived local-part email in `app/onboarding/join.tsx` against RFC-invalid characters and emoji-only inputs. The Join Family flow synthesizes an offline-only email at line 40 to use as a stable user record key — `${name.toLowerCase().replace(/\s/g, '')}@tonguetutor.app`. The previous regex only stripped whitespace, so a parent entering `Jon (Dad)!` produced `jon(dad)!@tonguetutor.app` (parentheses and `!` are technically allowed in RFC-3696 quoted local-parts but rejected by every common email parser including the project's own `loginSchema.email` validator), and an emoji-only displayName like `👶` produced `👶@tonguetutor.app` (Unicode local-parts require SMTPUTF8 negotiation that our future Convex sync layer won't speak). Both forms persist to `users.email` as the offline identity that the future cloud sync layer will key against — invalid local-parts make the row un-syncable. Same character-class drift v0.5.75/v0.5.76 fixed for the trim path; this commit closes the gap on the substitution path. Extracted a pure helper `deriveLocalEmailPart(displayName)` in `src/lib/utils.ts` that lowercases + strips everything not in the safe RFC subset `[a-z0-9.+-]` (preserves jane.doe+filter-2 unchanged), with an empty-string fallback to `'user'` so all-emoji or all-punctuation displayNames produce `user@tonguetutor.app` rather than a malformed `@tonguetutor.app` (which would fail any downstream email-shaped validator). Sync-friendly: an "Anne 👶" displayName now produces `anne@tonguetutor.app`, the same email that "Anne" alone would produce — so a parent who later removes the emoji decoration from their name doesn't get a duplicate user row at sync time. The helper is unit-tested with 7 tests covering the 4 character classes (alphanumeric pass-through, whitespace strip, RFC-punctuation strip, emoji strip), the safe-char preservation case (`.+-` + digits), and both fallback edges (empty input, all-invalid input). join.tsx imports the new helper alongside `generateId` and replaces the inline regex at line 40 with a single `deriveLocalEmailPart(trimmedName)` call. Bumped `APP_VERSION` to v0.5.78. 344 tests pass across 24 suites (was 337, +7). TypeScript clean.
- v0.5.77 — Fix: surface `temperature` in CSV export. `src/lib/export.ts` exported 10 columns (date, food, category, stage, rating, preparation, texture, meal, setting, notes) — silently dropping the `temperature` dimension that the Log Exposure form's chip row captures (added v0.4.x), the schema validates (`exposureSchema.temperature` enum hot/warm/room/cold), and the food detail page meta row surfaces (v0.5.50). The CSV is the only way to share data with a feeding therapist (per NEXT_STEPS.md P5) — a parent reporting "child accepts hot oatmeal but refuses cold cereal" had that distinction vanish from the therapist's view because the export never emitted the column. Same drift class as v0.5.50 (food detail page missing four optional dimensions on the meta row) and v0.5.67 (constants↔validation enum lock) — features land on the capture/persist path before the read path catches up. Four coordinated edits to `export.ts`: (1) `temperature: string | null` added to the `ExposureRow` interface between `texture` and `mealType` (semantic adjacency: both texture and temperature are physical-property dimensions), (2) `'temperature'` inserted at index 7 of the `HEADER` constant (between `'texture'` and `'meal'`), (3) `csvEscape(r.temperature)` added to the `formatExposuresCsv` row mapper at the matching position, (4) `temperature: schema.exposures.temperature` added to the drizzle `select` projection in `fetchExportRows`. Test fixture in `export.test.ts` widened to include `temperature: 'cold'` on the default row + `temperature: null` on the optional-fields-empty case + a new "serializes temperature alongside texture" assertion that pins the column-7-vs-column-6 ordering so a future contributor reordering the HEADER without updating the mapper gets a failing test (regression lock against silent reorder drift). Three integration test fixtures in `export.integration.test.ts` widened to include the field (Date-passthrough, numeric-timestamp coerce, exportChildData happy path), and the header-only assertion in the empty-export test updated to the new 11-column header. The CSV column count change from 10 → 11 is technically a breaking change for any therapist with a parser pinned to 10 cols, but the offline single-user app has no shipped therapist tooling depending on a frozen header (and the columns are name-keyed, not positional, in any reasonable Excel/Sheets parser). Bumped `APP_VERSION` to v0.5.77. 337 tests pass across 24 suites (was 336, +1). TypeScript clean.
- v0.5.76 — Fix: trim user input in `app/onboarding/join.tsx` before lookup, validation, and storage. The Join Family flow is the second on-ramp into the app (used by the partner who joins via invite code), but unlike the form-based screens it does not route through a `useForm`/zod schema — it uses ad-hoc `if (!inviteCode || !displayName)` validation at line 20. Two failure modes the v0.5.75 schema-level trim cleanup did not address because this screen bypasses validation: (1) `inviteCode.toUpperCase()` at line 29 doesn't strip whitespace — pasting `"  ABC123  "` from a partner's chat message (or `" ABC12"` with a stray leading space, which the `maxLength={6}` cap allows) uppercases to the same surrounding-whitespace string, and the `families.inviteCode` lookup never matches; the user sees the misleading "No family found with that invite code" alert when the code is correct and only the whitespace was wrong. (2) `displayName` is stored verbatim at line 42/50 — entering `" Anne "` persists `" Anne "` into the users table while the derived email at line 41 strips spaces (`anne@tonguetutor.app`); the displayed name in the Settings tab and exposure logs would render with the surrounding spaces forever. Three coordinated trims in `handleJoin`: pull `trimmedCode = inviteCode.trim().toUpperCase()` and `trimmedName = displayName.trim()` at the top of the handler; route the truthiness guard, the `eq(families.inviteCode, ...)` query, the `users.insert` and the `login()` call through both trimmed values; lift the email derivation into a single `email = ...trimmedName.toLowerCase().replace(/\\s/g, '')...` local so the same string flows into both the insert and the login (the prior code computed it twice from the un-trimmed `displayName`, a small drift surface where any future tweak to one would silently miss the other). Also dropped the now-stale `// Look up family by invite code in local DB` comment — the code reads cleanly without it. Same drift class as v0.5.75 (whitespace-only schema names) but for the one screen that intentionally bypasses zod because the schema (joinFamilySchema) requires email+password fields the local-only flow doesn't collect. Pure UX fix; no test changes — this is a screen-level handler that needs a render harness, and the change is targeted to a documented failure mode (the misleading "No family found" alert listed at NEXT_STEPS.md's "Failure Modes" table). Bumped `APP_VERSION` to v0.5.76. 336 tests pass across 24 suites; TypeScript clean.
- v0.5.75 — Fix: reject whitespace-only required names across all validation schemas. `src/lib/validation.ts` had `z.string().min(1, 'Name is required')` on six required-string fields (childSchema.name, foodSchema.name, registerSchema.displayName, registerSchema.familyName, joinFamilySchema.displayName, joinFamilySchema.inviteCode) — `.min(1)` only checks length, so a parent submitting "   " (whitespace-only) passed validation and would persist a child / food / user with a blank-looking name that displayed as empty space everywhere it rendered. Verified empirically: `childSchema.safeParse({ name: '   ' }).success === true` before this commit. Same drift class as not having a `.email()` after `.min(1)` on email fields — the visible label "Name is required" implies "non-empty meaningful text", but the schema only enforced "≥1 character". Added `.trim()` before `.min(1)`/`.length(6)` on the six fields. Two effects: (1) whitespace-only inputs are now rejected (the count zero after trim fails the .min(1)/.length(6) check), and (2) leading/trailing whitespace on otherwise-valid inputs is auto-stripped — `" Emma "` parses to `"Emma"`, so the persisted row is the user-intended trimmed value, not a verbatim "Emma " with a trailing space that creates display drift later. Also tightened `joinFamilySchema.inviteCode` so a paste like `"  ABC123  "` (common on mobile when copying from a chat message) trims to `"ABC123"` and the `families.inviteCode` lookup at `app/onboarding/join.tsx:29` succeeds — previously the surrounding whitespace would silently fail the eq match and surface the misleading "No family found" alert. +9 tests in a new `whitespace handling on required string fields` describe block at the end of `validation.test.ts` covering both rejection (whitespace-only) and stripping (surrounding whitespace) for each of the four schemas; the joinFamilySchema gets two extra cases for the inviteCode trim-then-length flow. Password fields are intentionally not trimmed — some users include leading/trailing space in passwords (unusual but legal). Bumped `APP_VERSION` to v0.5.75. 336 tests pass across 24 suites (was 327, +9). TypeScript clean.
- v0.5.74 — Refactor: tighten `exposuresList` typing on the food detail page and drop the `as ExposureStage` cast at `app/food/[id].tsx:252`. The state was declared `useState<any[]>` (line 22), so the projected joined-row shape (id/stage/rating/notes/occurredAt/mealType/temperature/texture/setting) was lost the moment it left drizzle's query builder — anywhere downstream that destructured `exp.fooBar` would compile against `any` instead of the actual schema, masking real refactor breakage. The cast at line 252 (`stage={exp.stage as ExposureStage}`) was a workaround for the same upstream `any[]`, not a real schema-vs-union mismatch: drizzle's `text('stage', { enum: [...] })` declaration at `src/db/schema.ts:48` already gives the column an `'tolerate' | 'interact' | 'smell' | 'touch' | 'taste' | 'eat'` literal-union type at the row level. Same drift-by-construction class as v0.5.61 (the dashboard's `recentExposures` typing fix) — both pages held the same eight projection fields with no shared type. Defined an explicit `ExposureRow` alias via `Pick<typeof schema.exposures.$inferSelect, ...>` listing the nine selected columns; switched the state to `useState<ExposureRow[]>([])`; dropped the `as ExposureStage` cast. Both holes are gone in one move — the food detail page now compiles cleanly end-to-end and a future contributor renaming a projection field gets a compile error rather than a silent runtime drift. The remaining `ExposureStage` import is still used by the `useState<ExposureStage | null>(null)` at line 28 for `highestStage`. No test changes — type-only refactor with no runtime behavior change. 327 tests pass across 24 suites; `npx tsc --noEmit` clean. Bumped `APP_VERSION` to v0.5.74.
- v0.5.73 — Refactor: delete the two dead barrel files `src/stores/index.ts` and `src/providers/index.ts`. Both re-exported their sibling modules (3 stores; 3 providers respectively) but a grep across `app/` and `src/` (excluding `__tests__/`) confirmed zero imports of either barrel — every store callsite uses `@/src/stores/<store-name>` directly (e.g. `app/(tabs)/settings.tsx:7-9` imports `useSettingsStore`, `useAuthStore`, `useChildStore` via three separate paths) and `app/_layout.tsx:6-8` imports `ConvexProvider`, `QueryProvider`, `DatabaseProvider` via three separate file paths. Same dead-public-export pattern as v0.5.69 (posthog re-export), v0.5.63 (3 dead utils helpers), v0.5.72 (just deleted `getStartOfWeek`). The third barrel `src/components/index.ts` was checked and is heavily used (10+ import sites in `app/`) — kept untouched. Why these two-line files matter: a future contributor opening `src/stores/` or `src/providers/` and seeing `index.ts` reasonably assumes the barrel is the canonical entry point and rewires imports through it, only to discover later that the convention in this codebase is direct-file imports — small drag, but cumulative across every contributor reading the directory cold. Pure deletion: 7 lines total across the two files. Reversible — if a future refactor wants to centralize the import surface, recreating either barrel is a 4-line write. Bumped `APP_VERSION` to v0.5.73. 327 tests pass across 24 suites; `npx tsc --noEmit` clean.
- v0.5.72 — Refactor: drop dead `getStartOfWeek` helper from `src/lib/utils.ts`. The function was exported (line 37) and exercised by 6 unit tests but a grep across `app/` and `src/` (excluding `__tests__/`) confirmed zero non-test references. The progress tab's weekly window in `progress-stats.ts:87-91` uses an inline rolling 7-day diff (`weekAgo = now - WEEK_MS`), not a Sunday-rooted calendar week — different semantics, never converged. The v0.5.66 changelog incorrectly claimed `progress.tsx` consumed this helper; that claim was wrong (verified by grep), so the v0.5.66 regression-lock tests it added were guarding a function with no production caller. Same dead-public-export pattern as v0.5.69 (posthog re-export) and v0.5.63 (the `getStageIndex`/`isStageHigher`/`getExposureProgress` trio) — features that exist in the API surface only because the test suite tested them into existence. Pure deletion: the 7-line function declaration plus the 6-test `describe('getStartOfWeek', ...)` block plus the import line. Reversible — when a real Sunday-rooted-week feature lands, the helper takes 10 seconds to recreate. The remaining utils.ts surface (`generateId`, `generateInviteCode`, `formatDate`, `formatRelativeDate`, `getStartOfDay`) stays — three are used by app code, `formatDate` is referenced by `formatRelativeDate`, and `getStartOfDay` is consumed by `formatRelativeDate` for the calendar-day delta v0.5.49 introduced. Bumped `APP_VERSION` to v0.5.72. 327 tests pass across 24 suites (was 333, -6 as the dead-test block was removed). TypeScript clean.
- v0.5.71 — Tests: cover the `removeItem` catch blocks in all three zustand+MMKV stores. The `mmkvStorage` adapter wired into `auth-store.ts:23-29`, `child-store.ts:23-29`, and `settings-store.ts:24-30` carries identical try/catch resilience on `getItem`/`setItem`/`removeItem` (added v0.5.56 to harden against the documented "MMKV storage corrupted across upgrade" failure mode), but `mmkv-resilience.test.ts` only exercised getItem/setItem failures — the `removeItem` catch was uncovered in all three files (lines 27-28 each), keeping the stores at 95-96% coverage instead of 100%. The removeItem path is exercised in production by zustand-persist's `clearStorage()` API (used implicitly by version migrations and explicitly during sign-out flows that wipe persisted state). A future contributor refactoring the adapter to drop the try/catch on the assumption "we never call clearStorage anyway" would have no failing test to flag the regression. Three new tests in the existing `MMKV adapter resilience` describe block, mirroring the established "loads with defaults / does not crash" pattern: each calls `useStore.persist.clearStorage()` against the throwing-MMKV mock and asserts (1) no throw escapes, (2) console.error fires with the store-prefixed `'<store-name> MMKV removeItem failed:'` message. Pure regression-lock tests; no production code touched. All three store files are now at 100% statements/branches/functions/lines coverage. Bumped `APP_VERSION` to v0.5.71. 333 tests pass across 24 suites (was 330, +3). TypeScript clean.
- v0.5.70 — Tests: lock cross-record alignment in `src/lib/thresholds.ts`. The module exposes three parallel records keyed by the same `FeedingProfile` literal-union (typical/picky/arfid): `FEEDING_PROFILES` (the const tuple of valid keys), `EXPOSURE_THRESHOLDS` (Record\<FeedingProfile, number\>), and `FEEDING_PROFILE_CONFIG` (Record\<FeedingProfile, { label, description }\>). TypeScript's `Record<>` mapping enforces key-set alignment at compile time — but the human-readable `description` copy ("8–15 exposures to acceptance" / "15–20…" / "20–30+…") is a literal string that contains the threshold value, and there was no test asserting that the two stay synchronized. A future contributor relaxing the typical-profile threshold from `15` to `12` (e.g. after re-reading the SOS literature) would silently leave the description reading "8–15 exposures to acceptance", contradicting the actual behavior on the Settings tab's Feeding Profile description block at `app/(tabs)/settings.tsx:147` which renders this exact string. Same drift-by-construction class as v0.5.67's constants↔validation alignment lock — both are static records that look type-safe but ship a free-form string that has to track a numeric value. Three new tests in a `cross-record alignment` describe block: (1) `Object.keys(EXPOSURE_THRESHOLDS).sort()` equals `[...FEEDING_PROFILES].sort()` (defensive — TS already enforces this but a contributor using `as any` to add an entry would slip through); (2) `Object.keys(FEEDING_PROFILE_CONFIG).sort()` equals `[...FEEDING_PROFILES].sort()` plus per-key non-empty label/description string checks; (3) for each profile, `FEEDING_PROFILE_CONFIG[profile].description.includes(String(EXPOSURE_THRESHOLDS[profile]))` — the substring check is liberal (allows "8–15", "8 – 15", "8 to 15", or any other phrasing) but catches the silent drift where the threshold number disappears from the copy entirely. Pure regression-lock tests; no production code touched. Bumped `APP_VERSION` to v0.5.70. 330 tests pass across 24 suites (was 327, +3). TypeScript clean.
- v0.5.69 — Refactor: drop the dead `export { posthog }` re-export from `src/lib/posthog.ts`. The module exposes a singleton `let posthog: PostHog | null = null` plus three functions (`initPostHog`, `captureEvent`, `identifyUser`) — the three functions are the entire public API and they all reference the singleton via the closure-scoped `posthog?.capture(...)` / `posthog?.identify(...)` null-guard pattern, so any caller wanting to send events should funnel through `captureEvent` / `identifyUser` rather than reaching at the singleton directly. The `export { posthog }` at line 24 (the only consumer-facing exit for the singleton) was a leftover from an earlier shape where the helper functions didn't exist; once the functions were added the re-export became dead code. A grep across `app/` and `src/` (excluding `__tests__/`) confirmed zero non-test references — `app/_layout.tsx:10` imports only `initPostHog`, and the test file at `src/lib/__tests__/posthog.test.ts` accesses `mod.initPostHog` / `mod.captureEvent` / `mod.identifyUser` via `jest.isolateModulesAsync` but never `mod.posthog`. Same dead-public-export pattern v0.5.62 cleaned up for `ExposureCard.loggedByName` and v0.5.63 for the three orphaned `utils.ts` helpers — features that exist in the API surface only because the test suite never asserted them away. Pure deletion — no behavior change for any caller, but the next contributor reading the module's exit surface won't pick up the singleton directly and start writing `posthog?.capture(...)` calls that bypass the null-guarded helper functions (which would silently misroute events when `initPostHog` hasn't fired because `EXPO_PUBLIC_POSTHOG_API_KEY` was missing). The 6 existing posthog tests are unchanged and still pass — they exercise the helper functions, not the re-export. Bumped `APP_VERSION` to v0.5.69. 327 tests pass across 24 suites; TypeScript clean.
- v0.5.68 — Fix: announce the Settings → Data → Export Data CSV button to assistive tech. `app/(tabs)/settings.tsx:158-167` already carried `accessibilityRole="button"` + `accessibilityState={{ disabled: exporting, busy: exporting }}` (so VoiceOver/TalkBack would announce "dimmed, busy" while the share-sheet wait is in flight) but had no `accessibilityLabel` — VoiceOver would read just the visible label-and-value pair "Export Data (CSV), Share" with no role anchor, and once the export is in flight the value flips to "Exporting…" so a SR user mid-tap heard a different reading than at idle. Same gap class as v0.5.21/v0.5.42/v0.5.46 (the v0.5.x a11y polish train) — every other interactive surface in the Settings tab (Add Child row, Theme chips, Feeding Profile chips, Sign Out button) carries an explicit context-rich label after that work, but the Export Data row was the last one missing. One-line fix following the established pattern: `accessibilityLabel="Export data as CSV"`. The label is static so it remains stable across the idle/busy/disabled state transitions, while the existing `accessibilityState` continues to surface the busy hint for in-flight feedback. Bumped `APP_VERSION` to v0.5.68. 327 tests pass across 24 suites; TypeScript clean.
- v0.5.67 — Tests: lock `constants.ts` enums against `validation.ts` schema drift, plus add the missing `SETTINGS` test. Two coverage gaps in one commit. (1) The existing `enum arrays` describe block in `src/lib/__tests__/constants.test.ts` covered `MEAL_TYPES`, `TEMPERATURES`, `TEXTURES`, and `PREPARATIONS` but the `SETTINGS` constant (4 options: home/school/restaurant/therapy, added v0.5.41 when the Setting chip row was wired into the Log Exposure form) was never imported or asserted — a contributor adding `'park'` would have no failing test even though the change has to land in two files (the constant + the zod enum at `validation.ts:30`). Added a length + membership test mirroring the `MEAL_TYPES` shape. (2) The deeper gap: the form chip rows on `app/(tabs)/log.tsx` and `app/food/add.tsx` iterate over the *constants*, but the form's `handleSubmit` callback validates against `exposureSchema`/`foodSchema` zod enums in `validation.ts`. If the two files drift (e.g. add `'park'` to `SETTINGS` but forget the zod enum, or rename `'restaurant'` → `'eatery'` in only one file), a chip renders fine but the schema silently rejects the submission with the inline "Please select a valid option" zod error — a UX-clarity bug that's invisible at typecheck time because both sides type as `string[]`. Added a new `constants align with validation schema enums` describe block with 6 cross-validation tests pairing each constant with its zod field via `schema.shape[key].unwrap().options` (the `.unwrap()` handles the `.optional()` wrapper on temperature/texture/mealType/setting; stage and category are required so `.unwrap()` is a no-op via the `typeof field.unwrap === 'function'` guard). Sorted equality compares both directions in one assertion — the constant's render-order is preserved on the rendering side; the test only enforces set-equality so a contributor reordering chips for UX reasons doesn't trip the lock. The 6 covered fields: `MEAL_TYPES↔mealType`, `TEMPERATURES↔temperature`, `TEXTURES↔texture`, `SETTINGS↔setting`, `STAGE_ORDER↔stage`, `FOOD_CATEGORIES↔category`. The 7th constant (`PREPARATIONS`) is intentionally not paired — the schema's `preparation` field is `z.string().max(50)`, free-form text not an enum, so the chip row is a suggestion-list rather than a closed-set. Pure regression-lock tests; no production code touched. Bumped `APP_VERSION` to v0.5.67. 327 tests pass across 24 suites (was 320, +7). TypeScript clean.
- v0.5.66 — Tests: close two `getStartOfWeek` coverage gaps in `src/lib/__tests__/utils.test.ts`. The sibling `getStartOfDay` already pinned a "does not mutate the original date" invariant (line 134-138) but the parallel `getStartOfWeek` describe block was missing the same defensive test — and missing a month-rollover boundary check entirely. Both helpers compute via `const d = new Date(date)` (a copy) before calling mutating setters, so neither *should* mutate the input — but only `getStartOfDay` had a regression lock. A future contributor refactoring `getStartOfWeek` to drop the copy (e.g., "the input was just used for `getDay()`, why allocate?") would silently break callers that pass in a `Date` they want to keep — `app/(tabs)/progress.tsx` uses `getStartOfWeek` to compute the trailing-7-day window and re-uses the `now` reference downstream. Two added tests: (1) parity with `getStartOfDay`'s mutation test — pass `new Date(2026, 2, 26, 14, 30)` and assert the input still reads `getDate()===26 / getHours()===14 / getMinutes()===30` after the call. (2) Month-rollover boundary — `new Date(2026, 0, 1)` is a Thursday, so `setDate(1 - 4) = setDate(-3)` must roll backwards into the previous month and land on Sun Dec 28, 2025. The existing tests only used dates inside March 2026 where the within-month subtraction works trivially (Mar 26 → Mar 22, Mar 22 → Mar 22), so the JavaScript Date `setDate(negative)` rollover behavior was never exercised. Both behaviors are correct in the current implementation; the tests are pure regression locks. No production code touched. 24 utils tests pass (was 22, +2). Bumped `APP_VERSION` to v0.5.66.
- v0.5.65 — Chore: harmonize `app/_layout.tsx` imports to the `@/src/` alias the rest of the app uses. The five provider/lib imports plus the unistyles theme side-effect import (lines 6-13) were the only `../src/` relative paths left in the entire `app/` tree — every other route file (`app/child/add.tsx`, `app/onboarding/*`, `app/food/*`, all five `(tabs)/*` screens) routes through the `@/*` baseUrl alias declared at `tsconfig.json:7`. The drift dated back to the project's original RootLayout scaffold and survived every subsequent refactor because no compile error ever surfaced — both forms resolve to the same module under the existing tsconfig paths. But the inconsistency is a small drag on every contributor reading or copy-pasting a provider import: the question "is the convention `../src/` or `@/src/`?" has no answer in this file alone, only by surveying siblings. Six-line mechanical change: `'../src/providers/ConvexProvider'` → `'@/src/providers/ConvexProvider'` and the four matching siblings; the bare `'../src/styles/theme'` side-effect import gets the same treatment. Also dropped the now-obvious `// Import Unistyles theme registration (side effect)` comment — the `@/src/styles/theme` path is self-explanatory in context (the only top-level non-named import in the file). Pure consistency change; no behavior or build-output difference. 318 tests pass across 24 suites; `npx tsc --noEmit` clean. Bumped `APP_VERSION` to v0.5.65.
- v0.5.64 — Chore: prune 12 dead imports/destructures across 7 files surfaced by `npx tsc --noEmit --noUnusedLocals`. The TS strict-unused flag is not part of the day-to-day typecheck so these had piled up across recent refactors with no compile-time signal — each one is a load-bearing line that does nothing, costing the next contributor a moment of "wait, why is X imported here?" every time. Cleanup batch by file: (1) `app/(tabs)/foods.tsx:7` — drop `and` from `drizzle-orm` import (the v0.5.53 alphabetize-foods commit added `asc` but the `and` from an earlier multi-condition WHERE was no longer used after the query simplified to a single `eq`). (2) `app/(tabs)/index.tsx:9,12,13` — three dead imports on the dashboard: `StageIndicator` (component is mounted on `app/food/[id].tsx` but the dashboard's stage distribution uses a custom inline grid, not the indicator), `TARGET_EXPOSURES` (referenced only by `app/food/[id].tsx`'s ProgressBar — the dashboard's own ProgressBar usage was removed in an earlier refactor), and `formatRelativeDate` from utils (replaced by ExposureCard's internal call to the same helper — the dashboard renders dates via `<ExposureCard occurredAt={...}/>`, not by hand). (3) `app/(tabs)/log.tsx:1,17` — `useCallback` import (the v0.5.12 `useState`-removal commit eliminated all three callbacks the form previously memoized) and `STAGE_ORDER` (the v0.5.27 a11y commit moved interactive-segment iteration into StageIndicator itself; the form just consumes it). (4) `app/(tabs)/settings.tsx:18` — `familyId` destructure from `useAuthStore` (no callsite in the file, leftover from a removed Family-card line). (5) `app/onboarding/index.tsx:5,13` — `useChildStore` import (the screen's first-launch path doesn't touch child state — that happens in the `add-child` follow-on screen) plus `setOnboarded` and `setFamilyId` destructures from `useAuthStore` (only `login` is called; the other two were destructured for an "auto-onboard" path that was simplified out). (6) `src/components/ChildSelector.tsx:1` — `View` from react-native (the component is purely a horizontal `ScrollView` of `Pressable`s with `Text` content; the outer `View` was dropped when the layout simplified). (7) `src/providers/DatabaseProvider.tsx:3` — `db` from `../db/client` (the migration runner uses the lower-level `expoDb.execAsync` directly; the drizzle-wrapped `db` is for query callsites elsewhere in the app). All seven files compile cleanly under `--noUnusedLocals` after the cleanup. Pure deletion — no behavior change anywhere — but the next contributor scanning a file's imports won't waste a beat asking "where is X used?" Bumped `APP_VERSION` to v0.5.64. 318 tests pass across 24 suites; `npx tsc --noEmit` and `npx tsc --noEmit --noUnusedLocals` both clean.
- v0.5.63 — Refactor: drop three dead helpers from `src/lib/utils.ts` — `getStageIndex(stage)`, `isStageHigher(a, b)`, and `getExposureProgress(count, target)`. All three were exported with full unit-test coverage (18 tests across 3 describe blocks) but a grep across `app/` and `src/` (excluding `__tests__/`) confirmed zero non-test references. The replacements live in canonical helper files: `getExposureProgress` was superseded by `calcExposureProgress` in `src/lib/thresholds.ts` (introduced v0.4.0 — same `current/threshold/pct/reached` shape but profile-aware), and stage-comparison logic lives in `src/lib/stage.ts` via `getNextStage`/`canBumpStage`/`getHighestStage` (extracted v0.5.4 from the inline food-detail loop). The utils.ts versions were left behind during those refactors as fossil exports — tested into existence but no production code path producing or consuming them. Same dead-code-with-tests pattern as v0.5.62 (loggedByName prop). Three coordinated removals: (1) the three function declarations + the `STAGE_ORDER`/`ExposureStage` import they pulled in (now no other line in utils.ts uses them, so the import goes too), (2) three describe blocks from `utils.test.ts` matching the deleted exports, (3) the four-symbol import list at the top of the test file. The remaining utils.ts surface (`generateId`, `generateInviteCode`, `formatDate`, `formatRelativeDate`, `getStartOfDay`, `getStartOfWeek`) is fully used by app code and stays unchanged. Pure deletion — no behavior change for any caller, but the next contributor reading utils.ts won't pick up the stale `getExposureProgress(7, 15)` helper instead of the profile-aware `calcExposureProgress(7, profile)` that the rest of the app actually uses, which would have silently broken the picky/ARFID threshold contract on any new surface that picked the wrong helper. 22 utils tests pass (was 40, -18). Bumped `APP_VERSION` to v0.5.63. TypeScript clean.
- v0.5.62 — Refactor: drop the dead `loggedByName` prop from `ExposureCard`. `src/components/ExposureCard.tsx:13,35,83-85` declared an optional `loggedByName?: string` prop and rendered a "Logged by {name}" `<Text style={styles.loggedBy}>` when truthy — but a grep across the two mount sites (`app/(tabs)/index.tsx:223-232` dashboard recently-logged list and `app/food/[id].tsx:244-254` per-food exposure history) confirmed neither caller ever passes the prop. The render branch was reachable only from the test file (`src/components/__tests__/ExposureCard.test.tsx:24-27` "renders logged by name when provided"), so the prop existed solely to be tested into existence — no production code path produced or consumed the field. Same dead-code-with-tests-attached pattern v0.5.51 (Invite Spouse row) and v0.5.59 (Quick Log Mode + Notifications toggles) cleaned up — features that look real because the test suite covers them, but ship as zero-behavior surfaces. Three coordinated removals: (1) prop declaration on the `ExposureCardProps` interface, (2) destructured param + conditional `<Text>...{loggedByName}</Text>` block, (3) the now-orphaned `loggedBy` style entry in the StyleSheet (both the Text node and the style live and die together). The matching `loggedByName="Mom"` test case is removed; the other 14 ExposureCard tests are unchanged and still pin the contract for every other prop branch (foodName, childName, notes, mealType/temperature/texture/setting meta-row, rating emoji, stage variants, a11y label gating, onPress wiring). Pure deletion — no behavior change for any existing call site, but the next contributor reading ExposureCard's interface won't waste cycles wondering when `loggedByName` is supposed to flow through (it never was). Bumped `APP_VERSION` to v0.5.62. 14 ExposureCard tests pass; full suite unchanged.
- v0.5.61 — Refactor: tighten dashboard `recentExposures` typing and drop the `stage as any` cast at `app/(tabs)/index.tsx:216`. Two coupled type holes in the dashboard's recently-logged list: (1) `useState<any[]>([])` at line 21 — the projected joined-row shape (id/foodId/foodName/childName/stage/rating/notes/occurredAt) was lost the moment it left drizzle's query builder, so anywhere downstream that destructured `exp.fooBar` would compile against `any` instead of the actual schema, masking real refactor breakage; (2) `stage={exp.stage as any}` at line 216, threaded into the `<ExposureCard>` whose `stage` prop is the strict `ExposureStage` literal union — the cast was a workaround for the `useState<any[]>` upstream, not a real schema vs union mismatch. Drizzle's `text('stage', { enum: [...] })` declaration at `src/db/schema.ts:48` already gives the column an `'tolerate' | 'interact' | 'smell' | 'touch' | 'taste' | 'eat'` literal-union type at the row level (verified empirically: `const y: typeof schema.exposures.$inferSelect.stage = 'invalid_stage'` fails tsc with `"invalid_stage" is not assignable to "tolerate" | "interact" | "smell" | "touch" | "taste" | "eat"`). Defined an explicit `RecentExposure` type at the module level matching the projection's eight fields exactly, with `stage: typeof schema.exposures.$inferSelect.stage` so the literal union flows through; switched the state to `useState<RecentExposure[]>([])`; dropped the `as any` on the `stage` prop. Both holes are gone in one move — dashboard now compiles cleanly end-to-end and a future contributor renaming a projection field gets a compile error rather than a silent runtime drift. The remaining `as any` casts in this file (`router.push(\`/food/${exp.foodId}\` as any)` at line 220) are expo-router typed-routes workarounds — different problem class, not in scope. No test changes — type-only refactor with no runtime behavior change. 328 tests pass across 24 suites; TypeScript clean. Bumped `APP_VERSION` to v0.5.61.
- v0.5.60 — Refactor: drop the four leftover `handleSubmit(onSubmit as any)` casts that v0.5.58 missed. v0.5.58 fixed the `zodResolver(...) as any` half of the form-typing problem by wiring the form to the 3-generic `useForm<z.input<typeof schema>, unknown, FormData>` overload — but the four form submit Buttons still passed `handleSubmit(onSubmit as any)` (`app/food/add.tsx:181`, `app/child/add.tsx:158`, `app/(tabs)/log.tsx:366`, `app/onboarding/add-child.tsx:163`) because the cast had been a load-bearing workaround for the same input/output mismatch on the resolver side and was never re-evaluated once the resolver was fixed. Now that the form is correctly typed end-to-end (input shape on the form, output shape on the submitter callback), the `onSubmit: (data: XFormData) => Promise<void>` callback signature exactly matches what `handleSubmit` expects — the cast is a no-op type hole that suppresses a real signature mismatch error nowhere because there is none. Mechanical 4-line change: `onSubmit as any` → `onSubmit` at every site. Same drift-by-construction risk class the v0.5.58 commit message called out: a future contributor adding a `.transform()` to one of the four schemas would have its return type quietly diverge from `XFormData` with no compile-time signal, because the `as any` would silently absorb the new mismatch. With this commit, all four submit paths are fully type-checked end-to-end. No test changes — the existing 328 tests across 24 suites pass unchanged, and `npx tsc --noEmit` is clean. Bumped `APP_VERSION` to v0.5.60.
- v0.5.59 — Fix: remove dead Notifications + Quick Log Mode toggles from Settings → Preferences. `app/(tabs)/settings.tsx:103-121` rendered two `<Switch>` controls — Quick Log Mode and Notifications — that the user could flip but neither one was wired to anything: a grep across `app/` confirmed `quickLogMode` and `notificationsEnabled` were referenced only in the Settings tab itself and the `settings-store.ts` that backed them. No `expo-notifications` package is installed (verified via `package.json`); no log screen reads `quickLogMode` to decide whether to short-circuit the optional-details block. Same UX-clarity bug class as v0.5.51 (the "Invite Spouse" Family-card row removal): two interactive surfaces that were misleading by construction — visually identical to the working Theme/Feeding Profile chip rows above and below them, but with no behavior. Removed both rows + their flanking dividers from the Settings UI; pruned `quickLogMode`, `notificationsEnabled`, `setQuickLogMode`, `setNotificationsEnabled` from the `SettingsState` interface and the persist-middleware initial state in `src/stores/settings-store.ts`; removed the matching `setQuickLogMode` / `setNotificationsEnabled` describe blocks from `src/stores/__tests__/settings-store.test.ts` (the field-presence assertions in the "initial state" test also dropped to `theme` + `feedingProfile`). The persisted MMKV blob carries the two stale fields for any user upgrading from v0.5.58 — zustand-persist tolerates extra keys (they're ignored on rehydration), so no migration is needed; the next write of the settings blob will silently drop them. The Switch import on settings.tsx was removed since no other surface used it. Bumped `APP_VERSION` to v0.5.59. 328 tests pass across 24 suites (was 330, -2 — the two removed setter tests). TypeScript clean.
- v0.5.58 — Refactor: drop `as any` cast on every `zodResolver(...)` call site and route the form's input/output split through react-hook-form's 3-generic `useForm<TInput, TContext, TOutput>` overload. The four form screens — `app/food/add.tsx:22`, `app/child/add.tsx:25`, `app/onboarding/add-child.tsx:26`, `app/(tabs)/log.tsx:28` — all carried `resolver: zodResolver(<schema>) as any` because the form was typed `useForm<XFormData>` where `XFormData = z.infer<schema>` is the *output* type (post-default-applied: `isSafeFood: boolean`, `avatarEmoji: string`), but the `Resolver` type the prop expects is keyed on the *input* type (pre-default: `isSafeFood?: boolean | undefined`, `avatarEmoji?: string | undefined`). The two are deliberately different in zod — the `.default()` annotation is the contract that bridges them — so the `as any` cast was a known-incorrect type hole that silently swallowed a real input/output mismatch error in every form. A future contributor adding a `.transform()` or `.refine().default()` to any of the four schemas would have its return type quietly diverge from `XFormData`'s assumed shape, with no compile-time signal. Fix is mechanical and uniform across all four files: import `z` from `zod`, switch `useForm<XFormData>({...})` to `useForm<z.input<typeof xSchema>, unknown, XFormData>({...})`. The first generic is the form input shape (matches what `defaultValues` and `Controller`'s `field.value` see), `unknown` is the unused context generic, and `XFormData` (= `z.output<...>`) is the parsed shape `handleSubmit((data) => ...)` receives — so the `onSubmit(data: XFormData)` callbacks at every site keep their fully-narrowed type without any cast. The `as any` is gone everywhere; runtime behavior is identical (same resolver, same defaults, same parse/validate path) but the type system now correctly enforces the input/output split. No test changes needed — the existing 330 tests across 24 suites pass unchanged, and `npx tsc --noEmit` is clean. Bumped `APP_VERSION` to v0.5.58.
- v0.5.57 — Tests: close max-length and enum-rejection coverage gaps in `src/lib/__tests__/validation.test.ts`. The original suite covered required-field rejection, enum acceptance, and a handful of enum-rejection paths but skipped every `.max(N)` validator boundary (childSchema name 50, childSchema notes 500, foodSchema name 80, foodSchema defaultPreparation 50, exposureSchema preparation 50, exposureSchema notes 500, loginSchema email 254, loginSchema password 128, registerSchema displayName 50, registerSchema familyName 50) and three enum rejections (exposureSchema mealType/setting, joinFamilySchema email/password). A future contributor relaxing one of those caps — e.g. dropping `.max(500)` on `exposureSchema.notes` — would silently break the 500-char clamp the form copy implies, with no test failure to flag the regression. Added 21 boundary tests using the canonical "accept exactly N, reject N+1" pattern (e.g. `'a'.repeat(80)` accepts on foodSchema.name, `'a'.repeat(81)` rejects). For the four schemas that already test the required-field rejection paths but missed enum/length validators (joinFamilySchema email format, joinFamilySchema password length), added matching tests so the cross-schema coverage shape is uniform. No production code touched. Bumped `APP_VERSION` to v0.5.57. 330 tests pass across 24 suites (was 309, +21). TypeScript clean.
- v0.5.56 — Fix: harden the three zustand persisted stores against MMKV corruption. `src/stores/child-store.ts:7-11`, `src/stores/auth-store.ts:7-11`, and `src/stores/settings-store.ts:8-12` previously wired `mmkvStorage = { getItem: name => storage.getString(name) ?? null, setItem: ..., removeItem: ... }` with zero error handling — a corrupted MMKV instance (documented failure mode in `NEXT_STEPS.md` failure-mode table: "MMKV storage corrupted across upgrade | Child store crashes | N | Add a try/catch + fallback to null in `child-store.ts:7-11`") would surface a thrown error from inside zustand's `persist` middleware on first hydration, propagating through the React tree and crashing the app at launch. The bug is invisible during normal operation but worst-possible at the moment it would occur — a corrupt-storage error on every app cold-start with no recovery path short of uninstalling. Three changes, one per store, each wrapping all three adapter methods (`getItem`/`setItem`/`removeItem`) in try/catch with a logged-but-swallowed error: getItem returns `null` on failure (so `persist` sees no stored state and hydrates with the default values), setItem and removeItem log + swallow (the next state change retries the write — a one-time write failure is recoverable, an unhandled throw is not). Console.error messages are prefixed with the store name so a future Sentry breadcrumb can attribute the failure correctly (matches the v0.5.34 log-then-toast pattern). +6 unit tests in a new `src/stores/__tests__/mmkv-resilience.test.ts` use `jest.doMock` + `jest.resetModules` to inject a throwing `createMMKV` and assert each store loads with defaults instead of crashing, and that mutations (`selectChild`, `login`, `setTheme`) succeed in-memory even when MMKV writes throw. Bumped `APP_VERSION` to v0.5.56. 309 tests pass across 24 suites (was 303, +6). TypeScript clean.
- v0.5.55 — Fix: `ExposureCard` no longer renders an empty `<Text>` when `childName` is the empty string. `app/food/[id].tsx:251` mounts ExposureCard for each row of the per-food exposure history with `childName=""` because the parent has already chosen the child upstream — there is no need to re-state it on every history row. But `src/components/ExposureCard.tsx:61` rendered `<Text style={styles.childName}>{childName}</Text>` unconditionally, so the empty string flowed through as an empty Text node — which still participates in the parent's `info: { gap: 2 }` flex layout, leaving a 2px ghost line below the food name on every row. Visually subtle but visible side-by-side under macro inspection (foodName has a tiny rule of empty space below it that the dashboard's variant — which always has a non-empty childName — doesn't have). One-line fix: `{childName ? <Text style={styles.childName}>{childName}</Text> : null}`. The dashboard recently-logged path (`app/(tabs)/index.tsx:215`) passes a non-empty `childName={exp.childName}` so the truthy branch fires and behavior is unchanged there. +1 component test pins the contract: when `childName=""`, the empty Text is gone and the food name is still the only header content (regression lock against a future refactor that strips the gating ternary). 303 tests pass across 23 suites (was 302, +1). TypeScript clean. Bumped `APP_VERSION` to v0.5.55.
- v0.5.54 — Fix: Log Exposure form's "no foods yet" hint is now a tappable CTA. `app/(tabs)/log.tsx:154-156` previously showed an italic-and-grey static `<Text style={styles.hint}>Add some foods first to start logging exposures.</Text>` when `foodsList.length === 0` — informational only, with no action. A parent who finished onboarding and immediately tapped Log saw a hint but had to discover the small "+ Add New" link in the section header (line 132) to actually do anything. Same UX-clarity gap class as v0.5.40 (Progress tab "Select a Child" empty state CTA) and v0.5.35 (Progress tab "No Progress Yet" CTA). Replaced the bare Text with a `Pressable` that routes via `router.push('/food/add')` (mirrors the in-repo pattern at line 133), styled as a primary-tinted card (`primaryLight` background + primary border + primary-color text + `→` arrow) so the call-to-action is visually distinct from the surrounding form fields without overshadowing the still-primary "Save Exposure" submit button. Carries `accessibilityRole="button"` + `accessibilityLabel="Add foods to start logging exposures"` per the v0.5.17–v0.5.42 a11y polish train so VoiceOver/TalkBack reads it as an actionable CTA, not orphan text. The original `hint` style is preserved (still used elsewhere) — only the empty-state branch swapped. No new tests — screen-level Pressable in a conditional render that needs a render harness; matches the v0.5.40/v0.5.35 ship-without-test pattern. Bumped `APP_VERSION` to v0.5.54. 302 tests pass across 23 suites. TypeScript clean.
- v0.5.53 — Fix: alphabetize foods loaded for the Foods tab list and the Log Exposure form's food chip row. `app/(tabs)/foods.tsx:38-39` and `app/(tabs)/log.tsx:60` previously ran `db.select().from(schema.foods).where(eq(...))` with no `.orderBy()` — SQLite returns rows in physical row order (effectively insertion order for a freshly-populated table), so a parent who batch-added "Carrots, Broccoli, Apples" through the Add Food modal saw them appear in that exact order on both screens forever. With more than ~10 foods the lack of order makes scanning the Foods tab slower than necessary and turns the Log form's horizontal food chip row into a search-by-memory exercise. Added `asc` to the existing drizzle-orm imports and chained `.orderBy(asc(schema.foods.name))` on both queries; the safe-food partition (`partitionSafeFoods`) and category/search filter (`filterFoods`) preserve relative order so the alphabetical sort flows through to the rendered surfaces — both the pinned Safe Foods row and the regular FlashList list end up alphabetical. SQLite default collation is binary, so capitalized food names ("Apple" < "Broccoli") sort intuitively; mixed-case input (uncommon for food names) would sort uppercase before lowercase, which is acceptable for MVP. No new tests — the change is at the query layer, and the existing 302 tests use in-memory fixtures that don't go through the DB. Bumped `APP_VERSION` to v0.5.53. 302 tests pass across 23 suites. TypeScript clean.
- v0.5.52 — Fix: announce dashboard summary cards and Stage Distribution items to assistive tech. `app/(tabs)/index.tsx:148-156` rendered two static summary cards ("Today's Exposures: 5", "Foods Tracked: 12") as bare `<View>` elements with no `accessibilityRole`/`accessibilityLabel` — VoiceOver/TalkBack would read the inner `<Text>` children as separate text nodes ("5" then "Today's Exposures") with no relational anchor that the number BELONGED to the label. The Stage Distribution grid (lines 173-185) had the same gap class six times over: each of the 6 stage tiles (Tolerate/Interact/Smell/Touch/Taste/Eat) rendered as bare `<View>`s, so a SR user navigating across the grid heard "5, Tolerate, 2, Interact, 7, Smell, …" with no clear "this count belongs to this stage" pairing — and would have to mentally re-zip every-other-item back together. Same a11y polish train as v0.5.17-v0.5.47 (the FoodCard/ExposureCard/StageIndicator/RatingPicker/ProgressBar/EmptyState round); the dashboard's two non-interactive widget surfaces were the last unannounced patches. Pattern follows v0.5.30 verbatim — `accessibilityRole="text"` (the RN-supported role for grouped read-only content; iOS VoiceOver maps it to `UIAccessibilityTraitStaticText`, Android TalkBack reads the bound label as a single chunk) plus a single combined `accessibilityLabel` so the screen reader announces "Today's Exposures: 5, text" instead of the two-token jumble. The Stage Distribution items use a count-aware label that pluralizes ("1 food" vs "5 foods") so a SR user hears natural English instead of "Tolerate stage: 1 foods" — small but matches how a parent reading the screen aloud would phrase it. The third summary card (the "+ Log Exposure" Pressable at line 162) already carried `accessibilityRole="button"` + `accessibilityLabel="Log new food exposure"` from v0.5.20 and is unchanged. No new tests — these are screen-level conditional renders that need a render harness; matches the v0.5.17-v0.5.47 a11y polish ship-without-test pattern (the underlying ExposureCard, StageIndicator, ChildSelector all have their own component tests). Bumped `APP_VERSION` to v0.5.52. 302 tests pass across 23 suites. TypeScript clean.
- v0.5.51 — Fix: remove dead "Invite Spouse" row from the Settings → Family card. `app/(tabs)/settings.tsx:96-101` rendered an "Invite Spouse" row immediately below the tappable "Add Child" row — visually identical (`styles.row` + `<Text style={styles.label}>` + `<Text style={styles.value}>`) but it was a bare `<View>`, not a `<Pressable>`, with no `onPress`. Tapping it did nothing — no haptic, no transition, no alert. Misleading by construction: the value text "Share invite code" implied a routable destination existed. Per `NEXT_STEPS.md` "NOT in scope: Co-parent family invites — `families.inviteCode` exists in schema, no reason to build the UI yet" and CLAUDE.md "Multi-child / parental consent flows: not built". The schema field is preserved untouched (no migration risk, no data loss); only the dead UI surface and its preceding `<View style={styles.divider} />` are removed. Closes the same UX-clarity gap class as v0.5.15 (food detail "no exposures yet" misleading copy) and v0.5.40 (Progress tab "Select a Child" empty state). The Family card now contains exactly one row — Add Child — which is the only family-management surface that's wired end-to-end. When co-parent invites ship in a future milestone, this row can return alongside an `inviteCode` lookup + `Share` API call (same pattern v0.5.0 used for CSV export); deleting it now is reversible. No new tests — pure deletion of dead code, no behavior change for any populated path. Bumped `APP_VERSION` to v0.5.51. 302 tests pass across 23 suites. TypeScript clean.
- v0.5.50 — Fix: surface the four optional exposure-detail fields (mealType, temperature, texture, setting) on the food detail page's exposure history. The Log Exposure form has captured all four since the v0.5.39 (texture) and v0.5.41 (setting) chip-row work; the data round-trips into SQLite correctly via `app/(tabs)/log.tsx:75-83`. But the read path silently dropped it: `app/food/[id].tsx:38-45` selected only `mealType` from the `exposures` table (and never passed it to the rendered ExposureCard), and skipped `temperature`/`texture`/`setting` entirely from the projection. A parent who logged "Carrots, smell stage, refused, mealType=lunch, temperature=hot, notes=spit it out" and opened the food detail page saw stage + notes + rating only — meal/temperature/texture/setting vanished into the database with no surface anywhere in the UI to read them back. The form's Notes copy ("optional") implied parents could capture nuance through these dimensions, but the only displayed dimension was Notes. Two coordinated edits: (a) `src/components/ExposureCard.tsx` gains four optional props (`mealType?`, `temperature?`, `texture?`, `setting?`) and a new `metaRow` block that renders 1–4 capitalized text items each prefixed with a unique emoji (🍽️ meal, 🌡️ temperature, 🧊 texture, 📍 setting), gated by `hasMeta = !!(mealType || temperature || texture || setting)` so the row collapses cleanly when no fields are set (the common case — most exposures will skip the optional details block since the form's "Add More Details" toggle is collapsed by default per v0.5.19). (b) `app/food/[id].tsx` projection widens to include `temperature`, `texture`, `setting` columns and the `.map` call site forwards all four with the existing `?? undefined` null-narrowing pattern used for `rating` and `notes`. The dashboard's `app/(tabs)/index.tsx:198-208` ExposureCard mount is intentionally unchanged — the dashboard's "Recent Activity" list uses a tighter projection and forcing 4 more columns through the join would balloon the recently-logged scan; the food detail page is the right home for nuance review (parent is already focused on a single food's history). +3 component tests pin the contract: meta row renders all 4 fields when populated (regression lock against silent-data-drop reverts), meta row stays absent when no fields are set (no leftover icons / empty layout space — the common case stays clean), partial-fill renders only the populated fields independently (a parent might log meal context "lunch" without bothering with texture). The 4-icon legend is documented inline in the test names and the META_ICONS const so a future contributor adding a 5th dimension has a clear template. Bumped `APP_VERSION` to v0.5.50. 302 tests pass across 23 suites (was 299, +3). TypeScript clean.
- v0.5.49 — Fix: `formatRelativeDate` now uses calendar-day deltas instead of a 24-hour subtraction. `src/lib/utils.ts:33` previously computed `Math.floor((now - date) / 86400000)` and labeled day=0 as "Today", day=1 as "Yesterday". This silently mislabeled the most common edge case in a logging app: a parent logs an exposure at 11pm, opens the dashboard at 1am the next morning — only 2 wall-clock hours separate the two timestamps, so `floor(7200000 / 86400000) = 0` and the row showed "Today" even though calendar-wise it was logged yesterday. The bug surfaces on every screen that calls this helper: `app/(tabs)/index.tsx` recently-logged dashboard list, `src/components/ExposureCard.tsx` (the one used in both the dashboard and the food detail page exposure history). Compounds the v0.5.48 navigation wiring — a parent tapping a "Today" row that was actually yesterday gets confused about which day's data they're seeing. Fix routes through the existing `getStartOfDay` helper to compare calendar boundaries: `Math.round((startOfToday - startOfDate) / 86400000)`. The `Math.round` (not `Math.floor`) handles DST drift cleanly — across a spring-forward boundary the diff is 23 hours, across fall-back it's 25, both of which round to 1 day. The day=0 → "Today" branch widened to `days <= 0` to keep clock-skew futures (device-time drift makes occurredAt occasionally land slightly in the future) rendering as "Today" instead of trying "1 days ago" with a negative round. +3 unit tests pin the contract: late-evening log viewed in early morning shows "Yesterday" (the bug case), same-day log earlier in the day shows "Today" (regression lock for the happy path), slightly-future-dated row shows "Today" (defensive). Existing 4 formatRelativeDate tests still pass — they used `setDate(getDate() - N)` which preserves time-of-day, so the 24-hour-aligned timestamps round identically under either implementation. Bumped `APP_VERSION` to v0.5.49. 299 tests pass across 23 suites (was 296, +3). TypeScript clean.
- v0.5.48 — Feature: wire dashboard recently-logged `ExposureCard` rows to navigate to the food detail page. `app/(tabs)/index.tsx:198-206` mounted ExposureCard with no `onPress`, so a parent looking at "I logged Broccoli yesterday" in the dashboard's recently-logged list could not tap to drill into the food's history (the page where stage-bump, exposure history, and safe-food toggle live). The food detail page is the secondary nav surface from every food-card tap on the Foods tab and from the dashboard's "Today's Exposures" stat — except the recently-logged list, which was a dead-end. Two-line wiring: (a) added `foodId: schema.exposures.foodId` to the existing dashboard query select projection (the join already pulled the row, just not the column); (b) passed `onPress={() => router.push(\`/food/${exp.foodId}\` as any)}` to each ExposureCard, mirroring the existing `as any` cast in-repo at `app/(tabs)/log.tsx:96`. Pairs with v0.5.47: now that ExposureCard's `onPress`-conditional `accessibilityRole="button"` + `accessibilityLabel` are wired, a SR user navigating the recently-logged list hears "Open Broccoli details, button" on each row instead of three separate text nodes — the v0.5.47 a11y fix is what makes this navigation discoverable. The food detail page mount at `app/food/[id].tsx:244-254` deliberately stays without `onPress` (read-only history list — tapping a row in the same food's history page should not loop back to itself). No new tests — screen-level wiring; the v0.5.47 component-level regression locks already cover the prop forwarding contract. Bumped `APP_VERSION` to v0.5.48. 296 tests pass across 23 suites. TypeScript clean.
- v0.5.47 — Fix: announce the `ExposureCard` to assistive tech and stop announcing it as a button when it isn't tappable. `src/components/ExposureCard.tsx:31` rendered the whole card as a bare `Pressable` with no `accessibilityRole`/`accessibilityLabel` — VoiceOver/TalkBack would read the inner texts (foodName, childName, "Today") as separate text nodes with no anchor that the row was a single navigable unit. Same gap class as v0.5.24 (FoodCard outer Pressable a11y) but worse: ExposureCard has *two* mount sites with *opposite* interactivity contracts — `app/food/[id].tsx:244-254` (the per-food exposure history list, no `onPress`) and `app/(tabs)/index.tsx:198-206` (the dashboard recently-logged list, no `onPress` today). Forcing `role=button` unconditionally would lie to a SR user when the card is decorative; omitting it would lie when the card is tappable. Conditional fix: `accessibilityRole={onPress ? 'button' : undefined}` + `accessibilityLabel={onPress ? \`Open ${foodName} details\` : undefined}` — when `onPress` is provided the card announces as a labeled button mirroring the v0.5.24 FoodCard pattern; when undefined the Pressable announces only its inner text content (the read-only history-list semantics). +2 component tests lock both branches: `getByLabelText('Open Carrots details')` resolves to a button when `onPress` is passed AND triggers the existing onPress assertion through that label (regression lock — a future refactor that drops the prop fails loudly); `queryByLabelText` returns null AND `queryByRole('button')` returns null when `onPress` is omitted (no leaky default — the read-only path stays clean). Bumped `APP_VERSION` to v0.5.47. 296 tests pass across 23 suites (was 294, +2). TypeScript clean.
- v0.5.46 — Fix: announce in-flight `Button` work to assistive tech via `aria-busy`. `src/components/Button.tsx:64` already carried `accessibilityState={{ disabled: disabled || loading }}` (so taps are blocked while async work is in flight) and swapped its label `<Text>` for an `<ActivityIndicator/>` (visual cue), but offered no audible cue to a screen-reader user that the in-flight work was happening — only that the button was "dimmed". Every async write surface in the app funnels through this component: Save Exposure (`app/(tabs)/log.tsx`), Add Food (`app/food/add.tsx`), Add Child (`app/child/add.tsx`, `app/onboarding/add-child.tsx`), Bump-to-stage and Log Detailed Exposure (`app/food/[id].tsx`), Get Started / Start Tracking (`app/onboarding/index.tsx`, `app/onboarding/join.tsx`). A parent on VoiceOver tapping Save and waiting for the SQLite write got no progress announcement — just silence followed by a fresh form (the v0.5.7 reset path) or an `Alert.alert` (the v0.5.34 catch path). Two-prop fix: kept the existing `accessibilityState.busy: loading` for native iOS/Android (which honor it via `UIAccessibilityTraitUpdatesFrequently` / `AccessibilityNodeInfo.setLiveRegion`) and added the standardized `aria-busy={loading}` prop alongside it for the web target — react-native-web 0.21.2 does not serialize `accessibilityState.busy` to `aria-busy` (verified empirically: the Pressable's rendered `<button>` shows only `aria-disabled`, `aria-label`, `role`, `tabindex`, no `aria-busy`), so the explicit `aria-busy` prop is required for the web platform. RN core 0.65+ maps the ARIA-style prop to native `accessibilityState.busy` internally, so passing both is redundant but additive — no platform reads the wrong value, and a future RNW upgrade that closes the serialization gap doesn't break anything. +2 component tests lock in the contract: one renders `<Button label="Save" loading />`, queries the labeled element, asserts `aria-busy="true"` is present (regression lock — a future refactor that drops the prop fails loudly); the other renders the same Button without `loading` and asserts `aria-busy` is not "true" (no leaky default — the not-loading path stays clean). Bumped `APP_VERSION` to v0.5.46. 294 tests pass across 23 suites (was 292, +2). TypeScript clean.
- v0.5.45 — Fix: announce the two Settings-tab `Switch` controls to assistive tech. `app/(tabs)/settings.tsx:108-124` rendered the Quick Log Mode and Notifications switches with a sibling `<Text style={styles.label}>` above each one but no `accessibilityLabel` on the `Switch` itself — VoiceOver/TalkBack would announce just "switch, on" / "switch, off" with no anchor noun, so a parent navigating the Preferences card by screen-reader could not tell which switch they had landed on. Same gap class as v0.5.22/v0.5.26/v0.5.28 (TextInputs without `accessibilityLabel`) and the v0.5.17-v0.5.42 chip/Pressable harmonization train — every other interactive surface in the Settings tab (Add Child row, Theme chips, Feeding Profile chips, Export Data row, Sign Out button) carries a context-rich label after that work, but the two `Switch`es slipped through because they're a different RN primitive class with their own a11y props. Two-line fix following the established pattern: `accessibilityLabel="Quick Log Mode"` and `accessibilityLabel="Notifications"`, mirroring the visible `<Text>` labels exactly so a SR user hears the same dimension name a sighted user reads. The `value` prop is already what RN/iOS map to `accessibilityState.checked` automatically, so no separate state prop is needed (mirrors how `<Switch>` is documented in RN core). No new tests — these are screen-level `Switch` props that need a render harness; matches the v0.5.17-v0.5.28 a11y polish ship-without-test pattern. Bumped `APP_VERSION` to v0.5.45. 292 tests pass across 23 suites. TypeScript clean.
- v0.5.44 — Fix: wrap the Progress tab's two early-return `EmptyState` branches in `SafeAreaView`. `app/(tabs)/progress.tsx` had three early returns: `loading` → `<SafeAreaView><ActivityIndicator/></SafeAreaView>` (line 65-71), `!selectedChildId` → bare `<EmptyState/>` (line 73-83, added v0.5.40), and `stats.totalExposures === 0` → bare `<EmptyState/>` (line 85-95, added v0.5.35). The `EmptyState` component itself is `flex: 1` + `justifyContent: 'center'` + `padding: theme.spacing.xl` (`src/components/EmptyState.tsx:32-39`) so it fills the screen. Without `SafeAreaView`, on a notched iPhone the icon (rendered above the centered title via `gap`) could clip under the status-bar/notch on devices with a tall notch — and on Android the system status bar would tint the EmptyState's primary background instead of being respected. The loading branch already does the right thing; the two empty branches diverged from the established pattern when they were added later. Two-line fix per branch: wrap each `<EmptyState>` in `<SafeAreaView style={styles.container}>...</SafeAreaView>`, mirroring the loading branch verbatim. The existing `styles.container` already carries `flex: 1` + `backgroundColor: theme.colors.background` so no new styles needed. The non-empty render path at line 103-241 was already correctly wrapped — only the two early-return branches were inconsistent. No new tests — these are screen-level conditional renders that need a render harness; the change is structural-consistency, not behavior. Bumped `APP_VERSION` to v0.5.44. 292 tests pass across 23 suites. TypeScript clean.
- v0.5.43 — Refactor: drop the stale `?? 15` fallback in the Progress tab's profile tag and route through `getThresholdForProfile(feedingProfile)`. `app/(tabs)/progress.tsx:156` rendered the section header tag as `{FEEDING_PROFILE_CONFIG[feedingProfile].label} · {stats.foodProgress[0]?.threshold ?? 15}` — derived the threshold from the *first food's* row in the sorted progress array, with a hardcoded `15` typical-profile fallback. Same crutch the v0.5.x `getEncouragementMessage` refactor explicitly removed (see the regression test at `src/lib/__tests__/progress-stats.test.ts:233` which locks "uses the explicit profile threshold instead of foodProgress[0].threshold"). The display path was the last unconverted reader. Today this is dead-code in practice — the section is gated on `stats.foodProgress.length > 0` so the `?? 15` branch never fires, and every row's `threshold` field is computed from the same `feedingProfile` so they all agree — but the indirection is still a footgun: a future contributor adding a per-row threshold override (e.g. food-specific allergen threshold) would silently change the tag based on which food sorted to position 0, divorcing the displayed number from the profile label next to it. One-line fix routes the tag through `getThresholdForProfile(feedingProfile)`, mirroring how `calcExposureProgress` reads thresholds and how `getEncouragementMessage` reads them after the v0.5.x cleanup. Single source of truth: the profile, not a serendipitous array index. Imported `getThresholdForProfile` alongside the existing `FEEDING_PROFILE_CONFIG` import from `@/src/lib/thresholds` (already in dependencies). No new tests — the change is a one-line read swap that produces identical output today; the existing 4 progress-stats tests covering the `getThresholdForProfile`-driven values cover the helper's correctness. Bumped `APP_VERSION` to v0.5.43. 292 tests pass across 23 suites. TypeScript clean.
- v0.5.42 — Fix: announce the `EmptyState` action button to assistive tech. `src/components/EmptyState.tsx` is mounted on every primary tab's empty branches — the dashboard's no-children state, the Foods tab's "no foods" / "no matches" branches (v0.5.2), the Progress tab's "Select a Child" (v0.5.40) and "No Progress Yet" (v0.5.35), the food detail page's "Food Not Found" (v0.5.6), and the food detail page's "Select a child to see exposure history" (v0.5.15). The component already shipped with `accessibilityRole`/`accessibilityLabel` on the inner emoji icon decoration via none, but the tappable Pressable button (line 19) carried only the inner `<Text style={styles.buttonText}>{actionLabel}</Text>` — bare `Pressable`, no role hint, no announced action. VoiceOver/TalkBack would read just the button's visible text ("Go to Dashboard", "Log Exposure", "Add Food", "Clear Filters") with no role anchor, so a parent on a screen reader could not tell the text was tappable. Same gap class as v0.5.17-v0.5.28 (FoodCard, ChildSelector, StageIndicator, RatingPicker, ProgressBar) — every other interactive surface in the app's component layer was harmonized; the EmptyState button was the last one. One-line fix following the established pattern verbatim: `accessibilityRole="button"` + `accessibilityLabel={actionLabel}` (the existing `actionLabel` prop is already a complete imperative phrase across all six call sites — "Go to Dashboard", "Log Exposure", "Add Food", "Clear Filters", "Go to Foods", "Add Child" — so no transformation is needed). +1 component test asserts `getByLabelText` resolves to a `<button role="button">` element on react-native-web and that the existing `onAction` click assertion still fires when bound to the labeled element (regression lock — a future refactor that drops the label fails loudly). Bumped `APP_VERSION` to v0.5.42. 292 tests pass across 23 suites (was 291, +1). TypeScript clean.
- v0.5.41 — Feature: wire the Setting chip row into the Log Exposure form. `app/(tabs)/log.tsx:16` imported `MEAL_TYPES`, `TEMPERATURES`, `TEXTURES` from `src/lib/constants.ts` (with `TEXTURES` wired in v0.5.39) but the matching `SETTINGS = ['home','school','restaurant','therapy']` constant (`constants.ts:38`) was never imported and the `setting` form field had no UI. The `exposureSchema.setting: z.enum(['home','school','restaurant','therapy']).optional()` validator (`src/lib/validation.ts:30`) and the DB write at line 82 (`setting: data.setting ?? null`) both already accepted the field — only the capture UI was missing. A parent logging an exposure at school vs. a therapy session had no way to capture the location dimension that SOS feeding therapy uses to spot setting-specific reactions (e.g. willing to taste at home but refuses at school). Adds a new chip row inside the `showDetails` block, immediately below the v0.5.39 Texture row and above Notes. Mirrors the Texture `Controller` block exactly: same `chipRow` layout, same `chip`/`chipSelected` styling, same `accessibilityRole="button"`, dimension-prefixed `accessibilityLabel={\`Setting: ${label}\`}` (matches the v0.5.19 Meal/Temperature + v0.5.39 Texture pattern), `accessibilityState={{ selected: isSelected }}`, and the `isSelected` local pulled out of the `.map` so the same `value === setting` comparison isn't computed three times per chip. Now all five optional-detail dimensions (Meal, Temperature, Texture, Setting, Notes) are surfaced in the form. No new tests — screen-level Controller-rendered chips that need a render harness; matches the v0.5.19 / v0.5.20 / v0.5.21 / v0.5.25 / v0.5.39 chip-row ship-without-test pattern. Bumped `APP_VERSION` to v0.5.41. 291 tests pass across 23 suites. TypeScript clean.
- v0.5.40 — Fix: add CTA action to the Progress tab's "Select a Child" empty state. `app/(tabs)/progress.tsx:73-81` rendered an `EmptyState` with `icon="📊"`, `title="Select a Child"`, and a description telling the user to "Go to the dashboard and select a child" — but no `actionLabel`/`onAction` props, so the user had to read the prose copy and figure out the dashboard tab is two icons over in the bottom tab bar. v0.5.35 added the same screen's "No Progress Yet" empty state with a working `actionLabel="Log Exposure"` + `onAction={() => router.push('/(tabs)/log')}`; the older "Select a Child" branch (which has been there since the v0.1.1 null-child safety pass) was left without one. Mirrors the v0.5.35 wiring: passes `actionLabel="Go to Dashboard"` and `onAction={() => router.push('/(tabs)' as any)}` (the `as any` cast matches the existing in-repo pattern at `app/(tabs)/log.tsx:96` for routing back to the tabs index — expo-router's typed-routes plugin doesn't synthesize the `/(tabs)` group root). Streamlined the description from "Go to the dashboard and select a child to view their progress." to "Pick a child on the dashboard to view their progress." since the action button is now the explicit instruction. The dashboard's `ChildSelector` (line 130 of `app/(tabs)/index.tsx`) is the destination — that's the only ChildSelector mount point that's always visible. No new tests — screen-level conditional render that needs a render harness; matches the v0.5.35 ship-without-test pattern. Bumped `APP_VERSION` to v0.5.40. 291 tests pass across 23 suites. TypeScript clean.
- v0.5.39 — Feature: wire the Texture chip row into the Log Exposure form. `app/(tabs)/log.tsx:16` imported `TEXTURES` from `src/lib/constants.ts` but never referenced it — dead code since the form was first wired. The `exposureSchema.texture: z.enum(['smooth','crunchy','soft','chewy','mixed']).optional()` validator (`src/lib/validation.ts:28`) and the DB write at line 80 (`texture: data.texture ?? null`) both already accepted the field; only the capture UI was missing. A parent who fed a "soft" mash to a toddler vs. a "crunchy" cracker had no way to log the texture distinction that SOS therapy treats as core diagnostic data — the Foods schema's `defaultPreparation` chip on the food add screen is preparation-method (raw/steamed/etc.), not texture-class, so the two are not interchangeable. Adds a new chip row inside the `showDetails` block, between the existing Temperature row (lines 232-260) and Notes (262-282). Mirrors the Temperature `Controller` block exactly: same `chipRow` layout, same `chip`/`chipSelected` styling, same `accessibilityRole="button"`, dimension-prefixed `accessibilityLabel={\`Texture: ${label}\`}` (matches the v0.5.19 Meal/Temperature pattern), `accessibilityState={{ selected: isSelected }}`, and the `isSelected` local pulled out of the `.map` so the same `value === tex` comparison isn't computed three times per chip. Now all four optional-detail dimensions (Meal, Temperature, Texture, Notes) are surfaced in the form. No new tests — screen-level Controller-rendered chips that need a render harness; matches the v0.5.19 / v0.5.20 / v0.5.21 / v0.5.25 chip-row ship-without-test pattern. Bumped `APP_VERSION` to v0.5.39. 291 tests pass across 23 suites. TypeScript clean.
- v0.5.38 — Fix: announce the Feeding Profile chip row to assistive tech. `app/(tabs)/settings.tsx:158-170` set `accessibilityRole="button"` and `accessibilityState={{ selected }}` on each Feeding Profile chip but had no `accessibilityLabel` — VoiceOver/TalkBack would read just the bare profile config label ("Typical", "Picky", "ARFID"). The Theme chip row directly above (line 137) carries `accessibilityLabel={\`Theme: ${label}\`}` from v0.5.21, and every other chip row in the app (Meal/Temperature on log.tsx from v0.5.19, Rating on RatingPicker from v0.5.25, Filter on Foods tab from v0.5.18, Category on food/add from v0.5.20) follows the dimension-prefix convention. The Feeding Profile chips were the last unprefixed row — a SR user navigating Settings in sequence heard "Theme: Light, button, selected → Theme: Dark, button → Theme: System, button" then dropped to bare "Typical, button → Picky, button" with no anchor noun. One-line fix: pulled `profileLabel` out of the `.map` into a local so the same string is reused in both the inner Text and the new label, then added `accessibilityLabel={\`Profile: ${profileLabel}\`}`. The `accessibilityRole` and `accessibilityState` are unchanged. No new tests — screen-level Pressable that needs a render harness; matches the v0.5.17-v0.5.28 a11y polish ship-without-test pattern. Bumped `APP_VERSION` to v0.5.38. 291 tests pass across 23 suites. TypeScript clean.
- v0.5.37 — Fix: surface truncation on the Progress tab's "Exposures Toward Acceptance" list. `app/(tabs)/progress.tsx:157` already capped the rendered rows at `stats.foodProgress.slice(0, 10)` — sensible to keep the screen scannable when a parent has logged exposures across many foods — but with no indicator that anything had been hidden. A parent of an active feeder with 25 logged foods saw the top 10 closest-to-target rows and assumed the rest didn't exist, undermining the per-food progress view's value (the top-N sort by `pct` then `current` puts unreached foods further from the threshold below the cut). Adds a one-line caption directly below the list when `stats.foodProgress.length > 10`: italic, muted, centered: "Showing top 10 of N". The list itself is unchanged — same sort, same slice — only the silent truncation is now disclosed. New `truncationNote` style (xs italic centered, theme.colors.textSecondary). Doesn't render when ≤10 foods exist (the no-truncation case stays clean). No new tests — screen-level conditional render; the existing 291 across 23 suites still pass. TypeScript clean. Bumped `APP_VERSION` to v0.5.37.
- v0.5.36 — Feature: pull-to-refresh on the Foods tab. `app/(tabs)/foods.tsx`'s FlashList previously had no `refreshControl` prop, so a parent who logged an exposure from the Log tab and switched to Foods saw stale per-food exposure counts (the v0.5.8 `buildFoodsWithStats` output) until they killed the app or changed the selectedChild. The dashboard at `app/(tabs)/index.tsx:119` already wires `RefreshControl`, and v0.5.35 added the same on the Progress tab — this commit closes the gap on the third primary tab. Adds a `refreshing` boolean state, a `useCallback` `onRefresh` handler that re-runs `loadFoods()` while toggling the flag, and passes `refreshControl={<RefreshControl refreshing={refreshing} onRefresh={onRefresh} />}` to the `FlashList`. FlashList accepts the standard `refreshControl` prop directly (mirrors FlatList's API) so no wrapper is needed. Pulls in `RefreshControl` from `react-native`. The non-list code paths (loading spinner branch + the two empty-state branches `none`/`filtered`) are unchanged — refresh only attaches to the populated list, which matches the dashboard's pattern (refresh on the populated ScrollView). No new tests — screen-level UX wiring; the existing 291 across 23 suites still pass. TypeScript clean. Bumped `APP_VERSION` to v0.5.36.
- v0.5.35 — Fix: Progress tab no longer renders an all-zeros dashboard for a freshly-onboarded child, and now supports pull-to-refresh. `app/(tabs)/progress.tsx` previously had two early-return branches — `loading` (ActivityIndicator) and `!selectedChildId` (EmptyState "Select a Child") — but no branch for the much more common state of "child is selected, queries succeeded, but `stats.totalExposures === 0`". A parent who finished onboarding, added a child, and tapped Progress saw three "0 / 0 / 0" stat cards plus six all-zero Stage Distribution bars plus six all-zero Food Categories tiles — visually dense, semantically empty, and with no CTA pointing at the Log tab where the cure lives. The avgRating section (gated `>0`) and the per-food foodProgress section (gated `length>0`) correctly hid themselves; only the always-rendered Stage Distribution + Food Categories grids made the screen look broken. Same UX-clarity bug class as v0.5.15 (food detail "no child selected" vs "no exposures yet for this food") and v0.5.2 (Foods tab "no foods at all" vs "no matches for filter"). Adds a third early-return branch immediately after the `!selectedChildId` guard: `if (stats.totalExposures === 0)` returns an `EmptyState` with `icon="📊"`, `title="No Progress Yet"`, `description="Log your first food exposure to see progress charts and stage distribution."`, and an `actionLabel="Log Exposure"` whose `onAction` calls `router.push('/(tabs)/log')` — pulled in `useRouter` from expo-router (mirrors `app/(tabs)/index.tsx:158`'s exact route string). Same commit also wires pull-to-refresh on the ScrollView: a new `refreshing` state + `onRefresh` async callback (re-runs `loadStats`, toggles refreshing flag) feeds a `RefreshControl` element into the ScrollView's `refreshControl` prop — copies `app/(tabs)/index.tsx:96-100,119` verbatim. Closes the gap where a parent who logged an exposure on the Log tab and switched back to Progress saw stale data with no way to refresh short of killing the app or changing feeding-profile (the only `loadStats` dep that retriggers). The `safeFoods` count is unrendered on the Progress tab so the EmptyState branch hides nothing visible. No new tests — these are screen-level conditional renders + a scroll-control wiring that need a render harness; the change matches the v0.5.15 / v0.5.2 ship-without-test pattern. Bumped `APP_VERSION` to v0.5.35. 291 tests pass across 23 suites. TypeScript clean.
- v0.5.34 — Refactor: harmonize error logging across every screen-level `catch (err)` block. Eight existing write-path catches across the app — `app/(tabs)/log.tsx:99` (onSubmit), `app/(tabs)/settings.tsx:46` (export), `app/food/[id].tsx:93` (handleBumpStage) and `:109` (handleToggleSafeFood), `app/food/add.tsx:63` (onSubmit), `app/child/add.tsx:55` (onSubmit), `app/onboarding/add-child.tsx:55` (onSubmit), `app/onboarding/join.tsx:55` (onSubmit) — only fired `Alert.alert(...)` and never logged the underlying error. The four load-path catches established by v0.5.14 → v0.5.31 → v0.5.32 → v0.5.33 (`Failed to load dashboard data:`, `Failed to load progress stats:`, `Failed to start onboarding:`, `Failed to load log data:`, `Failed to load food detail:`) all use `console.error('Failed to <action>:', err)` first, then `Alert.alert`. The write-path catches were the inconsistent half: a parent reporting "saving the exposure failed with an error toast" gave the dev no stack trace in Metro logs and no Sentry breadcrumb (Sentry's RN SDK auto-captures `console.error` calls — see `src/lib/sentry.ts`). One-line addition to each catch: `console.error('Failed to <verb>:', err);` immediately above the existing `Alert.alert`. Verbs match each handler's domain ("save exposure", "export data", "bump stage", "toggle safe food", "add food", "add child", "add child" again for onboarding, "join family"). All 15 screen-level catches in `app/` now follow the log-then-toast pattern. No new tests — these are screen-level handlers that need a render harness; the change matches the established pattern. Bumped `APP_VERSION` to v0.5.34. 291 tests pass across 23 suites. TypeScript clean.
- v0.5.33 — Fix: surface DB-load failures on the food detail screen. `app/food/[id].tsx:27-62` `loadData()` previously wrapped its two `db.select` calls in `try { ... } finally { setLoading(false); }` with no `catch` — so a thrown error escaped as an unhandled promise rejection while `food` stayed `null` and the screen rendered the v0.5.6 "🤔 Food Not Found" empty state. That UX is correct when `foodResult[0]` is genuinely missing (deleted row, bad route param) but actively misleads when the query *itself* failed: a parent navigating to a food they just logged would see "this food may have been deleted" + a "Go to Foods" button when the row exists fine and the issue is environmental (transient SQLite, schema mismatch, OOM). Same bug class as v0.5.14 (Dashboard/Progress) → v0.5.31 (onboarding) → v0.5.32 (Log tab); this commit closes the equivalent gap on the food detail screen — the last load path left without an error toast. Adds a `catch (err) { console.error('Failed to load food detail:', err); Alert.alert('Error', 'Failed to load food details. Please try again.'); }` between the existing `try` and `finally`. The `food === null` empty state is preserved untouched for the genuine row-missing case; the Alert only fires on a thrown query — the two error modes are now distinguishable to the user (toast vs. empty state). The two write handlers (`handleBumpStage`, `handleToggleSafeFood`) already had `try/catch + Alert.alert` from earlier work; only the load path needed harmonizing. No new tests — screen-level handler that needs a render harness; matches the v0.5.14/v0.5.31/v0.5.32 ship-without-test pattern. Bumped `APP_VERSION` to v0.5.33. 291 tests pass across 23 suites. TypeScript clean.
- v0.5.32 — Fix: surface DB-load failures on the Log Exposure tab. `app/(tabs)/log.tsx:55-61` `loadData()` previously fired two `db.select` calls (children, then foods) with no try/catch. If either threw — corrupted SQLite, transient init failure, schema mismatch after a faulty migration — the parent would tap the Log tab and see two empty selectors with no indication of why; tapping Save would then trip the inline `errors.childId` validation message ("Select a child") which is misleading because the children list was failed-to-load, not empty. Same bug class v0.5.14 fixed for the Dashboard/Progress tabs and v0.5.31 fixed for `app/onboarding/index.tsx` — the only primary-tab `loadData` left without surfacing. Wraps both selects in a try block, logs the error to `console.error('Failed to load log data:', err)` (mirrors v0.5.14's `'Failed to load progress stats:'` and v0.5.31's `'Failed to start onboarding:'`), and surfaces `Alert.alert('Error', 'Failed to load data. Please try again.')` so the parent knows the issue is environmental, not user-input. The `onSubmit` handler at line 63-98 already had try/catch from earlier work — only the load path needed harmonizing. No new tests — this is a screen-level handler that needs a render harness; the change matches the established pattern (v0.5.14 and v0.5.31 also shipped without new tests for the same reason). Bumped `APP_VERSION` to v0.5.32. 291 tests pass across 23 suites. TypeScript clean.
- v0.5.31 — Fix: surface DB-insert failures during first-launch onboarding. `app/onboarding/index.tsx:handleGetStarted` previously fired two `db.insert` calls (families, then users) with no try/catch — if either threw (corrupted SQLite, OOM, transient init issue) the parent would tap "Get Started" on the welcome screen and see *nothing*: no router navigation, no alert, no spinner state change, just a dead button. The first-launch hot path failing silently is the worst possible UX surface — every other entry into the app is downstream of a successful run of this handler. Closes the equivalent gap that v0.5.14 fixed for the dashboard/progress tabs and that `app/onboarding/add-child.tsx:54-58` already had: wrap the inserts + login + router.push in a try block, log the error to `console.error` with a `'Failed to start onboarding:'` prefix (mirrors v0.5.14's `'Failed to load progress stats:'` etc.), and surface `Alert.alert('Error', 'Failed to start. Please try again.')` to the user. Also dropped the now-stale `// Create a local family and user for offline-first experience` comment — the code reads cleanly without it. No new tests — this is a screen-level handler that needs a render harness; the change matches the established pattern (v0.5.14 also shipped without new tests for the same reason). Bumped `APP_VERSION` to v0.5.31. 291 tests pass across 23 suites. TypeScript clean.
- v0.5.30 — Fix: pass context-rich `accessibilityLabel` to every `ProgressBar` call site. v0.5.29 added the optional `accessibilityLabel` prop on `src/components/ProgressBar.tsx` but left every call site using the bare default — so a SR user navigating the food detail page or the Progress tab still heard only the numeric value ("7 of 15") with no anchor noun, and the `showLabel={false}` average-rating bar at `progress.tsx:111` was effectively invisible to assistive tech (no inline `<Text>` to fall back to either). Wired four call sites: (1) `app/food/[id].tsx:177` — single bar under the StageIndicator on the food detail page → `accessibilityLabel={\`${food.name} exposures progress\`}` (food.name is non-null inside the not-found-branch guard at line 121); (2) `app/(tabs)/progress.tsx:111` — the avg-acceptance gauge (showLabel false) → `"Average acceptance rating"`; (3) `app/(tabs)/progress.tsx:141` — per-food row in the "Exposures Toward Acceptance" list → `accessibilityLabel={\`${row.foodName} exposures progress\`}` so a parent navigating the 10-row list hears which food each bar belongs to (the "X/15" count is rendered as a separate `<Text>` above the bar but a SR user moving across 10 progressbars in a row needs each one self-describing); (4) `app/(tabs)/progress.tsx:166` — Stage Distribution per-stage bar → `accessibilityLabel={\`${config.label} stage food count\`}` so a SR user hears "Tolerate stage food count, progressbar, 5 of 12" rather than just "5 of 12". No new tests — the prop forwarding is already locked by the v0.5.29 ProgressBar component tests; these are screen-level wirings. Bumped `APP_VERSION` to v0.5.30. 278 tests pass across 23 suites. TypeScript clean.
- v0.5.29 — Fix: announce the `ProgressBar` to assistive tech. `src/components/ProgressBar.tsx` is mounted on the dashboard ("Today's Exposures" / "Foods Tracked"), the food detail page (the `current/TARGET_EXPOSURES` bar under the StageIndicator), and the Progress tab (per-food "X/15" rows + the average-rating bar) — every visual progress indicator in the app routes through this component. Previously it rendered a bare `<View>` outer wrapper with no `accessibilityRole`/`accessibilityValue`/`accessibilityLabel`, so a screen-reader user got nothing for the visual fill — a parent on TalkBack heard the "7/15" `<Text>` label inside the bar (when `showLabel` was true) but had no role announcement to tell them that figure represented progress, not just a fraction; with `showLabel={false}` (e.g. the average-rating bar at `progress.tsx:111-117`) the SR user got nothing at all. Two changes: (1) outer wrapper now carries `accessibilityRole="progressbar"` + `accessibilityValue={{ now: current, min: 0, max: target }}` — `now` reports the *actual* `current` (not the visually-clamped `Math.min(current/target, 1)` fill), so a parent past the threshold hears "22 of 15" rather than the visually-misleading "15 of 15"; (2) new optional `accessibilityLabel` prop forwards to `aria-label` for callers that want context-rich announcements (e.g. "Broccoli progress, progressbar, 7 of 15") without forcing the change on every existing call site. Read-only, decorative-bar default — no label — passes through unchanged. +3 component tests: role lands on the outer wrapper (regression lock); aria-label forwards when passed; aria-label is `null` when omitted (no leaky default). Note on react-native-web: `accessibilityValue` is not serialized to `aria-valuenow/min/max` by the current RNW version, so the web-side test only locks role + label — native iOS/Android still honor `accessibilityValue` so the prop is forwarded for those platforms. Bumped `APP_VERSION` to v0.5.29. 278 tests pass across 23 suites (was 275, +3). TypeScript clean.
- v0.5.28 — Fix: announce the four remaining unanchored `TextInput`s. Four screens still relied on `placeholder` for labelling, which only reads while the field is empty — so the moment a parent typed a character, VoiceOver/TalkBack lost the field-name anchor and any subsequent "edited" announcement (e.g. "edited: Em") had no way to tell which field it was. Same `accessibilityLabel`-mirrors-visible-`<Text style={styles.label}>` pattern v0.5.22 (`app/child/add.tsx`) and v0.5.26 (`app/onboarding/add-child.tsx`) applied to the form-style screens; this commit closes the gap on the remaining four. (1) `app/onboarding/join.tsx:70` — Your Name (label="Your name") — the second on-ramp into the app, used by the secondary parent who joins via invite code. (2) `app/onboarding/join.tsx:83` — the 6-character invite code field (label="Invite code"); placeholder is the literal example "ABC123", so without the explicit label a SR user would hear "edited: A" with no anchor. (3) `app/food/add.tsx:89` — Food Name (label="Food name") — the most-used input on the food-add modal that a parent reaches from both the Foods tab and the Log Exposure form's "+ Add New" link. (4) `app/(tabs)/log.tsx:263` — Notes (label="Notes (optional)") in the Log Exposure form's optional details block; the existing v0.5.19 a11y push covered the meal/temperature/rating chip rows but missed this multiline TextInput because it lives inside a `Controller` render prop. No new tests — these are screen-level inputs that need a render harness; existing 275 across 23 suites still pass. Bumped `APP_VERSION` to v0.5.28. TypeScript clean.
- v0.5.27 — Fix: announce the interactive `StageIndicator` segments. `src/components/StageIndicator.tsx` rendered each of the 6 stage dots (tolerate/interact/smell/touch/taste/eat) as a bare `Pressable` when `interactive={true}` — no `accessibilityRole`/`accessibilityLabel`/`accessibilityState`. The interactive variant is the heart of the v0.2.0 hierarchy UX, mounted on the Log Exposure form (`app/(tabs)/log.tsx:157-162`) at `size="lg"` so a parent picks the stage their child reached for the current exposure. VoiceOver/TalkBack would read just the visible stage label ("Tolerate", "Interact", …) with no role hint and no current-state cue, so a parent using assistive tech could not tell which segment was the highlighted/scaled "current" dot or that the dots were tappable at all — the differentiating SOS feature was effectively invisible to screen readers. Fix follows the v0.5.19/v0.5.21/v0.5.25 chip pattern verbatim: `accessibilityRole="button"`, `accessibilityLabel={\`Stage: ${config.label}\`}` (dimension-prefixed so a SR user navigating across the food chip row + this stage row + the rating chip row + the meal/temperature chip rows hears which dimension each option belongs to), and `accessibilityState={{ selected: isCurrent }}` mirroring the existing `currentDot` style scale-up. The non-interactive read-only variant (food detail page progress display, line 176) is unchanged — it's an informational dot row, not a tappable surface. +2 component tests lock the contract: one asserts `getByLabelText('Stage: <label>')` resolves to a `<button role="button">` for all 6 segments on react-native-web; the other re-binds the existing `onStageSelect` click assertion to the labeled element so a future refactor that drops the label fails loudly. Bumped `APP_VERSION` to v0.5.27. 275 tests pass across 23 suites (was 273, +2). TypeScript clean.
- v0.5.26 — Fix: announce onboarding/add-child screen interactives. The `app/onboarding/add-child.tsx` route is the first screen a brand-new user sees after the welcome — they have just installed the app, tapped "Get Started" on `app/onboarding/index.tsx`, and arrived here to add their first child. Four unannounced interactive surfaces: (1) the 12-emoji avatar grid (lines 73-81) — bare `Pressable`s with no `accessibilityRole`/`accessibilityLabel`/`accessibilityState`, so a screen-reader user could not tell which avatar was selected nor that the buttons were tappable, (2-4) the three TextInputs (Name, Date of Birth, Notes) had no `accessibilityLabel` — VoiceOver would fall back to the placeholder, which only reads while the field is empty, leaving labeled-state announcements ("edited: Emma") unanchored to a field name. The post-onboarding `app/child/add.tsx` screen got the same fix in v0.5.22; this commit harmonizes the onboarding entry point. Four fixes following the v0.5.22 pattern verbatim: each emoji button gets `role=button`, `label="Avatar: <emoji>"` (prefixed for context — some screen readers will read the emoji name like "baby", giving "Avatar: baby"), and `state.selected` mirroring `selectedEmoji === emoji` so the SR announces "selected" as the highlight moves; the three TextInputs get explicit `accessibilityLabel`s ("Child's name", "Date of birth (optional)", "Notes (optional)") matching the visible `<Text style={styles.label}>` above each. Pulled `isSelected` out of the emoji `.map` callback into a local so the same `selectedEmoji === emoji` comparison isn't computed twice. The "Start Tracking" `Button` at line 146 already wraps `Pressable` internally with full a11y; no change needed there. Bumped `APP_VERSION` to v0.5.26. No new tests — these are screen-level interactives that need a render harness; existing 273 across 23 suites still pass. TypeScript clean.
- v0.5.25 — Fix: announce the RatingPicker chip row. `src/components/RatingPicker.tsx:13-32` rendered the 5-segment rating control (Refused/Reluctant/Neutral/Willing/Enjoyed) as bare `Pressable`s with no `accessibilityRole`/`accessibilityLabel`/`accessibilityState`. The picker is mounted inside the Log Exposure form's optional details block (where `app/(tabs)/log.tsx` calls `<RatingPicker value={...} onChange={...} />`), and is one of three chip rows in that form — Meal and Temperature got the v0.5.19 a11y treatment but the rating row was missed because it lives in its own component file rather than inline JSX. VoiceOver/TalkBack would read just the visible "Refused" / "Neutral" / etc. with no role hint and no selected-state cue, so a parent using assistive tech could not tell which rating was active or that the chips were tappable. Fix follows the v0.5.19 chip pattern verbatim: `accessibilityRole="button"`, `accessibilityLabel={\`Rating: ${config.label}\`}` (dimension-prefixed so a screen reader user navigating across the meal/temperature/rating chip rows hears which dimension each option belongs to), and `accessibilityState={{ selected: isSelected }}` mirroring `value === config.value`. Pulled `isSelected` out of the `.map` callback into a local so the same `value === config.value` ternary isn't computed three times per chip (once for `styles.selected`, once for `styles.selectedLabel`, once for the new `accessibilityState`). +2 component tests lock in the contract: one asserts `getByLabelText('Rating: <label>')` resolves to a `<button role="button">` for all 5 chips on react-native-web; the other re-binds `onChange` to the labeled element. Bumped `APP_VERSION` to v0.5.25. 273 tests pass across 23 suites (was 271, +2). TypeScript clean.
- v0.5.24 — Fix: announce the FoodCard outer Pressable. `src/components/FoodCard.tsx:27` rendered the whole card as a bare `Pressable` with no `accessibilityRole`/`accessibilityLabel` — VoiceOver/TalkBack read only the inner `<Text>` (the food name + stage badge label + "Safe Food" badge) with no role hint and no announced action. Misleading because v0.5.18's note says "the main FlashList items already have a11y via the `FoodCard` component itself" — only the inner safe-food star (added v0.3.0) had `accessibilityLabel="Safe food"` + `accessibilityRole="image"`; the *navigation surface* (the Pressable wrapper that routes to `/food/[id]`) was unannounced. This is the highest-traffic interactive in the app: it renders on the Foods tab list (every food), the safe-foods pinned row above it (v0.3.0), and the dashboard's recently-logged list (`app/(tabs)/index.tsx`) — every food-detail navigation entrance flows through this component. Fix follows the v0.5.18 safe-food chip pattern verbatim: `accessibilityRole="button"` + `accessibilityLabel={\`Open ${name} details\`}`. The food name is the only context a screen-reader user needs since the destination is the per-food detail page. +2 component tests lock in the contract: one asserts `getByLabelText('Open Broccoli details')` resolves to a `<button role="button">` element on react-native-web; the other re-binds the existing `onPress` assertion to the labeled element so a future refactor that drops the label fails loudly. Bumped `APP_VERSION` to v0.5.24. 271 tests pass across 23 suites (was 269, +2). TypeScript clean.
- v0.5.23 — Fix: announce food-detail back link and onboarding-index join link. Three small unannounced surfaces. (a) `app/food/[id].tsx` — the food detail page is the destination of every food-card tap on the Foods tab and from the dashboard's recently-logged list, so its back link is on the hot path. There were two `Pressable`s rendering the same "Back" link: one in the food-not-found branch (line 121, post v0.5.6 split) and one in the happy path (line 141). Both bare `Pressable`s with no `accessibilityRole`/`accessibilityLabel` — VoiceOver/TalkBack would read just "Back" with no role hint. (b) `app/onboarding/index.tsx` — the "Have an invite code? Join your family" link at line 88 (the only secondary CTA on the first-launch screen) was a bare `Pressable`. Three fixes following the v0.5.17-v0.5.22 pattern: each back link gets `role=button` + `label="Back"`; the join link gets `role=button` + `label="Have an invite code? Join your family"` (matches the visible text — passing through is correct here since the visible copy already reads as a complete phrase). The other `Pressable` on the food detail page (the Safe Food toggle at line 195) already has full a11y from v0.3.0, and the two `Button` components above it (Bump, Log Detailed Exposure) wrap `Pressable` internally with full a11y. Bumped `APP_VERSION` to v0.5.23. No new tests — these are screen-level Pressables that need a render harness; existing 269 across 23 suites still pass. TypeScript clean.
- v0.5.22 — Fix: announce child/add screen interactives. The "Add Child" route (`app/child/add.tsx`), reachable from the onboarding flow AND the Settings → Family card, had five unannounced interactive surfaces: (1) the Cancel back link in the header (line 64), (2) the 12-emoji avatar grid (lines 73-81) — bare `Pressable`s with no `accessibilityRole`/`accessibilityLabel`/`accessibilityState`, so a screen-reader user could not tell which avatar was selected nor that the buttons were tappable, (3-5) the three TextInputs (Name, Date of Birth, Notes) had no `accessibilityLabel` — VoiceOver would fall back to the placeholder, which only reads while the field is empty, leaving labeled-state announcements ("edited: Emma") unanchored to a field name. Five fixes following the v0.5.17-v0.5.21 pattern: Cancel gets `role=button` + `label="Cancel"`; each emoji button gets `role=button`, `label="Avatar: <emoji>"` (prefixed for context — some screen readers will read the emoji name like "baby", giving "Avatar: baby"), and `state.selected` mirroring `selectedEmoji === emoji` so the SR announces "selected" as the highlight moves; the three TextInputs get explicit `accessibilityLabel`s ("Child's name", "Date of birth (optional)", "Notes (optional)") matching the visible `<Text style={styles.label}>` above each. Bumped `APP_VERSION` to v0.5.22. No new tests — these are screen-level interactives that need a render harness; existing 269 across 23 suites still pass.
- v0.5.21 — Fix: announce Settings tab interactives. Three unannounced surfaces on the Settings tab (`app/(tabs)/settings.tsx`): (1) the "Add Child" row Pressable at line 86 (route to `/child/add`) had no `accessibilityRole`/`accessibilityLabel` — VoiceOver/TalkBack would read just "Add Child" with no role hint and no indication it was tappable; (2) the Theme chip row at lines 124-132 (Light/Dark/System) — bare `Pressable`s with no role, no label, and no `selected` state, so a screen-reader user could not tell which theme was active or that the chips were tappable (the Feeding Profile chips just below already had `role=button` + `state.selected` from earlier work — this commit harmonizes the Theme chips to match); (3) the "Sign Out" button at line 204, the only destructive action on the screen, was a bare `Pressable`. Three fixes following the v0.5.17-v0.5.20 pattern: Add Child gets `role=button` + `label="Add child"`; theme chips get `role=button`, `label="Theme: Light/Dark/System"` (dimension-prefixed so a user navigating across multiple chip rows hears which dimension each option belongs to), and `state.selected` mirroring `theme === t`; Sign Out gets `role=button` + `label="Sign out"`. Pulled the title-cased label out of each theme chip's JSX into a local inside the `.map` so it is not computed twice. Bumped `APP_VERSION` constant to v0.5.21. No new tests — these are screen-level Pressables that need a render harness; existing 269 across 23 suites still pass. TypeScript clean.
- v0.5.20 — Fix: announce Add Food + Dashboard interactives. (a) `app/food/add.tsx`: the Cancel header link, the 6-tile category grid, and the preparation-method chip row were bare `Pressable`s with no `accessibilityRole`/`accessibilityLabel`/`accessibilityState` — VoiceOver/TalkBack would read just the visible text ("Cancel" / "Protein" / "Roasted") with no role and no selected cue, so a parent using assistive tech could not tell which category they had picked or that the preparation chip was tappable. The category grid and prep chips already used the same per-row title-cased label twice (once for the inner `Text`, once needed for `accessibilityLabel`); pulled `label` out of each `.map` callback into a local so it is not double-computed. (b) `app/(tabs)/index.tsx`: the dashboard's "+ Log Exposure" summary card — the primary CTA on the home screen for routing to `/(tabs)/log` — was an unannounced `Pressable`. Added `accessibilityRole="button"` + `accessibilityLabel="Log new food exposure"` so the home tab's hot path is reachable by screen reader. Bumped to v0.5.20. 269 tests pass across 23 suites.
- v0.5.19 — Fix: announce Log Exposure form interactives. The food-pick chip row (line 132-139), the "+ Add New" link in the Food section header (126-128), the "Add More Details (optional)" / "Hide Details" toggle (174-178), and the Meal-type + Temperature chip rows in the optional details block were all bare `Pressable`s lacking `accessibilityRole`/`accessibilityLabel`/`accessibilityState`. Five fixes: (1) food chips now carry `role=button`, `label="Select <food>"`, `state.selected` mirroring the watched form value (`watch('foodId')`), so VoiceOver announces "selected" when the highlight changes; (2) "+ Add New" gets `role=button`, `label="Add new food"`; (3) details toggle gets `role=button` and a label that flips between "Show additional details" / "Hide additional details" so the announcement matches the action, plus `state.expanded` for the collapsible block; (4)+(5) meal and temperature chips get `role=button`, `label="Meal: Breakfast"` / `label="Temperature: Hot"` (dimension-prefixed so a screen reader user navigating across both rows hears which dimension each option belongs to), and `state.selected` mirroring the form value. Pulled the title-cased label out of each chip's JSX into a local inside the `.map` so it is not computed twice. No new tests — these are screen-level Pressables that need a render harness; existing 269 across 23 suites still pass. Bumped to v0.5.19.
- v0.5.18 — Fix: announce Foods tab interactives. The "+ Add" header button (114), the search input (121-127), the category filter chip row (132-149), and the safe-food chips in the pinned row (89-97) were bare `Pressable`s / a `TextInput` with no `accessibilityRole`/`accessibilityLabel`/`accessibilityState`. VoiceOver/TalkBack would read just the visible text ("All", "Protein", "+ Add", or nothing for the search input) with no role and no selected-state cue — a parent using assistive tech could not tell which filter was active. Four fixes: (1) "+ Add" gets `role=button`, `label="Add new food"`; (2) search input gets `accessibilityLabel="Search foods"`; (3) the All chip plus all 8 category chips get `role=button`, `label="Filter by <category>"`, and `state.selected` reflecting the current `selectedCategory`; (4) safe-food chips get `role=button` + `label="Open <name> details"`. The main FlashList items already have a11y via the `FoodCard` component itself, so no change there. No new tests — these are screen-level Pressables that need a render harness; existing 269 across 23 suites still pass. Bumped to v0.5.18.
- v0.5.17 — Fix: surface the ChildSelector chips and the Add Food "Safe Food" toggle to assistive tech. Two interactive surfaces were unannounced: (a) `src/components/ChildSelector.tsx` rendered each child as a bare `Pressable` — VoiceOver/TalkBack would read the visible text ("👧 Emma") with no role, no label, no selected-state indication, so a parent using a screen reader could not tell which child was active or that the chip was tappable; (b) the Safe Food toggle on `app/food/add.tsx:136-145` was a `Pressable` with no `accessibilityRole`/`accessibilityLabel`/`accessibilityState`, even though the equivalent toggle on `app/food/[id].tsx:195-213` already had the full pattern (`accessibilityRole="switch"`, `accessibilityLabel`, `accessibilityState={{ checked }}`) — it was just missed when the detail-page version was added in v0.3.0. The fix adds `accessibilityRole="button"` + `accessibilityLabel="Select <name>"` + `accessibilityState={{ selected: isSelected }}` to each ChildSelector chip, and copies the food/[id].tsx switch pattern verbatim onto the Add Food Safe Food toggle. +2 ChildSelector tests lock in the role + label contract (the per-platform mapping of `accessibilityState.selected` → DOM is RN-internal — react-native-web doesn't serialize it for `button` role, so the web harness can only verify role + label; native iOS/Android honor the prop). 264 tests pass across 23 suites. Bumped to v0.5.17.
- v0.5.16 — Fix: "Add Another" on the Add Food screen now resets the form. `app/food/add.tsx` previously rendered the post-save `Alert.alert('Added!', …)` with two buttons — "Add Another" (no `onPress`) and "Done" (`router.back()`). Tapping "Add Another" simply dismissed the alert, leaving every field as the parent had typed it: the freshly-saved food's name still occupied the input, the category chip remained highlighted, the preparation chip stayed selected, and the safe-food toggle persisted. A parent batch-adding three foods at the start of a week had to manually delete the previous name on top of the placeholder before each next add — small individually, painful in a row of three. The fix mirrors `app/(tabs)/log.tsx:88`'s post-submit pattern: pull `reset` from the `useForm` destructure and call `reset({ name: '', category: 'other', defaultPreparation: undefined, isSafeFood: false })` from a new `onPress` handler on the "Add Another" button. Defaults match `defaultValues` at line 23-27 exactly so a re-rendered "fresh" form is indistinguishable from the initial mount. No new test — this is alert-callback behavior that requires a render harness plus `Alert` mock; the change is targeted UX clarity matching an established in-repo pattern. Bumped to v0.5.16. 262 tests pass across 23 suites.
- v0.5.15 — Fix: distinguish "no child selected" from "no exposures yet" on the food detail page. `app/food/[id].tsx:37-55` already short-circuits the exposures query when `selectedChildId` is null (the v0.1.1 null-child guard), but the screen still rendered the "Bump to {next stage}" button (because `canBumpStage(null)` returns true — first-stage bump is valid for an empty history) and showed the generic "No exposures yet for this food" empty state. Result: a parent who deep-links into a food page after clearing their child selection (or whose `selectedChildId` is briefly null between `ensureSelection` resolutions) saw a clickable Bump button that no-ops silently in `handleBumpStage` (line 66 early-returns on `!selectedChildId`), and an empty exposure list that misleadingly suggests "this child has never tried this food" rather than "no child is selected to view history for". Two changes: (a) the Bump button is now gated on `selectedChildId && canBumpStage(highestStage)` so it disappears when no child is selected, removing the silent-no-op surface; the secondary "Log Detailed Exposure" button promotes to primary in that case so the user still has a clear primary action. (b) The Exposure History section now renders a distinct empty-state ("Select a child on the dashboard to see exposure history.") when `!selectedChildId`, separate from the existing "No exposures yet for this food" copy that fires on `exposuresList.length === 0` with a child selected. The food card itself (name, category, isSafeFood toggle) still renders fine without a child — those are food-scoped, not child-scoped. No new tests — this is screen-level conditional rendering that requires a render harness; the change is targeted UX clarity for an existing edge case. Bumped to v0.5.15. 262 tests pass across 23 suites.
- v0.5.14 — Fix: surface DB-load failures on Dashboard and Progress tabs. `app/(tabs)/index.tsx:81` and `app/(tabs)/progress.tsx:51` previously swallowed errors with `console.error` only, leaving the user staring at zeroed cards or stale data with no signal that something went wrong — indistinguishable from "I haven't logged anything yet". `app/(tabs)/foods.tsx:48-50` already showed `Alert.alert('Error', '...')` on the same failure mode; this commit harmonizes the two outliers to that pattern. Wording matches the existing copy ("Failed to load <thing>. Please try again."). No new tests — these are screen-level integration paths that would need a render harness; the change is a one-line behavior addition, not a logic refactor. Bumped to v0.5.14. 262 tests pass across 23 suites.
- v0.5.13 — Fix: single-source the displayed app version. The Settings tab's About card hardcoded `v0.5.6` at `app/(tabs)/settings.tsx:192` and was never bumped during the v0.5.7 → v0.5.12 refactor train, so users opening Settings saw a 6-patch-stale version — confusing for "what's installed" and useless for bug reports. Added `APP_VERSION` constant to `src/lib/constants.ts` and read it in Settings via the `@/src/lib/constants` import. New unit test pins the format (`/^v\d+\.\d+\.\d+$/`) so any future contributor who removes the `v` prefix or breaks semver gets a failing test, not a silent UI regression. Bumped to v0.5.13. 262 tests pass across 23 suites.
- v0.5.12 — Refactor: drop the parallel `selectedFoodId` / `selectedStage` `useState` from the Log Exposure form (`app/(tabs)/log.tsx`) and derive both from the form's own state via `watch('foodId')` / `watch('stage')`. The screen previously held two `useState` slots that mirrored the form's `foodId` and `stage` fields; every chip tap and stage tap had to write to *both* (`setSelectedFoodId(food.id); setValue('foodId', food.id)`), which created a drift-by-construction bug surface — any future contributor adding a new code path that wrote to the form (`setValue`, `reset`) without touching the React state would leave the chip highlight pointing at one selection while the form (and validation/submit) carried another. The post-submit cleanup at the end of `onSubmit` had to call all three reset paths in lockstep (`reset({...}); setSelectedFoodId(null); setSelectedStage('tolerate');`) for the same reason. Removed both `useState` slots; UI selection now reads from `watch()`, so `reset()` flows back to the highlight automatically. Cleaned up 1 unused type import (`ExposureStage`). The remaining v0.5.7 `useEffect` that mirrors `selectedChildId` from the child store into the form is untouched — that one is genuinely cross-store sync, not duplicate state. No behavior change on the happy path; the eliminated drift is structural. 235 tests still pass across 21 suites; TypeScript clean.
- v0.5.11 — Refactor: route `calcProgressStats` (`src/lib/progress-stats.ts`) through canonical `getHighestStage` instead of an inline `STAGE_ORDER.indexOf` comparison. The previous loop set `highestStagePerFood[foodId] = stage` even when `stage` was a legacy/unknown string, then bucketed it into `stageCounts` — phantom keys like `stageCounts.mystery` would persist and never render under the JSX (which iterates `STAGE_ORDER`), silently dropping foods from the Stage Distribution widget on the Progress tab. Same drift v0.5.8 fixed for the Foods tab and v0.5.10 fixed for the dashboard, here for the progress screen. Restructured: a single pass buckets exposures by `foodId` into a `Map`, then a second pass calls `getHighestStage` on each bucket (skipping unknown stages) and feeds both `highestStagePerFood` and `stageCounts`. `totalFoods` switched from `Object.keys(highestStagePerFood).length` to `Object.keys(exposureCountPerFood).length` — semantic shift that matches the UI label "Foods Tried": a food with only unknown-stage rows is still a tried food, even if its highest stage can't be classified. +1 unit test asserts this exact split (food a with two unknown stages, food b with one valid stage → totalFoods=2, totalExposures=3, stageCounts={eat:1}). 235 tests pass across 21 suites.
- v0.5.10 — Refactor: extract `computeStageCounts(exposures)` into `src/lib/food-partition.ts` and route the dashboard's stage-distribution computation through it. The dashboard at `app/(tabs)/index.tsx:76-87` previously held a 12-line inline pass that grouped exposures by `foodId`, picked the highest stage per food using `STAGE_ORDER.indexOf` directly (without the unknown-stage skip from canonical `getHighestStage`), then re-iterated to bucket counts — the same drift v0.5.8 fixed for the Foods tab, but here for the home screen's "Stage Distribution" widget. The helper delegates per-food highest-stage selection to `getHighestStage`, so a legacy/unknown stage string in `exposures` no longer corrupts the count map (previously, `counts['mystery']` could appear and never render in the STAGE_ORDER-driven JSX, silently dropping foods from the distribution). Dashboard `loadData` is now a 1-liner over the helper. +5 unit tests cover empty input, single-food/highest-stage selection, multi-food aggregation at the same stage, all-unknown skip, and mixed-known/unknown selection. 234 tests pass across 21 suites.
- v0.5.9 — Refactor: extract `filterFoods(foods, search, category)` from `app/(tabs)/foods.tsx` into `src/lib/food-partition.ts`. The Foods tab held a 5-line inline filter that combined a substring search (lowercased on every keystroke) with a category equality check; now a 1-liner over the helper. Two small correctness improvements come along: whitespace-only search input no longer narrows results (was previously calling `.includes(' ')` which still matched everything but lowercased pointlessly), and the no-op fast path (empty search + "all" category) returns a fresh array slice rather than re-running `.filter` over every render. +9 unit tests cover the no-op path returning a distinct copy, case-insensitive matching, substring matching, category-only filtering, search+category AND, no-match emptiness, whitespace-only no-op, empty-list passthrough. 229 tests across 21 suites.
- v0.5.8 — Refactor: extract `buildFoodsWithStats(foods, exposures)` from `app/(tabs)/foods.tsx` into `src/lib/food-partition.ts`. The screen previously embedded a 25-line block that grouped exposures by `foodId` into a `Map`, then ran an inline loop to compute `highestStage` per food using `STAGE_ORDER.indexOf` with a comparison that did not skip unknown stage strings — inconsistent with the canonical `getHighestStage` helper used by `app/food/[id].tsx` (extracted in v0.5.4), so the two screens could disagree on a food's highest reached stage if any exposure row carried a legacy/unknown stage value. The new helper delegates to `getHighestStage`, harmonizing the two read paths. Foods screen `loadFoods` is now a 1-liner over the helper. +7 unit tests covering empty inputs, foods with no exposures, exposure counting, highest-stage selection, unknown-stage skip behavior, all-unknown returning `undefined`, input order preservation, and orphan-foodId tolerance. 220 tests pass across 21 suites.
- v0.5.7 — Fix: Log Exposure form's `childId` value now stays in sync with `useChildStore`. Two related defects: (1) `defaultValues.childId` snapshots `selectedChildId` once at form mount; if `ensureSelection` resolved the child *after* the form mounted (or the user switched child from another tab), the form held `childId=''` while the on-screen ChildSelector showed a child as selected — tapping Save then failed silently with the inline "Select a child" error from v0.5.1, even though the child appeared chosen. (2) After a successful submit, `reset()` restored that mount-time empty default, forcing the parent to re-tap the child for every subsequent exposure in a session — annoying since the SOS pattern is multiple short exposures per meal. Now: a `useEffect` mirrors `selectedChildId` into the form via `setValue('childId', ...)` whenever it changes, and the post-submit `reset({ childId: data.childId, foodId: '', stage: 'tolerate' })` preserves the just-used child so back-to-back logging works. No new tests (the bug only manifests through `useForm` + react-hook-form's lifecycle, which is not unit-testable without a render harness; behavior verified by re-running the existing 213 tests across 21 suites).
- v0.5.6 — Fix: distinguish loading vs not-found on the food detail page. `app/food/[id].tsx` previously rendered `null` whenever `food` was falsy, so the user saw a blank white screen during the initial DB query AND forever if the `id` route param pointed at a deleted/invalid row. Now: tracks a `loading` flag (default true, cleared in a finally), shows an `ActivityIndicator` while loading, and renders an `EmptyState` ("🤔 Food Not Found") with a "Go to Foods" action that `router.replace`s to the foods list when the row genuinely doesn't exist. The query no longer silently keeps stale food state when the new id misses (was `if (foodResult[0]) setFood(...)`, now `setFood(foodResult[0] ?? null)`).
- v0.5.5 — Refactor: extract `getEncouragementMessage(stats)` from `app/(tabs)/progress.tsx` into `src/lib/progress-stats.ts`. The encouragement copy under the progress tab was a 9-line nested ternary embedded in JSX — untestable and easy to reword incorrectly. Now a pure function with 4 named branches: zero exposures, early (<10), near-target (uses `foodProgress[0].threshold` with a 15 fallback), and established. +6 unit tests cover the empty/early/near/established branches plus the 10-exposure boundary and the threshold fallback. No copy or behavior change.
- v0.5.4 — Refactor: extract `getHighestStage(exposures)` helper from `app/food/[id].tsx` into `src/lib/stage.ts`, alongside the existing `getNextStage` / `canBumpStage`. The detail screen previously embedded an 8-line manual loop computing the highest reached stage from the in-memory exposures list — now a 1-line helper call. The helper iterates once, ignores unknown stage strings, and returns `null` for the empty case (mirroring how `STAGE_ORDER.indexOf` would behave for missing values). +6 unit tests covering empty list, single exposure, highest-among-many, terminal-stage-wins, unknown-stage skip, and all-unknown null. No behavior change.
- v0.5.3 — Refactor: extract `calcProgressStats(foods, exposures, profile, now?)` pure helper from `app/(tabs)/progress.tsx` into `src/lib/progress-stats.ts`. The previous `loadStats` callback mixed 80+ lines of data crunching (highest stage per food, stage/category counts, weekly window, per-food progress sort, foodsNearTarget) with React state and was untestable. The screen now does a 1-line call and only owns the React effect. The helper accepts an injectable `now` parameter so the trailing-7-day weekly window is deterministic. +9 unit tests covering empty inputs, safe-food count independence, highest-stage tracking, weekly boundary (Date and number), avgRating with/without ratings, sort order with ties, foodsNearTarget threshold semantics, and the picky profile (threshold 20). No behavior change.
- v0.5.2 — Foods tab now distinguishes "no foods at all" from "no matches for current filter". Previously the screen showed "No Foods Yet → Add Food" any time `filteredFoods.length === 0`, even if the user had foods and just typed a search that matched none — a misleading CTA that nudged duplicates. Added `getEmptyStateKind(totalCount, filteredCount)` to `src/lib/food-partition.ts` returning `'none' | 'filtered' | 'has-results'`. The "filtered" state shows a "🔎 No Matches" EmptyState with a "Clear Filters" action that resets `searchQuery` and `selectedCategory`. +4 unit tests for the helper.
- v0.5.1 — Log Exposure form now surfaces inline validation errors. `app/(tabs)/log.tsx` previously destructured `errors` from `useForm` but never rendered them, so tapping "Save Exposure" without picking a child or food failed silently. Added `errors.childId` / `errors.foodId` text below their respective sections (matches the pattern already used in `app/food/add.tsx`) and added `foodId: ''` to `defaultValues` so zod surfaces the error reliably. New `error` style mirrors `theme.colors.error`.
- tests: raise `src/lib/export.ts` coverage from 70.83% → 95.83% lines (overall 85.5% → 88.5%). New `src/lib/__tests__/export.integration.test.ts` covers `fetchExportRows` (empty, Date passthrough, numeric-timestamp coercion, query-builder call order) and `exportChildData` (share-sheet invocation with filename+CSV, header-only empty export, error propagation). +7 tests, no version bump per NEXT_STEPS.md P6 guidance.
- v0.5.0 — CSV data export (therapist-ready offline feature): new `src/lib/export.ts` provides pure `formatExposuresCsv`, `csvEscape`, `buildExportFilename` helpers (RFC 4180 quote-escape for commas, quotes, newlines) plus `fetchExportRows` (exposures JOIN foods, ordered by `occurredAt desc`) and `exportChildData` (uses React Native's built-in `Share` API — no new packages). Settings tab gains a "Data → Export Data (CSV)" row that opens the system share sheet with CSV content. 16 new tests cover the escape/format/filename logic.
- v0.4.0 — Per-food exposure progress vs profile-based threshold: new `src/lib/thresholds.ts` exposes `EXPOSURE_THRESHOLDS = { typical: 15, picky: 20, arfid: 30 }` and a `calcExposureProgress` helper. `feedingProfile` added to `settings-store` (default `typical`) with a chip selector on the Settings tab. Progress tab now lists each food with logged exposures alongside a `current/threshold` ProgressBar, sorted by proximity to threshold, with a check when reached.
- v0.3.0 — Safe-food availability surface: Foods tab pins a "⭐ Safe Foods" horizontal row above the main list when ≥1 safe food exists. FoodCard renders a star next to the name. Food detail page has a tap-to-toggle "Mark as Safe Food" control that persists to the `foods` table. Added pure `partitionSafeFoods` helper in `src/lib/food-partition.ts` with unit tests.
- v0.2.0 — Exposure hierarchy UX: "Bump to {next stage}" one-tap primary action on food detail page; writes new `exposures` row (no mutation). Confirmed log form already surfaces all 6 stages at lg size with `tolerate` default. Added `src/lib/stage.ts` helpers (`getNextStage`, `canBumpStage`).
- v0.1.1 — Null-child safety pass: `ensureSelection` on child-store, auto-repair of stale/null child id in tab layout, onboarding redirect when no children, progress-screen loading-forever fix.
- v0.1.0 — Initial versioning rules added to CLAUDE.md
